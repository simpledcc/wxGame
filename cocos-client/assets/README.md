# Assets

This folder is owned by Cocos Creator.

Current migration source:

- `scripts/`: typed domain, stores, platform services, gameplay orchestration, scene controllers, and theme bindings.
- `resources/`: shared runtime resources.
- `bundles/`: `theme_default` and `theme_island` Asset Bundle sources.
- `scenes/`: Cocos scenes generated by the editor.

