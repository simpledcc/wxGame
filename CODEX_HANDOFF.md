# Codex Handoff

> Active workspace is always the current Git worktree root; do not reuse another computer's absolute path.
>
> H4 baseline/high-fidelity integration and H8.4-H8.6 are complete. The user temporarily deferred Phase 9 and reopened pre-game art direction as H8.7 visual polish. Keep `miniprogram/` as the stable uploadable client during migration. Other absolute paths below are historical machine records.

## Project

WeChat Mini Game project: current Git worktree root (`D:\demo\wexin` during the latest 2026-07-14 documentation sync; never hard-code this path on another computer).

Product concept: a multi-mode vocabulary game platform with one portrait Home and preparation flow. Players select a word bank and mode, create/join a room and prepare before the selected gameplay Bundle takes over. Existing PK/shared/spell implementations and historical grass/insect or island/fish presentation remain gameplay-owned compatibility, not the product-level preparation identity.

## Current State

Resume point for the next agent:

- 2026-07-18 H8.7 sixtieth source visual checkpoint: shared `sectionCard` tabs move visual-title centers from compact/large `10/18` to `14/22` and tighten title widths by 8px. Compact formal-icon/title spacing rises from 3px to 11px; large spacing rises from 1px to 9px, while left/right insets remain at least 8px. Foundation tests cover both tab sizes and the visible UI contract enforces every mounted route tab. Card sizes, tab colors, icon slots, copy, actions and data are unchanged. Focused tests pass at `1,519,984 / 1,520,000`. No gameplay, bitmap, metadata, cloud/protocol, legacy client or AppID changed. Creator/WeChat visual review remains pending; not `dev_done`.
- 2026-07-18 H8.7 fifty-ninth source visual checkpoint: every shared page-header icon plate moves `y=14→18`; titles move `x=62,y=18,w=310→x=66,y=22,w=302` with the same right edge. Headers now provide 8px title/divider, 9px divider/subtitle, 8px icon/subtitle and 12px horizontal icon/title separation plus 14/15px top/bottom insets. Foundation tests and every mounted long/minimum route enforce the geometry. Back actions, backdrop, formal icon, copy, SafeArea and navigation are unchanged. Focused tests pass at `1,519,984 / 1,520,000`. No gameplay, bitmap, metadata, cloud/protocol, legacy client or AppID changed. Creator/WeChat visual review remains pending; not `dev_done`.
- 2026-07-18 H8.7 fifty-eighth source visual checkpoint: Home's real Bank-name rectangle moves `y=-14→-15` and narrows `326x34→320x34`, raising caption/name spacing from 7px to 8px and name/Change-badge from 5px to 8px. Long and true `640x960` trees also freeze formal icon/name at 8px; compact Home retains 8px below the name. Twelve-times repeated long names remain `SHRINK`, the full strip remains one route target and the Change badge remains presentation-only. Focused tests pass at `1,519,984 / 1,520,000`. No Bank data, route behavior, gameplay, bitmap, metadata, cloud/protocol, legacy client or AppID changed. Creator/WeChat visual review remains pending; not `dev_done`.
- 2026-07-18 H8.7 fifty-seventh source visual checkpoint: both `30x30` Room status indicators move `y=-10→-13`, raising compact title-tab clearance from 5px to 8px while retaining a 7px bottom inset. Both player cards now require at least 8px across tab/avatar, tab/status, status/name, avatar/status and avatar/name boundaries; existing geometry provides 16-47px. Room-code, Bank and status compact cards also require 8px tab/body clearance on long and true `640x960` trees. Quiet polling, waiting/ready/error switching, real names, copy/invite, ready and start highlight are unchanged. Focused tests pass at `1,519,984 / 1,520,000`. No room protocol, gameplay, bitmap, metadata, cloud function, legacy client or AppID changed. Creator/WeChat visual review remains pending; not `dev_done`.
- 2026-07-18 H8.7 fifty-sixth source visual checkpoint: create configuration moves the selected-mode `92x92` icon `y=-18→-21` and selected-Bank `66x66` icon `y=-23→-26`, raising both tab/icon clearances from 5px to 8px. The real Bank-name rectangle tightens `300x54→300x46` at the same center, raising tab/name clearance from 4px to 8px while preserving long-name `SHRINK`. Long and true `640x960` trees verify tab/body plus icon/title, icon/name and name/Change-Bank boundaries at 8px. Creation settings, data and handlers are unchanged. Focused tests pass with core source unchanged at `1,519,984 / 1,520,000`. No gameplay, bitmap, metadata, cloud/protocol, legacy client or AppID changed. Creator/WeChat visual review remains pending; not `dev_done`.
- 2026-07-18 H8.7 fifty-fifth source visual checkpoint: all eight `548x80` catalog rows tighten title rectangles from `236x34@y=18` to `236x28@y=19`; existing `236x28@y=-17` subtitles now yield 7px top, 8px title/subtitle and 9px bottom rhythm. Long and true `640x960` trees verify every row plus at least 8px subtitle/badge/action column separation. Accent rails, formal icons, first-mode enabled behavior, seven disabled `筹备中` actions, rules entry and mode routing are unchanged. Focused tests pass with core source unchanged at `1,519,984 / 1,520,000`. No mode availability, gameplay, bitmap, metadata, cloud/protocol, legacy client or AppID changed. Creator/WeChat visual review remains pending; not `dev_done`.
- 2026-07-18 H8.7 fifty-fourth source visual checkpoint: all six Help rule titles tighten from `430x28@y+17` to `430x24@y+20`, producing exact 8px title/body separation instead of 3px. Number badges, 430x36 body rectangles, adaptive row pitch, summary/icon area and card bounds stay fixed; minimum inter-row spacing remains at least 8px. Long and true `640x960` trees verify real rule text plus badge/title, title/body and inter-row boundaries. Help navigation is unchanged. Focused tests pass with core source unchanged at `1,519,984 / 1,520,000`. No Help content, gameplay, bitmap, metadata, cloud/protocol, legacy client or AppID changed. Creator/WeChat visual review remains pending; not `dev_done`.
- 2026-07-18 H8.7 fifty-third source visual checkpoint: Study keeps its `540x360` card and all node centers/actions fixed while tightening progress status `120x32→120x24`, word `480x80→480x76` and meaning `480x72→480x68`. Caption/status/progress/word/meaning/bottom actions now form a complete at-least-8px chain instead of 4/4/6/6/6px on long and true `640x960` trees; bottom actions retain 8px insets. Store-backed navigation, reveal/show, wrong-word state, progress and Bank routing are unchanged. Focused tests pass with core source unchanged at `1,519,984 / 1,520,000`. No Study data, gameplay, bitmap, metadata, cloud/protocol, legacy client or AppID changed. Creator/WeChat visual review remains pending; not `dev_done`.
- 2026-07-18 H8.7 fifty-second source visual checkpoint: Bank rows now create their intended subtitle labels before synchronous rendering, fixing the prior silent omission of real “word count · unlocked/locked” copy. All four `540x96` rows move title `y=18→21` and subtitle `y=-22→-24`, producing 6px top, 8px title/subtitle and 8px bottom spacing on long and true `640x960` trees. Dynamic Bank names remain shrinkable; icons, badges, selection, paging, unlock and confirmation behavior are unchanged. Full verification and WeChat dry-run pass at `1,519,984 / 1,520,000`. No Bank data, persistence, gameplay, bitmap, metadata, cloud/protocol, legacy client or AppID changed. Creator/WeChat visual review remains pending; not `dev_done`.
- 2026-07-18 H8.7 fifty-first source visual checkpoint: Feedback's multiline native EditBox changes from `500x202` to `500x196`, moves up two pixels, and the Contact caption moves up two. Prompt/privacy/content-caption/content-input/contact-caption/contact-input/status-band now form a complete at-least-8px internal chain instead of 8/10/7/5/6/8px. The outer `560x500` card and 12/8/8px action/SafeArea rhythm, 300-character count, validation, native layering and submit/privacy handlers remain unchanged. Full verification and WeChat dry-run pass with core source unchanged at `1,519,972 / 1,520,000`. No feedback data, cloud/protocol, gameplay, bitmap, metadata, legacy client or AppID changed. Creator/WeChat visual review remains pending; not `dev_done`.
- 2026-07-18 H8.7 fiftieth source visual checkpoint: History Detail moves its existing title up eight pixels and its page label/Previous/Next group down six. Title/body separation rises from 1px to 9px and body/navigation from 2px to 8px; the title retains 27px below the card top and 80px navigation retains 12px above the card bottom. Detail data, bounded spell pages, previous/next state and close behavior remain unchanged. Full verification and WeChat dry-run pass with core source unchanged at `1,519,972 / 1,520,000`. No history data, route behavior, gameplay, bitmap, metadata, cloud/protocol, legacy client or AppID changed. Creator/WeChat visual review remains pending; not `dev_done`.
- 2026-07-18 H8.7 forty-ninth source visual checkpoint: Home's Settings modal grows from `500x330` to `500x350`; its existing settings icon/title, real audio status, 80px stateful toggle and 80px Close action move up seven pixels as one group. Content top/bottom insets become 19/18px instead of the old one-pixel bottom clearance, while icon/status, status/toggle and toggle/Close gaps remain 11/10/8px. Persisted audio state, selection visuals and handlers are unchanged. Full verification and WeChat dry-run pass with core source reduced to `1,519,972 / 1,520,000`. No settings data, route behavior, gameplay, bitmap, metadata, cloud/protocol, legacy client or AppID changed. Creator/WeChat visual review remains pending; not `dev_done`.
- 2026-07-18 H8.7 forty-eighth source visual checkpoint: Home's Player Information modal grows from `500x360` to `500x400` and redistributes its existing title, 96px formal/fallback avatar, real Store-backed name, truthful identity badge and Close action. This removes the previous five-pixel title/avatar rectangle overlap and produces 11/11/8/9px inter-item gaps plus 13/12px content top/bottom insets. Modal behavior, player data and the avatar single-entry target remain unchanged. Full verification and WeChat dry-run pass with core source unchanged at `1,519,973 / 1,520,000`. No fake identity, route behavior, gameplay, bitmap, metadata, cloud/protocol, legacy client or AppID changed. Creator/WeChat visual review remains pending; not `dev_done`.
- 2026-07-18 H8.7 forty-seventh source visual checkpoint: Join Room moves only its title one pixel up plus the native room-code input and invite hint three pixels down. The existing icon, title, format hint, input, invite hint and primary Join action now form an at-least-8px vertical chain on long and true `640x960` trees; card/header and card/SafeArea bounds remain covered. The `560x600` card, `500x104` EditBox, `500x110` action, counters, validation, disabled/request states and production join handler are unchanged. Full verification and WeChat dry-run pass at `1,519,973 / 1,520,000`. No room data/protocol, gameplay, bitmap, metadata, cloud, legacy client or AppID changed. Creator/WeChat visual review remains pending; not `dev_done`.
- 2026-07-18 H8.7 forty-sixth source visual checkpoint: Help replaces its centered monolithic body label with six responsive rule rows. Each row uses an existing semantic theme color for a `44x34` numbered badge, then separates an 18px title and concise 14px explanation by at least three pixels; rows retain at least eight pixels on true `640x960` and expand with the card on taller screens. The same six rule facts remain owned by `HelpScene`, and Back still returns to the mode catalog. Full verification and WeChat dry-run pass at `1,519,972 / 1,520,000`. No gameplay rule/route behavior, bitmap, metadata, gameplay Bundle, cloud/protocol, legacy client or AppID changed. Creator/WeChat visual review remains pending; not `dev_done`.
- 2026-07-18 H8.7 forty-fifth source visual checkpoint: Feedback's Submit action moves up one pixel and Privacy action moves up three, producing 12px form/Submit, 8px Submit/Privacy and 8px Privacy/SafeArea-bottom gaps at true `640x960`. The 500px form, 94/80px actions, native SafeArea EditBoxes, counters, validation, disabled/success/failure states and privacy route remain unchanged. Full verification and WeChat dry-run pass with core source unchanged at `1,519,835 / 1,520,000`. No feedback data, route behavior, gameplay, cloud, protocol, assets, metadata, legacy client or AppID changed. Creator/WeChat visual review remains pending; not `dev_done`.
- 2026-07-18 H8.7 forty-fourth source visual checkpoint: Home's 34px coin-add badge moves two pixels inward. Formal coin/value, value/add and add/right-edge gaps now all retain at least 4px inside the unchanged `206x80` single action across long, true `640x960` and refreshed nine-digit coin states. The 82px shrinkable real-value column, 26px plus, dark pill, Bank route and no-nested-Button boundary remain unchanged. Full verification and WeChat dry-run pass with core source unchanged at `1,519,835 / 1,520,000`. No coin data, route behavior, gameplay, cloud, protocol, assets, metadata, legacy client or AppID changed. Creator/WeChat visual review remains pending; not `dev_done`.
- 2026-07-18 H8.7 forty-third source visual checkpoint: Bank's status card and four 96px Bank rows move down two pixels as one group. Shared header/status, status/first row and fourth row/pagination now all retain at least 4px on long and true `640x960` trees; the remaining row, pagination and bottom dual-action gaps stay at or above the same gate. Card content, paging, selection, unlock rollback, confirmation, 80px controls and SafeArea bottom remain unchanged and covered. Full verification and WeChat dry-run pass with core source unchanged at `1,519,835 / 1,520,000`. No Bank data, route behavior, gameplay, cloud, protocol, assets, metadata, legacy client or AppID changed. Creator/WeChat visual review remains pending; not `dev_done`.
- 2026-07-18 H8.7 forty-second source visual checkpoint: long-screen Home moves only the current-Bank strip down two pixels, raising subtitle-ribbon/Bank separation from 2px to 4px while retaining 13px before the primary Create action. The compact `640x960` formula and its existing 4px gap remain unchanged, as do Bank content, single click target, dynamic name, badge and route. Long/minimum contracts now require the visible brand/configuration separation and primary-action clearance. Full verification and WeChat dry-run pass with core source unchanged at `1,519,835 / 1,520,000`. No Home data, route behavior, gameplay, cloud, protocol, assets, metadata, legacy client or AppID changed. Creator/WeChat visual review remains pending; not `dev_done`.
- 2026-07-18 H8.7 forty-first source visual checkpoint: Study's true `640x960` chain is rebalanced from the shared header through Bank strip, a tightened `540x360` learning card, Reveal/Wrong actions, Meaning toggle and Next action. Every external boundary now retains at least 4px, the card's `220x80` Previous/Random actions keep 8px inside its bottom edge and Next keeps 6px above the SafeArea bottom. Long-screen interpolation, all 80px targets, real Study state/actions and Bank return behavior remain unchanged. Full verification and WeChat dry-run pass with core source unchanged at `1,519,835 / 1,520,000`. No Study data, route behavior, gameplay, cloud, protocol, assets, metadata, legacy client or AppID changed. Creator/WeChat visual review remains pending; not `dev_done`.
- 2026-07-18 H8.7 fortieth source visual checkpoint: every shared supporting-page header moves its divider and subtitle down two pixels, raising title/divider and semantic-icon/subtitle gaps from 2px to 4px without changing the 116px header, Back target, icon plate or text widths. History's filter row also moves down two pixels so its header gap reaches 4px while gaining space before the summary cards. The visible-UI contract enforces the stronger header hierarchy across all routes and the true `640x960` History tree. Full verification and WeChat dry-run pass with core source unchanged at `1,519,835 / 1,520,000`. No copy, route behavior, gameplay, cloud, protocol, assets, metadata, legacy client or AppID changed. Creator/WeChat visual review remains pending; not `dev_done`.
- 2026-07-18 H8.7 thirty-ninth source visual checkpoint: History's Recent and Best summary cards remove the duplicate 44px body icon because each compact title tab already owns the same formal semantic icon. Both real-data summary columns are centered and expanded from 190px to 236px, leaving 18px horizontal card insets for the two-line local timestamp and dynamic best score. Long and true `640x960` contracts require no direct body icon, exactly one title-tab icon and the expanded centered text geometry. Full verification and WeChat dry-run pass with core source reduced to `1,519,835 / 1,520,000`. No history data, formatting, route behavior, gameplay, cloud, protocol, assets, metadata, legacy client or AppID changed. Creator/WeChat visual review remains pending; not `dev_done`.
- 2026-07-18 H8.7 thirty-eighth source visual checkpoint: the shared action hierarchy now adapts icon size, inset, gap and right padding below 120px, allowing History row Detail to replace its plain-text control with a formal `history` icon and pure title inside the unchanged `92x80` target. Long and true `640x960` contracts require icon/title separation plus 8/4px inner edges; existing dynamic title/score/detail columns and real open/page/close behavior remain covered. Full verification and WeChat dry-run pass at `1,519,952 / 1,520,000`; only 48 bytes remain, so the next source change must reclaim space first. No history data, route behavior, gameplay, cloud, protocol, assets, metadata, legacy client or AppID changed. Creator/WeChat visual review remains pending; not `dev_done`.
- 2026-07-18 H8.7 thirty-seventh source visual checkpoint: create configuration's Change Bank control replaces its text-only button with the shared compact action hierarchy and imported `wordBank` icon. The unchanged `118x80` action keeps at least four pixels between icon/title and remains separated from the 300px shrinkable real Bank-name column. Long and true `640x960` contracts cover tab/body/icon/name/action boundaries; runtime interaction still opens Bank and returns to room configuration. Full verification and WeChat dry-run pass at `1,519,853 / 1,520,000`. No Bank data, room creation settings/request, route behavior, gameplay, cloud, protocol, assets, metadata, legacy client or AppID changed. Creator/WeChat visual review remains pending; not `dev_done`.
- 2026-07-18 H8.7 thirty-sixth source visual checkpoint: preparation-lobby Copy and Invite replace plain text-only controls with the shared compact icon action-button hierarchy. Both remain `112x80`, retain at least four pixels between icon/title, preserve the established room-code 8/14/8 column rhythm and keep their original handlers, busy locks and colors. Invite mounts the imported `joinRoom` slot; Copy uses a dedicated programmatic “码” fallback until a formal copy asset is approved. Mode catalog now reuses the existing page constructor with identical runtime root/scenery/safe-area names, reclaiming source without changing behavior. Long and true `640x960` geometry plus real clipboard/share and disabled-state tests pass. Full verification and WeChat dry-run pass with core source reduced to `1,519,828 / 1,520,000`. No room data/state, protocol, gameplay, cloud, assets, metadata, legacy client or AppID changed. Creator/WeChat visual review remains pending; not `dev_done`.
- 2026-07-18 H8.7 thirty-fifth source visual checkpoint: Home's player utility now presents the real Store-backed system name and a truthful “系统安全身份” secondary line inside the same `178x62` dark pill. Moving the pill six pixels right creates at least eight pixels between avatar/player and player/coin controls; the two text rectangles retain four pixels. The pill has no Button, so the existing formal avatar remains the single player-information target. No nickname editing, example nickname or level returns. One-time background-slot assignment and deferred callback declarations were tightened to keep the strict core source gate at `1,519,956 / 1,520,000`. Long and true `640x960` contracts pass, along with full verification and WeChat dry-run. No player data, route behavior, gameplay, cloud, protocol, assets, metadata, legacy client or AppID changed. Creator/WeChat visual review remains pending; not `dev_done`.
- 2026-07-18 H8.7 thirty-fourth source visual checkpoint: Home's coin utility replaces the bare trailing plus text with a `34x34` blue circular badge and 26px plus, while the formal coin icon, fixed shrinkable value column and badge retain visible 4/4/2-pixel internal gaps. The badge has no Button, so the existing `206x80` coin action remains the only route target and still opens the real Bank/coin-spend flow. The shared status-badge primitive only gains an optional font-size parameter; every existing badge keeps its 14px default. Long, true `640x960` and nine-digit coin states pass the same contract. Full verification and WeChat dry-run pass at `1,519,903 / 1,520,000`. No coin data, route behavior, gameplay, cloud, protocol, assets, metadata, legacy client or AppID changed. Creator/WeChat visual review remains pending; not `dev_done`.
- 2026-07-18 H8.7 thirty-third source visual checkpoint: Study's Previous and Random controls now use the shared icon action-button structure instead of embedding arrow/refresh glyphs in plain text. The unchanged `220x80` controls mount the existing `wordBank` and `practice` formal slots, keep at least eight pixels between icon and pure title, retain their original positions and handlers, and still fall back programmatically if art is unavailable. Long and true `640x960` contracts cover geometry; runtime clicks prove Previous and Random change the real Study card. Full verification and WeChat dry-run pass at `1,519,891 / 1,520,000`. No card bounds, visible-label semantics, Study state logic, gameplay, cloud, protocol, assets, metadata, legacy client or AppID changed. Creator/WeChat visual review remains pending; not `dev_done`.
- 2026-07-18 H8.7 thirty-second source visual checkpoint: the Study card now separates progress caption/status and status/bar by four pixels, then keeps six pixels through progress/word, word/meaning and meaning/actions. This fixes the previous four-pixel caption/status overlap and raises both unchanged `220x80` bottom actions to an 11-pixel card inset. One shared contract runs against long and true `640x960` Study trees; its first focused run exposed the old overlap before the final layout passed. Full verification and WeChat dry-run pass with core source unchanged at `1,519,849 / 1,520,000`. No copy, action/state behavior, card bounds, gameplay, cloud, protocol, assets, metadata, legacy client or AppID changed. Creator/WeChat visual review remains pending; not `dev_done`.
- 2026-07-18 H8.7 thirty-first source visual checkpoint: the shared accent-card decoration changes from a 10px horizontal rail crossing dense top-row titles to a 6px vertical rail inset eight pixels from the left edge and spanning card height minus 24. Mode and History representatives retain at least four pixels between the rail and their icon columns; mode title/summary rectangles now also retain four pixels. Join, Result, History detail, Feedback and Help inherit the same non-text-crossing primitive. Full verification and WeChat dry-run pass with core source reduced to `1,519,849 / 1,520,000`. No copy, card bounds, route/action/state, gameplay, cloud, protocol, assets, metadata, legacy client or AppID changed. Creator/WeChat visual review remains pending; not `dev_done`.
- 2026-07-18 H8.7 thirtieth source visual checkpoint: the lobby room-code card now uses a 216px code column followed by equal `112x80` Copy and Invite actions. Code-to-Copy, Copy-to-Invite and Invite-to-card-edge gaps are 8/14/8 pixels instead of the former asymmetric actions and four-pixel right edge. A shared long/minimum lobby-utility contract also freezes compact-tab/body spacing, Bank-copy inset and both status-indicator/copy columns. Full verification and WeChat dry-run pass with core source at `1,519,856 / 1,520,000`. No room code/state copy, copy/invite behavior, room state, gameplay, cloud, protocol, assets, metadata, legacy client or AppID changed. Creator/WeChat visual review remains pending; not `dev_done`.
- 2026-07-18 H8.7 twenty-ninth source visual checkpoint: each preparation-lobby player card now owns one 72px body avatar instead of repeating a second avatar in its title tab. The tab clears the avatar by 20 pixels and the ready/waiting badge by 24; the right status column keeps 16 pixels between badge and player name, while avatar-to-status/name columns retain 47/29 pixels. Long and `640x960` contracts cover both players and both mutually exclusive badges. Full verification and WeChat dry-run pass with core source reduced to `1,519,857 / 1,520,000`. No player copy/state, card bounds, room behavior, gameplay, cloud, protocol, assets, metadata, legacy client or AppID changed. Creator/WeChat visual review remains pending; not `dev_done`.
- 2026-07-18 H8.7 twenty-eighth source visual checkpoint: create configuration cards now keep eight pixels between selected-mode title and summary, at least four pixels from each large title tab to body icon/copy, nine pixels between the current-Bank icon and its 300-pixel shrinkable name column, and eleven pixels between that column and the change action. The same contracts run on long and `640x960` rebuilds. Full verification and WeChat dry-run pass with core source unchanged at `1,519,864 / 1,520,000`. No copy, card/action bounds, room/Bank behavior, gameplay, cloud, protocol, assets, metadata, legacy client or AppID changed. Creator/WeChat visual review remains pending; not `dev_done`.
- 2026-07-18 H8.7 twenty-seventh source visual checkpoint: the Home Bank strip now separates its fixed caption and real dynamic Bank name by seven design pixels, while the semantic icon, 326-pixel shrinkable name column and non-clickable change badge retain at least four pixels between columns on long and minimum viewports. The Bank-status body coin is reduced from 44 to 32 pixels and moved down 18 pixels, clearing both the compact title tab and status-copy column. Runtime contracts freeze both hierarchies. Full verification and WeChat dry-run pass at `1,519,864 / 1,520,000`. No copy, route/action/state, gameplay, cloud, protocol, assets, metadata, legacy client or AppID changed. Creator/WeChat visual review remains pending; not `dev_done`.
- 2026-07-18 H8.7 twenty-sixth source visual checkpoint: shared section cards below 120 design pixels now use a compact 24-pixel title tab, 14px title and 18px semantic icon instead of allowing the former 58/22/38 header to consume most of a short status card. Cards at or above 120 retain the established large hierarchy. Bank status plus lobby Bank/room-status body rectangles move below the compact tabs, and representative large/compact runtime contracts require at least four pixels between tab and body. Full verification and WeChat dry-run pass at `1,519,860 / 1,520,000`. No copy, route/action/state, gameplay, cloud, protocol, assets, metadata, legacy client or AppID changed. Creator/WeChat visual review remains pending; not `dev_done`.
- 2026-07-18 H8.7 twenty-fifth source visual checkpoint: shared page headers move the divider from `-4` to `-7` and the 15px subtitle from a `y=-25/h=30` box to `y=-29/h=24`. This creates explicit 2px title/divider and icon/subtitle gaps while retaining header height, copy, widths, icon, Back and catalog-help actions. The visible-UI contract now rejects header hierarchy intersections across long and minimum routes. Full verification and WeChat dry-run pass at `1,519,812 / 1,520,000`. No route/action/state, gameplay, cloud, protocol, assets, metadata, legacy client or AppID changed. Creator/WeChat visual review remains pending; not `dev_done`.
- 2026-07-18 H8.7 twenty-fourth source visual checkpoint: shared action buttons now use height-aware title/subtitle baselines and a tighter 40% title box, removing the prior 2-16 pixel text-rectangle intersections across 80/90/94/96/118/136/146-pixel controls. The visible-UI contract now rejects any action title/subtitle gap below 2 design pixels across both long and minimum-height routes. Full verification and WeChat dry-run pass at `1,519,812 / 1,520,000`. No copy, action, state, gameplay, cloud, protocol, assets, metadata, legacy client or AppID changed. Creator/WeChat visual review remains pending; not `dev_done`.
- 2026-07-18 H8.7 twenty-third source visual checkpoint: Home now switches to a compact layout when its SafeArea is below 1000 design pixels. At the true `640x960` viewport it retains every real top utility, Bank, create/join, study/Bank, catalog/history and footer action with 4-pixel minimum gaps and 80-pixel minimum targets; only the decorative character hides when its reserved space collapses. Long-screen dimensions remain byte-for-byte asserted at their former values. Full verification and WeChat dry-run pass at `1,519,772 / 1,520,000`. No behavior, gameplay, cloud, protocol, assets, metadata, legacy client or AppID changed. Creator/WeChat visual review remains pending; not `dev_done`.
- 2026-07-18 H8.7 twenty-second source visual checkpoint: Join, mode catalog, History, Feedback and Help now rebuild and pass full geometry at the true `640x960` minimum viewport. Eight catalog rows remain 80-pixel targets; four History rows interpolate from compact 86/94 geometry to the former long-screen 102/110 rhythm; Feedback and Help clear the shared header; Join copy retains visible gaps through its full icon-to-action chain. Full verification and WeChat dry-run pass at `1,519,483 / 1,520,000`. No gameplay, cloud, protocol, assets, metadata, legacy client or AppID changed. Creator/WeChat visual review remains pending; not `dev_done`.
- 2026-07-18 H8.7 twenty-first source visual checkpoint: Bank now remounts cleanly at `640x960` with an 80-pixel status card, four 96-pixel full-content rows, 80-pixel pagination and 80-pixel bottom actions. Runtime geometry covers all gaps, bounds and touch sizes while paging, selection, unlock and return behavior remain unchanged. Full verification and WeChat dry-run pass at `1,519,140 / 1,520,000`. No gameplay, cloud, protocol, assets, metadata, legacy client or AppID changed. Creator/WeChat visual review remains pending; not `dev_done`.
- 2026-07-18 H8.7 twentieth source visual checkpoint: Create and active Lobby are now rebuilt and geometrically verified at the real `640x960` minimum viewport. Create keeps five sections with 8-pixel gaps; Lobby keeps six sections with 7-pixel gaps and bounded 80/90-pixel actions. The out-of-bounds duplicate Leave Button and controller field are removed; shared header Back remains the locked single exit. Full verification and WeChat dry-run pass with core source reduced to `1,519,141 / 1,520,000`. No room behavior/protocol, gameplay, cloud, assets, metadata, legacy client or AppID changed. Creator/WeChat visual review remains pending; not `dev_done`.
- 2026-07-18 H8.7 nineteenth source visual checkpoint: Study now fits the minimum `640x960` logical viewport with an 80-pixel Bank strip and three non-overlapping 80-pixel action rows. The redundant bottom Bank shortcut that overlapped “下一个” is removed; the top strip remains the route. Word/meaning rectangles and responsive formulas were tightened, and runtime assertions cover every vertical gap plus the SafeArea bottom. Full verification and WeChat dry-run pass with core source reduced to `1,519,367 / 1,520,000`. No Study state, Bank route, gameplay, cloud, protocol, assets, metadata, legacy client or AppID changed. Creator/WeChat visual review remains pending; not `dev_done`.
- 2026-07-18 H8.7 eighteenth source visual checkpoint: mode-catalog copy now ends 8 pixels before its player badge, and the dense Feedback form has explicit 4-pixel minimum vertical gaps plus 8 pixels between contact input and status band. Runtime geometry assertions cover both structures while preserving mode availability, native EditBox ownership and feedback submission behavior. Full verification and WeChat dry-run pass with core source unchanged at `1,519,564 / 1,520,000`. No gameplay, cloud, protocol, assets, metadata, legacy client or AppID changed. Creator/WeChat visual review remains pending; not `dev_done`.
- 2026-07-18 H8.7 seventeenth source visual checkpoint: Home player/settings modals now reserve measurable vertical gaps between avatar/name/identity/close and status/sound/close while preserving both panel sizes and 80-pixel button targets. Runtime geometry assertions require 4-pixel player-content gaps and 8-pixel settings gaps. Full verification and WeChat dry-run pass with core source unchanged at `1,519,564 / 1,520,000`. No data, audio behavior, gameplay, cloud, protocol, assets, metadata, legacy client or AppID changed. Creator/WeChat visual review remains pending; not `dev_done`.
- 2026-07-18 H8.7 sixteenth source visual checkpoint: create-configuration Bank text and History record rows now have explicit non-overlapping text/score/action columns, with runtime geometry assertions requiring 8-pixel gaps. Runtime-only `HistoryRecordItem` fields no longer carry unused Inspector decorators. Full verification and WeChat dry-run pass with core source reduced to `1,519,564 / 1,520,000`. No history data, room behavior, gameplay, cloud, protocol, assets, metadata, legacy client or AppID changed. Creator/WeChat visual review remains pending; not `dev_done`.
- 2026-07-18 H8.7 fifteenth source visual checkpoint: Study's current-Bank row is now one full-width Button with a non-Button “更换” badge and a fixed 350-pixel title/subtitle column, removing the split adjacent target and preventing long Bank names from entering the badge area. The bottom shortcut remains. Runtime assertions freeze the structure, title boundary and Study return route. Full verification and WeChat dry-run pass at `1,519,652 / 1,520,000`. No gameplay, cloud, protocol, assets, metadata, legacy client or AppID changed. Creator/WeChat visual review remains pending; not `dev_done`.
- 2026-07-18 H8.7 fourteenth source visual checkpoint: Home's current-Bank strip is now one Button with a compact non-Button “更换” badge, removing nested press/disabled state while preserving the full-strip route and long-name width. Runtime assertions freeze the one-target structure. Unused Inspector field decorators were also removed from all other dynamically assigned pre-game controllers, reducing core source to `1,519,267 / 1,520,000`. Full verification passes. No gameplay, cloud, protocol, assets, metadata, legacy client or AppID changed. Creator/WeChat visual review remains pending; not `dev_done`.
- 2026-07-18 H8.7 thirteenth source visual checkpoint: the room status card now shows a mutually exclusive orange attention dot for pending commands/sync errors and green for true can-start availability; ordinary waiting shows neither. Runtime-only `RoomScene` fields dropped unused Inspector decorators to offset the new visible node. Runtime assertions cover normal, ready, pending and error transitions. Full verification passes at `1,519,981 / 1,520,000`. No gameplay, cloud, protocol, assets, metadata, legacy client or AppID changed. Creator/WeChat visual review remains pending; not `dev_done`.
- 2026-07-18 H8.7 twelfth source visual checkpoint: the mode-catalog header now exposes a visible `?` rules action with an 80-design-pixel target, reserved title/subtitle bounds and full `360x800` touch compliance. It opens the existing numbered Help page, now titled “玩法说明”, and Back returns to the catalog. The unused hidden Home handler was removed. Runtime/release assertions cover entry, return, title, separation and handler ownership. Full verification passes at `1,519,959 / 1,520,000`. No gameplay, cloud, protocol, assets, metadata, legacy client or AppID changed. Creator/WeChat visual review remains pending; not `dev_done`.
- 2026-07-18 H8.7 eleventh source visual checkpoint: Home now keeps “当前词库” only in the fixed caption while the dynamic title uses the full width for the real bank name. Accepted rooms without a snapshot keep player cards identity-only and show “正在进入房间...” solely in the central status card; empty loaded-room slots use the same concise “等待加入” body. Runtime assertions cover both copy hierarchies and long-title shrink width. Full verification passes at `1,519,676 / 1,520,000`. No gameplay, cloud, protocol, assets, metadata, legacy client or AppID changed. Creator/WeChat visual review remains pending; not `dev_done`.
- 2026-07-18 H8.7 tenth source visual checkpoint: selected Bank rows no longer prefix the textbook/unit title with a check character. The controller-driven selected ring and explicit “已选择” badge remain, leaving the full title width for long real bank names. Runtime assertions freeze the no-pseudo-icon title plus selected visual/badge contract. Focused and full verification pass at `1,519,742 / 1,520,000`. No gameplay, cloud, data protocol, assets, metadata, legacy client or AppID changed. Creator/WeChat visual review remains pending; not `dev_done`.
- 2026-07-18 H8.7 ninth source visual checkpoint: create-configuration auto-ready and preparation-lobby ready now use the shared icon action-button structure with formal practice icons and programmatic fallbacks. Text no longer embeds a check glyph; auto-ready selection still follows `roomAutoReady`, and local ready selection still follows the room snapshot. Runtime assertions cover icon slots, title transitions and selection states. Full `npm run verify` and `npm run build:wechat:dry-run` pass at `1,519,781 / 1,520,000`. No gameplay, cloud, protocol, assets, metadata, legacy client or AppID changed. Creator/WeChat visual review remains pending; not `dev_done`.
- 2026-07-18 H8.7 eighth source visual checkpoint: all Feedback guidance/result text now sits in a fixed rounded status band, while Help presents its six real topics as numbered title/body groups for clearer scanning. Runtime assertions cover the new band and both ends of the Help hierarchy without weakening existing validation, submission or navigation tests. Private button-visual fields were consolidated to retain source headroom. Full `npm run verify` and `npm run build:wechat:dry-run` pass at `1,519,769 / 1,520,000`. No gameplay, cloud, protocol, assets, metadata, legacy client or AppID changed. Creator/WeChat visual review remains pending; not `dev_done`.
- 2026-07-18 H8.7 seventh source visual checkpoint: Study reveal, wrong-word and Chinese-display controls now use shared icon action buttons with formal practice/word-bank assets and existing programmatic icon fallbacks. Their real reveal, wrong-word persistence and selected-state behavior is unchanged. Mode-catalog “双人” metadata now uses the shared compact badge. Runtime assertions cover icon slots, titles, selected states and the badge. Full `npm run verify` and `npm run build:wechat:dry-run` pass at `1,519,847 / 1,520,000`. No gameplay, cloud, protocol, assets, metadata, legacy client or AppID changed. Creator/WeChat visual review remains pending; not `dev_done`.
- 2026-07-18 H8.7 sixth source visual checkpoint: preparation player cards no longer repeat ready/waiting text in their body; the existing badges are now the single readiness source. The Home player modal uses a compact safe-identity badge, while Settings gains its formal icon and a sound-button selected state driven by the persisted mute value. Private button-visual names were consolidated to preserve the unchanged source gate. Full `npm run verify` and `npm run build:wechat:dry-run` pass at `1,519,796 / 1,520,000`. No gameplay, cloud, protocol, assets, metadata, legacy client or AppID changed. Creator/WeChat visual review remains pending; not `dev_done`.
- 2026-07-18 H8.7 fifth source visual checkpoint: Join now stays visibly disabled for empty/partial/invalid room codes, shows remaining-length guidance and enables only when the normalized code reaches six characters. Feedback Submit follows the existing 4-300 character rule in real time, disables again after successful clearing and remains retryable after an active failure. Shared button tinting and seven page-shell constructors were consolidated first, preserving runtime node names. Full `npm run verify` and `npm run build:wechat:dry-run` pass at `1,519,888 / 1,520,000`; do not raise the gate for further styling. No gameplay, cloud, protocol, assets, metadata, legacy client or AppID changed. Creator/WeChat visual review remains pending; not `dev_done`.
- 2026-07-18 H8.7 fourth source visual checkpoint: host/player preparation cards now share one constructor with stable node names. Tall EditBoxes display live length counters, covering room code `0/6` and feedback body `0/300`; successful feedback clearing also resets the counter. Local Cocos typings now accurately expose the runtime Node/EditBox APIs. Full `npm run verify` and `npm run build:wechat:dry-run` pass at `1,519,857 / 1,520,000`, so further source UI work must reclaim space first. No gameplay, cloud, protocol, assets, metadata, legacy client or AppID changed. Creator/WeChat visual review remains pending; not `dev_done`.
- 2026-07-18 H8.7 third source visual checkpoint: Bank unlock presentation now follows real unlockability/progress and displays a disabled “无需解锁” state where no purchase is needed. Study wrong-word presentation now remains selected and reads “已在错题库” after the current word is stored. The unassigned room player-summary path was removed so card badges remain the visible preparation source. Full `npm run verify` and WeChat dry-run pass; core source is `1,519,994 / 1,520,000`, so future source work must first reclaim space rather than increase this warning gate. No gameplay, cloud, protocol, asset, metadata, legacy-client or AppID changes. Creator/WeChat visual review remains pending; not `dev_done`.
- 2026-07-18 H8.7 second source visual checkpoint: shared controls now have controller-driven selected styling with disabled-state priority. Bank selection, Study Chinese visibility/progress, room auto-ready/local-ready/start availability, player ready/waiting badges and History filters all reflect real Store or room snapshot state. Join/Feedback EditBoxes gain native focus outlines. Full `npm run verify` and WeChat dry-run pass; the unchanged core warning gate passes at `1,519,708 / 1,520,000` bytes. No assets, importer metadata, gameplay, cloud, legacy client, protocol or AppID changed. Creator/WeChat visual comparison remains the next gate; this checkpoint is not `dev_done`.
- 2026-07-18 H8.7 source-verified visual-polish checkpoint: every non-Home header now uses its formal semantic icon with clearer title/subtitle separation; shared cards and EditBox surfaces gain a restrained inner border, section tabs gain shadow, and colored compact buttons gain readable outlines. `RuntimeButtonVisual` moves only icon/text content on press and softens loaded icons plus labels when disabled. History filter markers now follow the real selected mode. Focused tests and full `npm run verify` pass. No bitmap, Creator metadata, gameplay Bundle, cloud function, legacy client, protocol or AppID changed. This computer has no Creator; next action is a Creator 3.8.8/WeChat multi-page visual comparison and screenshot-driven adjustment. This is not `dev_done`.
- 2026-07-17 H8.6 final reference-aligned preparation UI: Home primary actions are shorter and taller, player/coin/subtitle/current-bank areas follow the approved reference, and “更换” is a real touch-safe button. Every non-Home page now has a visible blue Back button and icon-bearing title treatment. The mode catalog is a single eight-row list with separate “立即体验/筹备中” actions. Create configuration, preparation lobby, Bank, Study and History use framed, icon-labeled sections while preserving real Store/session data. The Home character scales into the available lower gap and hides on short screens instead of covering controls. Full `npm run verify`, Creator 3.8.8 build and package inspection passed: 147 files / 9,973,107 total bytes, 4,121,077 / 4,194,304 main bytes, and 3,472,932 `home_common` bytes. The normalized core-source warning gate is now 1.52 MB (`1,504,561` current) while the real 4 MiB WeChat gate is unchanged. No gameplay Bundle, cloud function, legacy client, protocol or AppID changed. Next action remains two-real-phone create/join/ready/start acceptance.
- 2026-07-16 high-fidelity button rendering correction: analysis of `C:\work\wxgame\picture\实际图.png` confirmed that phone scaling was secondary. The visible blocks came from 2x source insets being treated as design units, while programmatic fill/shadow/highlight remained visible beneath a bitmap that already contained those effects. Formal skins now render at 2x content size with 0.5 node scale, so 56 source pixels equal 28 design units; successful loading disables all duplicate fallback decoration. Runtime geometry tests account for node scale and prove every skin still exactly covers its hit target. Full verify, Creator 3.8.8 build, package inspection and WeChat Developer Tools CLI auto compile passed at 9,970,209 total / 4,121,077 main / 3,472,932 `home_common` bytes. The Windows desktop was locked during final screenshot capture, so one unlocked simulator/phone visual comparison remains recommended; no gameplay, cloud, protocol or interaction code changed.
- 2026-07-16 H4 high-fidelity completion: pulled source checkpoint `5b73bdf`, replaced all 18 `home_common` runtime images with the approved high-DPI sources, preserved existing UUIDs and let Cocos Creator 3.8.8 regenerate importer dimensions. Runtime/source SHA-256 values match exactly; `home-art:status` is `imported` and `home-art:verify-import` passes. The actual WeChat build passed at 147 files / 9,970,161 bytes, main package 4,121,077 / 4,194,304 bytes and `home_common` 3,472,932 bytes. After clearing Developer Tools caches, the generated project visibly loaded the upgraded background, Logo, rabbit, icons and button skins with intact layout and no application errors. No gameplay, cloud, protocol, AppID or page interaction changed. Next action is Phase 9 two-real-phone create/join/ready/start preparation-flow QA.
- 2026-07-15 H4 high-fidelity source checkpoint: all 18 optimized sources were regenerated from the five masters to remove the main clarity losses. The set now uses a 1080x1920 high-quality JPG background, 1280x400 Logo, 512x768 character, 768x328 skins and 320x320 icons; every transparent PNG is truecolor RGBA instead of indexed color. Total source payload is 3,453,135 bytes under a deliberate 4 MB `home_common` subpackage gate. `home-art:sync-upgrade` preserves existing Creator UUIDs, status distinguishes `upgrade-ready`/`reimport-required`, and verification rejects stale SpriteFrame dimensions. This no-Creator computer intentionally did not overwrite `assets/bundles/home_common` or edit importer metadata. Next action on the designated Creator 3.8.8 computer: pull, require `upgrade-ready`, run `npm run home-art:sync-upgrade`, open Creator for automatic reimport, require `verify-import` and `imported`, then run full verify/build/inspection and compare clarity at 360x800, 393x852 and 430x932. This checkpoint is not `dev_done`.
- 2026-07-15 Cocos art/build-path diagnosis: the generated package contains `home_common` as a 355,148-byte subpackage and the correctly imported `cocos-client/build/wechatgame/` project visibly loads Home background, Logo, character, icons and skins. The repository root project still targets legacy `miniprogram/`, while supporting pages intentionally have only shared icons and programmatic cards. Current V0 background/Logo/character/skin textures are also below modern high-DPI display size and use linear magnification. `result.md`, the build pipeline, README and asset manifest now record the evidence and next art-quality steps. No runtime, gameplay, cloud, protocol or AppID code changed.
- 2026-07-15 H8.5 supporting-page visual refinement: Bank, Study, mode catalog, create/join/lobby, Result, History, Feedback and Help now share a stronger translucent page header, transparent Back target, accent-card hierarchy and long-screen spacing. Unsupported Bank filter affordances were removed, History has a complete real-data empty state, and Feedback/Join use explicit Cocos field guidance with blank native placeholders plus top-left input anchors to prevent WeChat ghost text. Focused tests, full verification, Creator 3.8.8 WeChat build, package inspection and Developer Tools traversal passed. No gameplay Bundle, cloud function, legacy client, room/cloud protocol or AppID changed. Next action remains two-real-phone create/join/ready/start acceptance.
- 2026-07-15 Home hierarchy/clarity refinement: restored the final approved Home layout with full-width Create and Join actions followed by Study/Bank and Catalog/History rows. Avatar, coin and settings no longer have white background cards; colored action titles use responsive sizing and restrained outlines, and the avatar is larger without violating safe-area/icon containment. Current runtime assets plus larger source masters were exported to `C:\work\游戏图片\游戏主界面图片` with replacement guidance. Full `npm run verify`, the actual Creator 3.8.8 WeChat build and package inspection passed (`6,850,587` total, `4,121,077 / 4,194,304` main, `355,148` `home_common`). After clearing stale Developer Tools compile/file cache, the H4-360x800 simulator displayed the correct hierarchy with complete top and bottom controls and no application error. No gameplay Bundle, cloud function, legacy client or protocol changed.
- 2026-07-14 final icon/capsule repair: a real screenshot proved the previous custom-size assignment could still be overwritten after asynchronous art loading, leaving raw-width skins and detached icons. `RuntimeButtonVisual` now repairs late skin resets, the programmatic full-size base remains visible under formal art, shared action/top icons are capped at 56/48 design pixels, and the common top safe inset is 104 so Home Settings and every page Back control clear the WeChat capsule. Cross-route runtime QA checks Home, Bank, Study, mode selection, Room, Result, History, Feedback and Help for icon containment and exact skin/hit-area coverage. Full verify, Creator build, package inspection and a fresh 360x800 Developer Tools Home check passed with 0 application errors. No gameplay Bundle, cloud function, legacy client or protocol changed.
- 2026-07-14 post-H4 icon-layout maintenance: formal sliced action skins no longer collapse to their source-image width after SpriteFrame assignment; they are fixed to custom size and restored to the full clickable bounds. Action icons are capped at 64 design pixels with a tested title gap, top icon controls use a smaller slot, and crowded card/character art was reduced and repositioned. `npm run verify`, the actual Creator WeChat build and generated-package inspection passed; the 360x800 Home view showed full-width button skins, contained icons and 0 application errors. No gameplay Bundle, cloud function, legacy client or protocol changed.
- 2026-07-14 H4 completion: exact 360x800 and 430x932 Home checks passed in WeChat Developer Tools after the imported H4.1 package. Formal background cover, contained foreground art, live Labels, button skins, bottom actions and safe-area spacing remained visible without incoherent overlap; application errors were 0. H4 is now `COMPLETE`. The next single action is two-real-phone create/join/ready/start preparation-flow QA; gameplay implementation remains frozen.
- 2026-07-14 H4.1 Creator import/build: the designated Creator 3.8.8 owner imported all 18 approved files into `home_common`; `home-art:verify-import` confirms 23 generated metadata files, unique UUIDs and SpriteFrame sub-resources, and status is `imported`. The release budget audit now preserves the 1.5 MB core gate while checking formal Home art separately against 350 KB. Full verify passed in 48.1s, the actual WeChat build passed in 60.6s, and inspection reports 6,849,298 total / 4,121,077 main / 355,148 `home_common` subpackage bytes. Developer Tools iPhone 12/13 traversal covered Home, mode catalog, create room, Bank, Study and History with formal art visible and application errors 0. H4 remains `IN_PROGRESS` only for 360x800 and 430x932 visual evidence.
- 2026-07-14 tracking-document sync after resource archive `54b3964`: H4 is still `IN_PROGRESS`, and `npm run home-art:status` reports `source-ready`. All generated masters, optimized runtime art, the composition preview and nine original Goal references are on GitHub. Historical H8 Creator/WeChat evidence remains valid only for that baseline; this `D:\demo\wexin` machine has no Creator or WeChat tools and has not imported `home_common`. The only next action is H4.1 on the designated Creator 3.8.8 computer using the repository prepare/status/verifier commands.
- 2026-07-14 H4 complete resource archive: all nine Goal reference images are now tracked under `docs/design/home/references/` with ASCII names, byte counts and SHA-256 values matching the original `image-1.png` through `image-9.png` attachments. They are design-only evidence and remain outside every Cocos Bundle. Together with the five generated masters, 18 optimized runtime assets and the H4 composition preview already tracked, this removes the final unpushed local image-resource gap; Creator first import remains the only H4 execution blocker.
- 2026-07-14 H4 cross-computer import automation: `npm run home-art:prepare` performs an exact 18-file, hash-protected copy into the transient `assets/bundles/home_common/textures` import worktree and refuses unexpected files, changed files or an already imported directory. `npm run home-art:status` reports `source-ready`, `prepared`, `imported` or an actionable invalid state. After Creator 3.8.8 generates metadata, `npm run home-art:verify-import` requires byte-identical approved images, Bundle name `home_common`, 23 Bundle/directory/image metadata files, unique top-level UUIDs and a generated SpriteFrame sub-resource for every image. `npm run test:home-art-import` exercises preparation, idempotence, status transitions, tamper rejection and the metadata gate without Creator. The current computer still does not create or commit importer metadata; the designated Creator owner runs these commands and commits the imported tree plus generated metadata atomically.
- 2026-07-14 H4 art-generation/code-binding checkpoint: five new source masters and 18 optimized import files are under `cocos-client/art-source/home-v1/`; the optimized payload is `335,229` bytes. A dedicated full-body reading rabbit replaces the atlas bust, connected chroma spill is removed, and the checked-in `640x1387` composition preview verifies current Cocos coordinates. `HomeArtManager` deduplicates/caches/retries `home_common` loads, every existing visual slot binds by semantic key, sliced orange/blue/green/purple button skins preserve live Labels/actions, formal background success disables the programmatic scenery overlay, and build tooling requires `home_common` as a WeChat subpackage. This computer has no Creator, so no file was placed under `assets/` and no importer `.meta` was hand-written. Next action: one designated Creator 3.8.8 computer creates/imports `home_common`, commits images plus all generated metadata atomically, runs full verification/actual build/inspection, and captures Creator visual evidence. This is an in-progress checkpoint, not H4 `dev_done`.
- 2026-07-14 H8.3 mode-neutral preparation naming: the active pre-game chain is now `openModeSetup -> createConfiguredRoom -> startSelectedMode -> startPreparedMode`. Obsolete trial/single-game controller names are rejected by static release tests. Cocos package/build-task metadata now uses `word-battle-park-*`. `RoomService.startGame`, cloud handler names, protocols and gameplay `Fishing*` code remain unchanged as explicit compatibility boundaries. Root and Cocos README introductions now describe Word Battle Park as a multi-mode vocabulary game platform instead of a fishing prototype. Next action remains two-real-phone preparation acceptance.
- 2026-07-14 H8.2 quiet room sync: routine `RoomStore.syncing` changes no longer replace the visible preparation condition with a repeating sync message. Initial room entry and real errors remain visible, retries are described as background work, and two ready human players enable the primary-colored start action with explicit ready copy. Runtime tests freeze both the quiet-polling state and enabled visual transition. No polling service, cloud/room protocol, gameplay Bundle, legacy client or AppID changed. Next action remains two-real-phone preparation acceptance.
- 2026-07-14 H8.1 pre-game dead-code/two-human audit: removed unused Home, Room and Study handlers plus the active Cocos preparation `addBot` chain from pending actions, `RoomSessionService`, `RoomService` and room action availability. Every visible room start now requires two real human players, including PK snapshots that happen to contain a legacy bot. Passive `RoomTypes` bot/duration fields, raw cloud contract types and gameplay compatibility remain intentionally frozen because they support old snapshots and game internals but are no longer reachable from preparation UI/services. Full local verification and dry-run passed; no gameplay Bundle, cloud function, legacy client, room/scoring protocol or AppID changed. Next action is two-real-phone create/join/ready/start QA, including proving one-player start is unavailable and no robot entry exists.
- 2026-07-14 H7 pre-game option cleanup: pre-game state no longer stores a selected duration, room creation settings no longer accept or send duration/bot difficulty, and the preparation lobby no longer displays seconds or robot identity. Help/feedback pre-game copy was reduced to necessary context. Existing gameplay timers, robot match compatibility, cloud functions, gameplay Bundles and the legacy mini-program remain intentionally untouched after the user narrowed the request to preparation-only behavior. `npm run verify` passed in 50.2s, dependency audit found 0 vulnerabilities, Creator 3.8.8 built successfully, package inspection passed at 6,487,834 total / 4,120,918 main bytes, and Developer Tools CLI `auto` passed. Next action remains two-real-phone create/join/ready/start QA.
- 2026-07-13 H6 pre-game logic simplification: the visible seven-page layout is unchanged and no art was added. Mode selection no longer carries hidden shared/spell/bank handlers or duplicate bank status; Home uses `openModeCatalog`; Room no longer owns invisible bot-difficulty/manual-refresh controls. Create and join now construct mutually exclusive entry trees, and accepted sessions destroy those trees before showing the lobby. Lower-level room/gameplay compatibility, automatic polling, copy/invite, ready/start and cloud contracts are untouched. `npm run verify` passed in 47.4s, Creator 3.8.8 built in 48.5s, the inspector passed at 6,488,045 total / 4,120,918 main bytes (2,713 bytes below H5), and Developer Tools CLI `auto` passed. Next action is two-real-phone create/join/ready/start QA; H4 art remains deferred.
- 2026-07-13 documentation and QA revalidation checkpoint before H6: `AGENTS.md` temporarily pointed to H4 as `DESIGN_READY` and removed the old fixed startup path; `COCOS_WORKSPACE.md`, the completion matrix, release QA and pre-game design were aligned with the H5 build evidence. `npm run verify` passed in 42.7s, Creator 3.8.8 rebuilt WeChat in 49.7s, the package inspector reported 6,490,758 total / 4,120,918 main bytes, and Developer Tools CLI `auto` passed. The newer H6 entry above supersedes this checkpoint.
- 2026-07-13 H5 seven-page pre-game completion: Home create now enters an eight-item mode catalog, then the enabled preparation mode opens real-bank creation settings and an optional existing ready action before the active lobby. Join uses the production six-character alphanumeric contract; lobby refresh stays in background polling; Bank, History and Study use real stores without fabricated identity, coins or records. Full `npm run verify`, Cocos Creator 3.8.8 `build:wechat`, generated-package inspection and WeChat Developer Tools traversal passed. The generated package is 6,490,758 bytes total and 4,120,918 bytes in the main package, leaving only 73,386 bytes. No cloud function, gameplay Bundle, room/scoring/synchronization contract, AppID or cloud environment changed. Real two-phone room acceptance remains release QA; H4 bitmap work remains blocked until approved standalone assets are supplied.
- 2026-07-13 repository-entry redesign: root `AGENTS.md` is now the single Codex startup entry for new computers and tasks. It performs Git safety checks, routes Codex to the current progress/design/asset files, defines document update ownership, preserves H4 scope boundaries, and requires verification plus push/remote-sync evidence. Users can now request only “read AGENTS.md and continue”; detailed progress remains in its authoritative progress file rather than being duplicated in the entry document. The redesign passed the complete `npm run verify` chain and `npm run build:wechat:dry-run`.
- 2026-07-13 H4 formal-art handoff: `COCOS_FINAL_ART_INTEGRATION_DESIGN.md` now defines the approved path for adding theme backgrounds plus a shared `home_common` Bundle, resource catalog/manager, slot binding, fallback/retry/lifecycle behavior, optional nine-slice skins, package budgets and Creator acceptance. H4 implementation is not started and no empty Bundle was created. Start with H4.0 only after approved image files exist; one computer must own the first Creator 3.8.8 import and push each image together with its generated `.meta` before another computer continues.

- 2026-07-10 Cocos Phase 8 completion: gameplay presentation/controllers are now real load-on-demand Asset Bundles. `mode_pk` contains PK/shared builders and controllers; `mode_spell` contains spell builder/controller/keyboard. `GameplayBundleManager` gates route mounting, deduplicates loads, verifies registration, retries failures, and keeps the old screen under the loading overlay. Both mode bundle metas request WeChat `subpackage` compression, the generated-package inspector requires them as declared subpackages, and runtime tests delay `mode_pk` plus reject/retry `mode_spell`. Phase 8 source development is complete; Phase 9 Creator/WeChat/device evidence remains external.
- 2026-07-10 Cocos cloud deployment diagnostics: `CloudService` now recognizes missing functions, invalid environments, permission failures, network failures, and timeouts, emits only bounded public messages, and wraps cloud initialization failures as `CloudCallError`. Boot shows that sanitized message instead of a generic network-only hint. Platform tests inject `-501000`, invalid-env, permission and timeout signatures; actual Cocos-era function deployment and `rooms` collection state still require DevTools/cloud confirmation.
- 2026-07-10 Cocos word-bank transaction hardening: `StorageService.writeWordBankProgress()` writes the unlock list before the deduction and compensates both legacy keys if either synchronous write fails; `BankScene` restores its pre-unlock `WordBankStore` snapshot so coins cannot remain deducted without the matching unlock. Stage 3 injects a second-key coin failure; runtime execution proves controller rollback and the explicit no-deduction message. Phase 3 documentation was updated to reflect that Home/Bank/Study runtime UI is implemented and only Creator visual evidence remains.
- 2026-07-10 Cocos feedback lifecycle hardening: `FeedbackScene` now keys each submission to its live controller instance, locks duplicate submission, catches privacy-contract failures, and makes delayed success/failure inert after route destruction. Runtime execution covers local validation, active success/failure, and a deferred success after returning Home; the stale response cannot clear the destroyed form or affect the new route.
- 2026-07-10 Cocos abandoned-response isolation: `RoomStore` now exposes a monotonic session version incremented by every enter/leave. Fishing catch/power-up/bot/finish and spell submit/skip/finish capture room ID plus version and ignore responses after leave or replacement; canceled failures are silent in gameplay scenes. Phase 5 proves a delayed catch cannot restore an abandoned room, Phase 7 proves a delayed submit cannot refresh/restore it, and switching from spell to PK clears the stale spell result. Finished rooms also remain stopped across hide/show.
- 2026-07-10 Cocos interaction/settlement hardening: runtime execution now clicks duration, co-op mode, Study reveal/hide/wrong-word/bank-return, bank paging, invalid room join, copy/invite, all gameplay leaves, Result-to-History, history detail/back, invalid feedback and Help back controls. The audit found finished rooms kept polling; `App` now stops polling on the first finished snapshot, and Result-to-History releases the completed room after history persistence. Tests prove no timer survives Result and the saved record remains available.
- 2026-07-10 Cocos runtime-boundary hardening: every generated Label now uses Cocos `Label.Overflow.SHRINK`, including Boot privacy and route-loading copy; serialized Home labels use the matching overflow value. The runtime shell walks every visible route/overlay transform and fails if any node leaves the `960x640` design area or any Label can resize its node. Room-code input, join, lifecycle invitations, sharing and clipboard now share one six-character domain rule; overlong external codes are rejected instead of silently joining a truncated code.
- 2026-07-10 Cocos source-asset audit: `test:release` now parses all 103 committed `.meta` files and rejects missing/duplicate UUIDs, missing directory metadata, unapproved file-meta gaps, or out-of-range Boot/Home object references. Creator still needs to generate and retain exactly four importer metas for the two theme JSON files and two JPEGs; do not hand-author those version-specific image sub-metas without the editor.
- 2026-07-10 Cocos serialized-layout hardening: source audit found `Home.scene` had retained editor viewport offsets (`y=-20`, width `0`, left/right `480`) even though runtime mocks started from a clean node and therefore missed the defect. Home now shares Boot's centered `960x640` Canvas and zero-edge Widget contract. `test:shell` parses both serialized scenes and fails on any position, size, alignment-flag, or edge-offset regression; Creator visual proof is still required.
- 2026-07-10 Cocos spell-data completion: the old 1.8 MB `spellWordBankData.js` is represented by a lossless 35 KB `SpellTemplateData.generated.ts` index. `SpellTemplateCatalog` reconstructs all fields without randomness; `test:spell-data` hashes the source and compares 44 banks/6,351 templates exactly. `RoomScene` now includes the selected template pool in `roomSpellQuestions`, `RoomRules` caps it at 240 like production, and non-spell modes carry none. Future/wrong banks use the same stable hash algorithm as the cloud fallback.
- 2026-07-10 Cocos generated-package hardening: the build config now carries the official `taskName=wechatgame`; `inspect-wechat-build.js` requires generated configs for `theme_default` and `theme_island`, reports each Bundle's main/subpackage location and bytes, rejects undeclared or empty subpackages, and enforces 4 MiB main plus 30 MiB aggregate subpackage limits. Synthetic valid/missing/undeclared/empty/oversize fixtures pass. A real Creator export is still required before these artifact claims can move from tooling evidence to release evidence.
- 2026-07-10 Cocos preload/busy-state hardening: `HomePlaceholder` now preloads the destination route's semantic asset before mounting it, retains the old screen under `BlockInputEvents`, rejects stale completions, and lets `ThemeManager` fall back on individual asset failure as well as bundle failure. `RuntimeButtonVisual` applies theme pressed/disabled colors without per-frame redraw, and Room locks create/join/ready/bot/start/copy/invite/refresh/back from one pending state. Phase 8, static/runtime shell, release, structure, and both type checks cover this path; Creator bundle timing and visual QA remain external.
- 2026-07-10 Cocos room robot parity: the runtime Room screen now exposes low/medium/high PK robot controls, derives selected state from room snapshots, keeps all robot controls unavailable in co-op, and removes duplicate human names from player rows. Phase 4/5, static/runtime shell, release, and both type checks cover this path; real Creator layout and robot matches remain external gates.
- 2026-07-10 Cocos privacy presentation hardening: Boot's generated privacy nodes now use `UI_2D`, explain the processed data, and include `暂不进入`; decline clears consent, makes zero cloud init calls, and stays on Boot. Acceptance after decline initializes once and enters Home. Home now displays `系统玩家：玩家` and exposes the native privacy contract. Platform, static release, and actual runtime-shell click tests cover the flow; Creator visual verification remains pending.
- 2026-07-10 Cocos performance evidence path: `PerformanceService` records full-session/per-route frame aggregates, recent-frame P95, slow frames, and node peaks with a fixed memory window. `HomePlaceholder` samples every frame and counts nodes every 60 frames; DEV Home can copy a privacy-safe JSON report while release builds omit the control. `npm run test:performance` and the runtime shell test cover collection/export. Real device reports remain required; follow `COCOS_RUNTIME_PERFORMANCE.md`.
- 2026-07-10 Cocos theme/performance hardening: semantic Home/gameplay/spell backgrounds now follow the active route, `ThemeManager` deduplicates sprite-frame requests, PK/shared targets consume insect/fish theme geometry, and hit/miss/correction effects reuse a fixed three-label pool. `test:phase8`, static shell, runtime shell, and both type checks cover the path. `COCOS_MIGRATION_COMPLETION_MATRIX.md` now records exactly which design criteria still need Creator/WeChat/two-device evidence.
- 2026-07-10 Cocos WeChat build pipeline: `tools/wechat-build-config.json` now fixes the Creator 3.8 WeChat build contract, `npm run build:wechat` builds only to `cocos-client/build/wechatgame/`, and the post-build inspector validates AppID/orientation/entry files, rejects source maps and forbidden legacy/backend paths, enforces the 4 MiB main-package gate, and records main/subpackage bytes. `npm run test:build-pipeline` passes with synthetic valid/failure fixtures; a real Creator export remains pending. See `COCOS_WECHAT_BUILD_PIPELINE.md`.
- 2026-07-10 Cocos accepted-join recovery: `RoomSessionService.join` now distinguishes a failed `joinRoom` call from a successful join whose first room-document read fails. The latter keeps the new room code/openid, routes to Room, displays an automatic-retry state, and lets polling recover without joining twice. Phase 4, lifecycle, shell runtime, and type checks cover it. Real weak-network recovery remains a device gate.
- 2026-07-10 Cocos room replacement hardening: `RoomScene` no longer calls `leave()` before create/join validation and cloud success. Invalid codes plus failed create/join calls now preserve the current room, local openid, and polling; only explicit Back leaves. `test:phase4` and the static shell test lock this behavior. Run `npm run test:phase4` or `npm run verify`.
- 2026-07-10 Cocos cold-invite scene fix: a successful launch invitation previously set route `room` but skipped loading `Home.scene`, which could leave the user on Boot. `SceneRouter.enterRuntimeShell` now always loads the persistent shell while preserving the target route, and serialized `HomeScene.onLoad` only defaults when route is still `boot`. Static and mock runtime tests verify Boot -> Home scene with route `room` intact. Run `npm run test:shell-runtime` or `npm run verify`.
- 2026-07-10 Cocos remote-settlement hardening: the mock Cocos runtime now instantiates a real `App`, injects remotely finished PK and spell snapshots, and proves result/history state is created before Result routing with zero duplicate cloud-function calls. This covers the opponent-finishes-first path that local timeout tests did not isolate. Run `npm run test:shell-runtime` or `npm run verify`; real two-device timing remains pending.
- 2026-07-10 Cocos contract/history hardening: `npm run test:contracts` now checks all 11 production cloud-function sources against frozen request/response markers and Cocos service coverage. `HistoryScene` now pages long spell details three rounds at a time; the runtime shell test opens a seven-round record and verifies page navigation. Run `npm run test:contracts`, `npm run test:shell-runtime`, or `npm run verify`. Real cloud calls and narrow-phone rendering remain external gates.
- 2026-07-10 Cocos lifecycle hardening: `RuntimePort`/`WechatRuntimePort` now expose launch options and show/hide hooks; `LifecycleService` defers invitation joins until privacy-approved boot, pauses room polling in the background, resumes/refreshes it on show, and blocks room switching during gameplay. `App.boot()` no longer calls `getOpenId`; create/join responses remain the identity source. Run `npm run test:lifecycle` or `npm run verify`. Real WeChat share return and background recovery still require DevTools/two-device verification.
- 2026-07-10 Cocos migration runtime assembly: `Home.scene` is now a persistent runtime UI shell. `RuntimeScreenFactory` creates functional Home/Bank/Study/Co-op Select/Room/PK/Shared/Spell/Result/History/Feedback/Help controls and mounts the existing controllers; `SceneRouter` stays in Home after boot. Study now also supports random word, add-to-wrong-bank persistence, and Home return. Run `npm run test:shell`, `npm run test:shell-runtime`, or `npm run verify`; the runtime test instantiates the real shell against a test-only Cocos lifecycle mock and mounts/destroys every route. Manual multi-scene creation is no longer required for the first preview; remaining gates are Creator import/visual QA, real WeChat build, two-device/low-end-device tests, screenshots, and upload.
- 2026-07-10 Cocos migration: Phase 8 theme core and Phase 9 static readiness are implemented. `ThemeManager` loads compact `theme_default`/`theme_island` bundles with latest-selection race protection and default fallback; generic `ThemeBinding` keeps gameplay code theme-agnostic. Feedback/Help controllers, live room-name anonymization, legacy custom-name cleanup, visible-placeholder removal, and release static QA are complete. Run `npm run test:phase8`, `npm run test:release`, or `npm run verify`. The remaining gates are Creator visual verification, WeChat build/package size, two-device/low-end-device QA, screenshots, and development upload, documented in `COCOS_RELEASE_QA.md`.
- 2026-07-10 Cocos migration: Phase 7 同舟拼词记 core is implemented. `CoopSpellStore`/`CoopSpellService` isolate drafts by question ID, handle optimistic submit/wait, exact server timeout retries, local question/total anchors, skip, settlement, wrong words, and serialized history. `CoopSpellScene`, `SpellLetterKey`, `HistoryScene`, `HistoryRecordItem`, and spell-aware `ResultScene` controllers are ready for Creator binding. Run `npm run test:phase7` or `npm run verify`. Creator scene binding, narrow-phone layout, and two-device acceptance remain pending.
- 2026-07-10 Cocos migration: Phase 6 默契捕词赛 core is implemented. `FishingMatchService` now supports shared co-op team scoring without bots or power-ups, `CoopSharedScene` exposes team/contribution/timer/target bindings, and result/history persistence uses `coopShared` with the fixed label `默契捕词赛`. Run `npm run test:phase6` or `npm run verify`. Creator scene binding and two-real-device acceptance remain pending.
- 2026-07-10 Cocos migration: Phase 5 双人PK core is implemented under `cocos-client/`. It includes synchronous optimistic tap feedback, authoritative `catchFish` correction, moving target components, robot timing, power-ups/stun, timeout settlement, confirmed wrong words, result records, per-mode history limits, and PK/result scene controllers. Run `npm run test:phase5` or `npm run verify`. Creator scene binding and real-device latency/two-player acceptance remain pending.
- 2026-07-10 Cocos migration: Phase 4 room-flow core now lives under `cocos-client/`. It includes read-only `rooms` document access, normalized `RoomStore`, non-overlapping polling, create/join/ready/bot/start/share session coordination, and `RoomScene`/`CoopSelectScene` controllers. Run `npm run verify` in `cocos-client`. Final Creator scene bindings and two-device acceptance remain pending because this machine has no Cocos Creator or WeChat Developer Tools.
- Current latest build includes: word-bank unlocks with `金币`, `背单词`, background music, PK mode, test robot, power-ups, and two co-op modes.
- Latest preview QR is at `C:\work\wxgame_cocos\.codex-preview\preview.png`.
- Latest uploaded package size is about `2.1 MB` (`2253921` bytes).
- Latest cloud deploy target is `cloud1-d3gre86i51a49821a`.
- Last deployed cloud functions: `createRoom`, `joinRoom`, `submitFeedback`, `checkText`, plus existing game functions.
- 2026-06-30 UGC nickname violation整改: platform warning cited a political-sensitive custom nickname (`刁进平`). The game now disables public custom nickname UGC entirely. Frontend no longer reads/saves `playerName` from storage, clears the old local `playerName` key on startup, shows the home nickname area as system `玩家` with a disabled `系统` button instead of `修改`, and sanitizes room snapshots/history/spell details to `玩家1`/`玩家2` (bots keep fixed bot names only). `createRoom` and `joinRoom` no longer trust client nicknames; they write system names (`玩家1`, `玩家2`) regardless of submitted nickname. `createRoom` and `joinRoom` were deployed through the WeChat DevTools right-click cloud function menu because CLI deploy still returned `getCloudAPISignedHeader 41002 system error`. Uploaded development version is `2026.06.30.1` with description `remove custom nickname UGC and use system player names`; preview/upload package size is `2254041` bytes. Appeal screenshot saved at `.codex-preview/ugc-nickname-fix-screenshot.png`. Before申诉, also clean cloud database `rooms` records containing old违规 nicknames, if any.
- 2026-06-29 content safety/audit fix: review rejected the previous build because user-generated text did not clearly filter illegal/political/porn/gambling content. Added WeChat content safety checks through `security.msgSecCheck` for player nickname editing (`checkText`), room creation/join nickname writes (`createRoom`, `joinRoom`), and player feedback content/contact/nickname (`submitFeedback`). These cloud functions now include `config.json` `permissions.openapi=["security.msgSecCheck"]`. `checkText`, `createRoom`, `joinRoom`, and `submitFeedback` were deployed to `cloud1-d3gre86i51a49821a`; CLI deploy hit `getCloudAPISignedHeader 41002 system error`, so deployment was completed through the WeChat DevTools right-click cloud function menu. Uploaded development version is `2026.06.29.1` with description `add msgSecCheck content safety for nickname rooms and feedback`.
- 2026-06-28 player feedback pass: home now has a `反馈` button next to `加入房间`. Players can submit problem/suggestion text and optional contact info. The frontend calls the new `submitFeedback` cloud function, which validates the content and writes records into the cloud database `feedbacks` collection with player name, openid, contact, scene/room/bank/game context, client version, platform, and `status: "new"`. `submitFeedback` was deployed to `cloud1-d3gre86i51a49821a`. WeChat DevTools preview and upload passed; latest uploaded development version is `2026.06.28.3` with description `resubmit build with player feedback collection`.
- 2026-06-28 launch hardening pass: client diagnostic tools are now disabled for release (`DEBUG_TOOLS_ENABLED=false`, `DEBUG_LOGS_ENABLED=false`), so the visible home/room `logs` buttons are hidden and the stale debug handler is gated. `project.config.json` now disables source-map upload for release previews. Validation passed for JS syntax, JSON parsing, static button-handler coverage, direct cloud-function references, all 44 prebuilt spell banks/6351 spell templates, coop spell submit/wait/history behavior, and question-change input reset behavior. A WeChat DevTools preview was generated successfully at `.codex-preview/preview.png`.
- 2026-06-28 coop spell UI focus pass: the two-player spell screen now emphasizes the local player's responsibility area. The local segment is shown as a large full-width card above the keyboard with two prominent input boxes and progress, while the teammate area is reduced to a compact status chip showing only range/submission state. Boat height now adapts to the remaining vertical space, and the boat hint is hidden on short screens to avoid overlap. JS syntax checks and WeChat DevTools preview passed.
- 2026-06-28 coop spell timer/layout tuning: coop spell total/question countdowns now use local room/question anchors after the room snapshot arrives, avoiding visible countdown differences caused by two phones having different system clocks. The local input card label is shortened to `请填` / `已填`, the boat area is moved slightly lower with adaptive height, and PK tap ripple feedback is disabled by `TAP_RIPPLE_ENABLED=false` to reduce extra per-frame drawing on lower-end devices. Clock-skew simulation, JS syntax checks, JSON checks, layout coordinate checks, and WeChat DevTools preview passed.
- 2026-06-28 coin economy pricing pass: new players now start with `50` coins. Default unit-bank unlocks remain `10` coins; high-school compulsory/elective review banks (`jilin-g3r-b*-review`, `jilin-g3r-x*-review`) cost `30` coins each; the all-review bank (`jilin-g3r-all-review`) costs `150` coins. Recharge constants are recorded as `1 RMB = 10 coins`, but real recharge still needs WeChat Mini Game virtual payment/Midas server-side integration before exposing a production purchase button.
- 2026-06-21 latest co-op update: both `coopShared` and `coopSpell` now create rooms and require two real players to join and ready before starting. Neither co-op mode exposes or accepts robots; this is enforced in both the frontend and the deployed `addBot`/`startGame` cloud functions. `coopShared` is named `默契捕词赛`; `coopSpell` is named `同舟拼词记`. Spell rooms use the home-screen duration selection (30/60/90/120 seconds), keep 15 seconds per word, and split four blanks between player 1 (blue) and player 2 (pink). Level targets remain removed. The home screen shows best scores for `双人PK`, `默契捕词赛`, and `同舟拼词记`; each mode retains its latest 50 local records and a separate all-time best.
- 2026-06-21 naming/history audit: active home, help, room, gameplay, result, history, and busy/error copy now use the latest mode names. On startup, old local score records are migrated from legacy labels/IDs (`双人闯关`, `合作闯关`, `小船拼词`, `合作拼词`, etc.) to `coopShared`/`coopSpell`, then rewritten with `默契捕词赛`/`同舟拼词记` labels and result text without changing scores. The updated `addBot` rejection message was deployed successfully.
- 2026-06-21 PK input latency pass: tapping a word now shows an immediate Canvas ripple, routine score feedback no longer invokes native `showToast`, and `catchFish` returns the updated players/fishes/target snapshot so the frontend can apply it without a second room-document read. The updated `catchFish` cloud function was deployed successfully. The existing cached grass background, cockroach sprites, prompt, timer, and score dock remain in use.
- 2026-06-22 co-op spell start fix: the active `startGame` cloud function has a 3-second timeout and previously parsed the full ~850 KB word-bank module during cold start. Rooms now carry a sanitized current-bank `roomWords` pool, and the manual start request also sends the pool so already-waiting rooms are covered. `startGame` and `catchFish` load the full bank module only as a legacy fallback. Local integration verified two ready humans enter `playing`, receive four blanks split across both openids, and do not load the full bank module. Updated `createRoom`, `startGame`, and `catchFish` were deployed successfully.
- 2026-06-22 environment/logging fix: local DevTools logs showed session/AppID environment noise while `miniprogram/config.js` still left `envId` empty. The client is now pinned to the deployed environment `cloud1-d3gre86i51a49821a`, preventing real-device previews from calling stale functions in another selected environment. Room start failures now remain visible in the room status bar, and `startGame` emits structured request/start diagnostics (mode, player count, ready count, word-pool count) without logging openids. The instrumented `startGame` was deployed successfully.
- 2026-06-22 dedicated spell start fix: `同舟拼词记` no longer uses the shared `startGame` path. The frontend routes only this mode to a new `startCoopSpell` cloud function, which is a 2.2 KB package with no full-bank/PK/power-up initialization. It transactionally validates the room mode, two real players, both ready states, and a valid 4+ letter word pool; then generates four blanks, assigns two to each openid, and starts the room. Ready and not-ready integration paths passed, and `startCoopSpell` was created/deployed successfully.
- 2026-06-22 startup send-retry fix: normal app launch no longer calls `getOpenId`. Cloud initialization is local-only, and identity is remembered from `createRoom`/`joinRoom` responses when the user actually enters multiplayer. Pending invite joins wait for cloud initialization and remember the returned openid. A launch simulation verified the correct env initializes with zero startup cloud calls, removing the only immediate outbound request that could trigger the WeChat runtime `发送失败/继续重试` prompt.
- 2026-06-22 invitation reliability fix: the room `邀请` button now opens an explicit action sheet with `发送给微信好友` and `复制房间码`. Share payload titles use the active mode name and include the room code; the query carries `invite=1&roomCode=...`. If active sharing is unavailable or the action sheet fails outside cancellation, the flow falls back to copying the room code. The standalone copy button now confirms success and provides a manual-code message on clipboard failure. Share and copy paths passed a mocked runtime test.
- 2026-06-23 logging/diagnostics pass: added structured client logs with `[word-game]` prefixes for boot, cloud init/calls, scene changes, busy/toast state, room polling, room state transitions, invite/share/copy, PK fish taps, power-ups, coop spell letters/submit/skip, slow render frames, render loop gaps, and slow touch handlers. Logs redact openids/answers/word meanings and summarize large arrays such as room word pools. Added server-side request logs to `createRoom`, `joinRoom`, `toggleReady`, `addBot`, `startGame`, `startCoopSpell`, `catchFish`, `finishGame`, and `getOpenId`; all changed cloud functions were deployed to `cloud1-d3gre86i51a49821a`. Old page-based fallback files also now emit `[legacy-*]` logs if they are accidentally opened.
- 2026-06-23 live debugging aid: added a visible `日志` button on the home screen and room screen. It copies the latest in-memory client diagnostic snapshot (scene, room, player count, game options, status message, and the latest `[word-game]` logs) to the clipboard so real-phone failures can be pasted back without relying on DevTools console access. The room-screen `日志` button is below the room actions so it does not cover the invite/room code. Latest preview package size after this change is about `663.4 KB` (`679328` bytes).
- 2026-06-23 coop spell live-log fix: two pasted client logs were different rooms. Room `G2LTNZ` had one player and no start failure; room `4Y4C3G` had two ready real players and failed in `startCoopSpell` with `document.update:fail -5`. The function was changed from a transaction that rewrote `gameOptions.roomWords` to a normal read plus minimal room update, preserving the stored room word pool while only writing gameplay fields. Local two-player simulation passed, and `startCoopSpell` was redeployed successfully to `cloud1-d3gre86i51a49821a` as a `2.4 KB` package.
- 2026-06-23 debug log copy fix: pasted phone logs were being truncated at about 5 KB, leaving invalid JSON before the actual start-call failure entries. The `日志` button now copies compact JSON with a dynamic size cap, keeps recent logs plus top-level room/error state, verifies the clipboard with `wx.getClipboardData`, and automatically retries with a smaller summary if verification fails. Frontend syntax checks passed and a new preview was generated; latest preview package size is about `665.2 KB` (`681195` bytes).
- 2026-06-23 coop spell update-shape fix: same-room two-phone logs for room `VYVBCU` proved both clients had two ready players and both calls failed inside `startCoopSpell` at `document.update:fail -5`; the room word pool count was 56 on both start calls, so mismatched local word banks were not the likely cause. `startCoopSpell` now stores `spellQuestion` as a JSON string in the database, removes empty-object/empty-array writes from the start update, uses numeric timestamps, sanitizes reset players without `null`, and falls back from full update to split/slim updates before throwing. `catchFish` now parses string/object spell questions and writes next spell questions as strings, with non-empty submission reset markers. The frontend parses string spell questions when applying room snapshots. Local syntax checks and start simulation passed; `startCoopSpell` (`2.8 KB`) and `catchFish` (`200.2 KB`) were redeployed, and a new preview was generated (`665.4 KB`, `681402` bytes). Follow-up optimization idea: precompute spell templates (`blankPositions`, `mask`, segment indexes) per word-bank unit so game start only samples a prepared template.
- 2026-06-23 coop spell submit/history update: `同舟拼词记` now uses deferred team judging instead of showing partner letters in real time. A player submit only stores a `submitted` marker and locks their keyboard; the partner sees only the filled marker. When both players submit, `catchFish` judges both segments together, applies team `+100/-100`, syncs both displayed player scores to the team score, moves to the next word, and stores a per-word history record showing each player's submitted letters and correctness. Manual skip records a `manualSkip` round and deducts `100`; automatic word timeout records `timeout` without extra deduction; total game finish records an unfinished current word as `gameOver` if needed. `spellHistory` is stored in cloud DB as JSON-string entries to avoid nested update risk, then normalized on the frontend. The result screen for coop spell shows only the total score. Local history records now save `teamScore` and `spellHistory`; coop spell rows can be tapped to open a new detail page with per-word/per-player results. Deployed `startCoopSpell` (`2.8 KB`), `catchFish` (`200.8 KB`), and `finishGame` (`2.0 KB`); generated new preview package `670.1 KB` (`686201` bytes).
- 2026-06-24 coop spell input reset fix: fixed a bug where submitted letters/status could leak into later words and lock the keyboard. Each `spellSubmissions` entry now carries its `questionId`, reset markers carry `_questionId`, and both cloud functions and frontend ignore submissions that do not belong to the current `spellQuestion.id`. Frontend slot rendering also only reads local input when `coopSpellInputQuestionId` matches the current question, and round completion clears the local input/submission cache before the next fetch. A local cloud simulation with stale old submissions verified that old answers are ignored, the next word remains editable, and history records the new word's answers. Deployed `startCoopSpell` (`2.9 KB`), `catchFish` (`200.9 KB`), and `finishGame` (`2.0 KB`); generated new preview package `670.6 KB` (`686724` bytes).
- 2026-06-20 study update: `studyShowMeaning` is the session-wide Chinese visibility setting and now persists while moving through words. `studyRevealCurrentMeaning` is a separate one-word reveal that resets on previous/next/random/bank change. The right thumb column places `查看本词释义` directly above a larger `下一个` button. Global visibility uses explicit `全局隐藏中文` / `全局显示中文` labels.
- Recommended first validation on resume: use two real phones, create/join a `默契捕词赛` room and a `同舟拼词记` room, ready both players, verify one-player start stays disabled, and verify no robot control appears.

The game is running as a WeChat mini game with cloud functions. The latest work focused on rebuilding the word-bank system from the provided Word document:

`C:\Users\13031\.vscode\Downloads\吉林高中英语人教版单词表_精简去长词人名版.docx`

Parsed result:

- 36 textbook unit banks
- 2653 textbook word entries
- 8 senior-year review banks
- wrong-word bank integrated into word-bank selection

Current word-bank hierarchy:

- `人教版`
  - final selectable banks are shown directly in a two-column grid, such as `Welcome Unit`, `必修一 Unit 1`, `必修二 Unit 1`, `选必一 Unit 1`, `高三复习 全部词汇`.
- `错题词库`
  - `错题库`

Important UI behavior:

- Main screen no longer has the old `题目范围` option.
- Wrong-word practice is now selected from the word-bank page.
- Word-bank page selection is two-step:
  - tap a unit to temporarily select it
  - tap `确定选择` to return to the home screen
- `Welcome Unit` label was shortened to remove `必修一`, because the full text was too long for the button.
- The word-bank picker no longer shows the middle `年级上下册` column. Top buttons are version/type categories (`人教版`, `错题词库`), and the lower area shows final word-bank options directly in two columns.
- The selected word-bank banner no longer shows the `暂选` label or grade/term text such as `高一下`; it displays labels like `人教版-必修一 Unit 3`.
- A local word-coin system was added. First launch initializes `wordCoins` to `1000`; normal textbook/review banks are locked until unlocked for `10` coins each and saved in `unlockedWordBanks`. The wrong-word bank remains a free review entry.
- The home setup buttons are wider, and the word-bank confirm button is larger. Coin balance is shown on the home setup card and word-bank picker.
- Performance pass: long words now shrink the horizontal movement range based on cockroach width, word labels are clamped near the visible screen, cockroach metrics are cached, cockroach body drawing avoids per-frame gradients, render cadence is dynamic (`33ms` playing, slower on room/static screens), and room polling coalesces overlapping cloud reads.
- Home wrong-word-bank helper text was removed. Room ready/start guidance is now a pill-shaped status strip above the action buttons. Cockroach antennae and legs were shortened to reduce visual clutter.
- Background/sprite optimization pass: grass backgrounds are rendered once into an offscreen canvas and reused each frame; cockroach bodies are cached as width-bucketed sprites, with only position/rotation/word label drawn per frame. Both paths fall back to direct drawing if offscreen canvas/drawImage fails.
- Latest tuning: active cockroach count and lanes were reduced from `8` to `6` in frontend, `startGame`, and `catchFish`; top Chinese prompt supports two-line wrapping in a left text area so it does not collide with the timer; word-bank picker bottom now has side-by-side `返回` and `确定选择`; `createRoom` was lightened by removing the full word-bank import and deleting its unused `wordBankData.js`.
- Deployed updated cloud functions to `cloud1-d3gre86i51a49821a`: `createRoom`, `startGame`, `catchFish`. `createRoom` now deploys as a small package (`1.6 KB`) instead of bundling/parsing the full word bank.
- Power-up slots now float above the player avatar, are larger, have a white badge background, and show a gold pulse ring when the local player can use them.
- Room setup speed pass: `createRoom`, `toggleReady`, `addBot`, and `startGame` now return room/player snapshots so the frontend updates immediately instead of calling `fetchRoom()` after each action. Bot selection also applies an optimistic local update. Updated functions were deployed to `cloud1-d3gre86i51a49821a`.
- Runtime UI cache pass: prompt panel frame, timer base, score dock, score-box backgrounds, and VS badge are now cached as offscreen sprites. Per-frame gameplay rendering mostly updates moving cockroaches, dynamic text, scores, power-ups, and effects.
- Latest UI pass: home now has a `玩法` button that opens a new `GAME.HELP` canvas scene with concise gameplay, scoring, word-bank, robot, power-up, and match-record instructions. Visible `词币` wording was changed to `金币`; the local storage key remains `wordCoins` to preserve existing balances. The coin pill was restyled with a gold gradient, `金` icon, and adjusted positions on home and word-bank screens.
- Latest gameplay HUD pass: the top Chinese target panel is taller, uses smaller adaptive text, and anchors bottom status text to avoid overlap with the `目标中文` badge and timer. Power-ups now render as two larger fixed slots above each score card: pesticide and swatter. Multiple power-ups of the same type are merged into one slot with an `X2`-style count badge, and the local player's active slots remain clickable.
- Latest room UI pass: the robot opponent selector in `drawRoom` is now a custom switch card instead of a plain button. Empty player slot and existing bot row both show robot name, difficulty badge, and explicit `点击切换` / `点击切换等级` text while preserving the existing `cycleBotPreset` action.
- Latest study-mode pass: home now includes a `背单词` module for the currently selected unlocked word bank. It opens a new `GAME.STUDY` canvas scene that shows English and Chinese flashcard-style content without room/gameplay logic. Study controls include previous, next, random, show/hide Chinese, add unfamiliar word to wrong-word bank, change bank, and return home.
- Latest audio pass: added a lightweight local looping background music file at `miniprogram/assets/bgm.wav` and `wx.createInnerAudioContext` playback in `game.js`. Music starts on launch when allowed, retries on first touch if autoplay is blocked, pauses on `wx.onHide`, and resumes on `wx.onShow`.
- Latest co-op pass: added a home `双人合作` entry and new `GAME.COOP_SELECT` scene. Co-op mode creates rooms with `gameOptions.matchMode='coop'` and either `coopMode='shared'` (`默契捕词赛`) or `coopMode='spell'` (`同舟拼词记`). Shared co-op reuses the moving-word gameplay: either player can answer the Chinese target, correct answers advance, wrong answers deduct score, and power-ups are disabled. Spell co-op creates `spellQuestion` rounds where each player has a fixed missing-letter segment; both players must submit correct segments before the next word. Both modes require two real players and do not support robots.

## Important Files

Frontend:

- `miniprogram/game.js`
  - Main canvas game logic and UI.
  - Uses `require("./wordBankData")`.
  - Word-bank picker scene is `GAME.BANKS`.
  - Relevant helpers include:
    - `getSelectableWordBankIds`
    - `getUnitsForTerm`
    - `syncBankPickerFromSelected`
    - `drawBankPicker`
    - `isWrongBankId`

- `miniprogram/wordBankData.js`
  - Generated word-bank data module.
  - Contains `DEFAULT_BANK_ID`, `WRONG_BANK_ID`, `WORD_BANK_PROVINCES`, `WORD_BANK_TERMS`, `WORD_BANKS`.

Cloud functions:

- `cloudfunctions/createRoom/index.js`
  - Validates/normalizes `bankId`.
  - Uses `require("./wordBankData")`.

- `cloudfunctions/startGame/index.js`
  - Creates initial fishes from the selected word bank.
  - Uses `require("./wordBankData")`.

- `cloudfunctions/catchFish/index.js`
  - Handles answer taps, robot answers, power-ups, and refill words from selected bank.
  - Uses `require("./wordBankData")`.

- `cloudfunctions/finishGame/index.js`
  - Finishes timed-out rooms.
  - Co-op rooms finish without a winner openid.

- `cloudfunctions/submitFeedback/index.js`
  - Receives player feedback from the home `反馈` button.
  - Writes records to the cloud database `feedbacks` collection.
  - Keep writes server-side because `database.rules.json` has client writes disabled.

Each cloud function has its own copy of:

- `cloudfunctions/createRoom/wordBankData.js`
- `cloudfunctions/startGame/wordBankData.js`
- `cloudfunctions/catchFish/wordBankData.js`

These copies are currently identical to `miniprogram/wordBankData.js`.

## Recent Deployed State

The following cloud functions were deployed successfully after the latest logging update:

- `createRoom`
- `joinRoom`
- `toggleReady`
- `addBot`
- `startGame`
- `startCoopSpell`
- `catchFish`
- `finishGame`
- `getOpenId`
- `submitFeedback`

Environment used:

`cloud1-d3gre86i51a49821a`

WeChat DevTools path:

`C:\Program Files (x86)\Tencent\微信web开发者工具`

Latest preview was generated successfully:

`C:\work\wxgame_cocos\.codex-preview\preview.png`

Latest preview package size was about `2.1 MB` (`2252810` bytes).

## Useful Commands

Syntax checks:

```powershell
node --check C:\work\wxgame_cocos\miniprogram\game.js
node --check C:\work\wxgame_cocos\miniprogram\wordBankData.js
node --check C:\work\wxgame_cocos\cloudfunctions\createRoom\index.js
node --check C:\work\wxgame_cocos\cloudfunctions\startGame\index.js
node --check C:\work\wxgame_cocos\cloudfunctions\catchFish\index.js
node --check C:\work\wxgame_cocos\cloudfunctions\finishGame\index.js
```

Deploy cloud functions:

```powershell
& 'C:\Program Files (x86)\Tencent\微信web开发者工具\cli.bat' cloud functions deploy --env cloud1-d3gre86i51a49821a --names createRoom --remote-npm-install --project 'C:\work\wxgame_cocos' --lang zh
& 'C:\Program Files (x86)\Tencent\微信web开发者工具\cli.bat' cloud functions deploy --env cloud1-d3gre86i51a49821a --names startGame --remote-npm-install --project 'C:\work\wxgame_cocos' --lang zh
& 'C:\Program Files (x86)\Tencent\微信web开发者工具\cli.bat' cloud functions deploy --env cloud1-d3gre86i51a49821a --names catchFish --remote-npm-install --project 'C:\work\wxgame_cocos' --lang zh
& 'C:\Program Files (x86)\Tencent\微信web开发者工具\cli.bat' cloud functions deploy --env cloud1-d3gre86i51a49821a --names finishGame --remote-npm-install --project 'C:\work\wxgame_cocos' --lang zh
```

Generate preview:

```powershell
$outDir='C:\work\wxgame_cocos\.codex-preview'
New-Item -ItemType Directory -Force -Path $outDir | Out-Null
& 'C:\Program Files (x86)\Tencent\微信web开发者工具\cli.bat' preview --project 'C:\work\wxgame_cocos' --qr-format image --qr-output "$outDir\preview.png" --info-output "$outDir\preview-info.json" --lang zh
```

## Known Design/Implementation Notes

- The project is canvas-based, not a normal multi-page WXML UI. New screens are usually implemented as scene values in `GAME`.
- The game currently forces grass/cockroach theme with `FORCED_VISUAL_SKIN = "grass"`.
- Single-player practice was previously removed from the main flow; current main flow is double-player room with optional robot opponent.
- Robot opponent is chosen inside the room, not on the home screen.
- Power-up rules:
  - continuous 3 correct answers grants a power-up
  - pesticide kills current visible words and grants score
  - swatter stuns opponent for 5s
  - pesticide effect is synchronized to both players through room fields
- Wrong answers are collected locally into `wrongWords` storage.
- When wrong-word bank is selected, `wrongWords` are sent in `gameOptions` during room creation.
- Co-op modes are selected before room creation:
  - `matchMode='coop'`, `coopMode='shared'`: shared Chinese-target clicking; either player can answer, correct advances, wrong deducts score, power-ups disabled.
  - `matchMode='coop'`, `coopMode='spell'`: active two-player room flow. Player 1 fills the first two blue blanks and player 2 fills the last two pink blanks.
  - Both co-op modes require two real players. Robots are restricted to normal PK rooms.
- Current spell co-op stores answer segments in the room document as part of `spellQuestion`. This is acceptable for prototype testing, but should be hardened later if anti-cheat matters.

## Caution Points

- 2026-06-28 update: co-op spell interaction polish.
  - Per-word spell timer is now 20 seconds on both client and `catchFish`.
  - Co-op spell rooms poll every 600ms instead of 1000ms to reduce partner-submit/switch-word delay.
  - Submit is optimistic locally: the player sees "已提交，等队友" immediately, with rollback on cloud failure.
  - In-game co-op spell prompts now emphasize checking the Chinese meaning and filling the player's own colored blanks.
- 2026-06-27 update: co-op spell now uses pre-generated blank templates.
  - Added `spellWordBankData.js` to the miniprogram and spell-related cloud functions.
  - 44 built-in banks generate 6351 fixed spell templates with `mask`, `blankPositions`, and `slots`.
  - Room creation now stores `roomSpellQuestions`; `startCoopSpell`, `catchFish`, and legacy `startGame` draw from templates instead of randomly blanking letters at runtime.
  - If one player submits and the other times out, `timeoutCoopSpell` records the submitted player, leaves the other as unsubmitted in spell history, resets `spellSubmissions`, and the next room snapshot clears all blank inputs.
- 2026-06-26 update: co-op spell input is now isolated by `spellQuestion.id`.
  - Frontend filters `spellSubmissions` to the active question only.
  - Room snapshots clear local draft input when the question changes.
  - Late submit callbacks from an old question only trigger a room refresh and do not overwrite the current question.
  - While a completed round is waiting for the next room snapshot, letter input is temporarily locked to avoid editing the old word.
- `wordBankData.js` is currently duplicated into frontend and three cloud functions. If the data is regenerated, copy/update all four locations.
- Do not rely on latest `.docx` blindly in Downloads because Word creates temporary `~$...docx` lock files that are not valid zip/docx files.
- When parsing the Chinese `.docx` through PowerShell, set UTF-8 output encoding, otherwise Chinese matching can fail:

```powershell
$OutputEncoding = [System.Text.Encoding]::UTF8
[Console]::OutputEncoding = [System.Text.Encoding]::UTF8
$env:PYTHONIOENCODING='utf-8'
```

- The project directory is now a git repository connected to `git@github.com:simpledcc/wxGame.git`; still use file-level care because user testing may leave local changes.

## Suggested Next Steps

### 2026-07-15 Home hierarchy and clarity refinement

- The authoritative Home hierarchy is Create Room, Join Room, Study/Bank, Catalog/History, followed by Privacy/Feedback. Do not restore the superseded Start Study-first layout.
- Keep avatar, coin and settings free of white card backgrounds. Any replacement art must preserve the current transparent icon slots and WeChat capsule clearance.
- Enhancement source packages are available outside Git at `C:\work\游戏图片\游戏主界面图片`; replace runtime images only through the existing controlled Home-art import workflow.
- The only external release action remains two-real-phone create/join/ready/start acceptance; gameplay implementation is still outside this Home-only refinement.

### 2026-07-14 full-screen pre-game visual update

- Branch `feature/pre-game-ui-home-goal` now uses `ResolutionPolicy.FIXED_WIDTH` and a dynamic `960-1440` logical viewport for Boot and all persistent-shell routes.
- Home, mode catalog, create/join/lobby, Bank, Study, History, Feedback, Help and Result share a full-height programmatic learning-park scene; formal bitmap backgrounds, characters and icons are still pending standalone asset delivery.
- Runtime simulation now runs at `393x852` and checks the derived `640x1387` viewport for visible-node overflow while exercising the full preparation flow.
- Cocos Creator 3.8.8 build and WeChat DevTools iPhone 12/13 inspection passed; the visible Home, Bank, Study, mode catalog, create-room and History screens have no top/bottom black bars.
- Gameplay Bundles, game rules, timers, robot compatibility, cloud functions and the legacy mini-program client were not changed.
- Next external action is two-real-phone create/join/ready/start acceptance. The next art action is to import approved standalone assets through Creator; never crop the reference composite.

### 2026-07-13 unified pre-game page update

- Branch `feature/pre-game-ui-home-goal` now applies the Home visual system to Bank, Study, Co-op Select, Room, Result, History, Feedback and Help routes.
- `COCOS_PRE_GAME_PAGES_DESIGN.md` is the source of truth for page hierarchy, color roles, control sizes and per-page behavior.
- Create, join and active lobby still share `RoomRuntimeScreen`; `roomEntryIntent` controls the initial title and guidance, while an accepted room changes the title to “房间大厅”.
- All business controllers, cloud contracts and gameplay Bundles remain unchanged. Continue to modify gameplay screens in their own branch/ownership stream.
- Automated checks cover 360/393/430 target widths and require approximately 44px minimum touch height for every visible button on the unified pages.
- Dedicated bitmap art and Creator screenshots remain optional follow-up work; current pages use the committed theme background and programmatic icon fallbacks.

### 2026-07-11 Cocos V0 audit update

- `develop/cocos-migration` has passed `npm run verify`, `npm run build:wechat:dry-run`, and `npm run inspect:wechat-build` for the V0 visual baseline.
- The WeChat DevTools iPhone 12/13 simulator now renders the Cocos home screen after the user actually selected “Trust and Run”; the trust dialog did not reappear during the follow-up inspection.
- Controlled Windows automation can observe the simulator but its synthetic clicks are not forwarded into the game canvas. Do not treat this as a confirmed player-side input bug; use a manual DevTools interaction or a real phone to finish S03/S05-S13 screenshots and performance measurements.
- The original PK “正在加载界面…” overlay was not reproduced by the subsequent homepage run. Keep it as an unconfirmed V0 manual-reproduction item before changing `HomePlaceholder` or bundle loading logic.
- V0 remains in progress. The source of truth is `COCOS_VISUAL_BASELINE_V0.md`; do not start V1 visual work until the screenshot/performance/PK reproduction gates are closed.

### 2026-07-11 Cocos V0 Web visual audit supplement

- Added the non-business build config `cocos-client/tools/web-visual-build-config.json`. It produces the ignored local preview `cocos-client/build/web-visual/` for Cocos `web-mobile` visual inspection only; it must never replace the WeChat release build.
- Captured Web debug screenshots under ignored `cocos-client/build/visual-baseline/`: Boot, home at 360/393/430, bank paging, study normal/hidden meaning, cooperative selection, PK/spell rooms, history, help and feedback validation.
- Web profiler measurements: home at 393×852 was 60 FPS, 1.20 ms frame time, 34 Draw Calls, 876 triangles, 5.14 MiB texture memory and 36.07 MiB buffer memory. See `COCOS_VISUAL_BASELINE_V0.md` for the full table and route-node baseline.
- Historical note: the browser audit once reported a same-node `Graphics`/`EditBox` renderer conflict. G1 split `RuntimeUi.edit()` into an `EditBox` host and child `Graphics` background; `test:pre-game-ui` now enforces that structure. Do not reopen this issue unless a fresh Creator console reproduces it on the current branch.
- Web preview uses `MemoryRuntimePort`, so it cannot create cloud rooms. V0 still needs real WeChat/phone screenshots for PK, shared co-op, spell co-op, result and spell-history detail, plus real multiplayer performance data.

### 2026-07-11 Cocos V0 local multiplayer-render audit

- Created only an ignored generated copy at `cocos-client/build/web-visual-audit/`; it injects `audit-wx-stub.js` before the Web entry so the existing `WechatRuntimePort`, room services, route transition and Cocos gameplay bundles can render deterministic audit rooms. It is not source code, is not tracked, does not connect to cloud development, and must never be used as a release build.
- Captured deterministic 393 x 852 screenshots for PK room/ready/game, shared-coop room/game, spell room/ready/game/filled letters, and spell result under `cocos-client/build/visual-baseline/`. The spell flow entered `X` and `C`, submitted, and rendered a 100-point result page.
- New V0 finding: completing a PK or shared-catch action triggers `GameplayFeedbackPool.onDestroy()` after its labels have been destroyed. `clear()` accesses `label.node.active`, produces `TypeError: Cannot set properties of null`, repeats destroy warnings, and leaves the runtime route loading overlay visible. The affected source is `cocos-client/assets/bundles/mode_pk/scripts/GameplayFeedbackPool.ts`; do not change it in V0. V1 must make the pool destruction-safe, then test PK and shared-coop completion in actual WeChat.
- The old `RuntimeUi.edit()` renderer conflict is closed in the current source and covered by an automated layering test. V0 remains open only for the separate evidence items that local audit fixtures cannot prove, including real two-player cloud synchronization and true phone performance.
- A fresh real-device preview QR was generated successfully from `cocos-client/build/wechatgame`; it is stored at ignored `.codex-preview/v0-audit/cocos-preview.png` with `cocos-preview-info.json`. The DevTools preview pack reported 6,469,608 bytes total and 4,108,623 bytes in the main package. Use two devices to join the same room and capture the remaining V0 evidence before beginning V1.
- The 36 V0 PNG screenshots are now frozen under tracked `docs/visual-baseline/v0/screenshots/` (956,174 bytes total). `manifest.json` records dimensions and SHA-256 values; `README.md` documents provenance and the difference between Web audit fixtures and real-device evidence. `COCOS_VISUAL_BASELINE_V0.md` now also contains the required interaction-recording checklist and a per-page before/issues/after-acceptance matrix.


1. Test the latest preview on phone:
   - create a normal `双人PK` room and verify existing gameplay still works
   - create a `默契捕词赛` room and verify either player can answer and team score changes
   - create a `同舟拼词记` room at each selectable duration and verify the room/game timers match that selection
   - verify each `同舟拼词记` player can only submit their own segment and no robot control appears

2. Check layout on narrow phones:
   - home screen after adding `背单词` and `双人合作`
   - co-op selection cards
   - co-op spell status cards and input button
   - result screen for co-op rooms

3. Harden co-op spell if needed:
   - move correct answer segments out of the public room document
   - add per-round attempt limits or error feedback
   - decide final scoring rules for `同舟拼词记`

4. Consider reducing cloud function package duplication later by centralizing shared word-bank data if the deployment/runtime structure allows it.
