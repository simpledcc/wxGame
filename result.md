# Cocos 新编译版本图片加载与清晰度排查结果

排查日期：2026-07-16；2026-07-21 补充开发者工具分包缓存复现
排查基线：`5b73bdf` 之后的高清导入完成提交，分支 `feature/pre-game-ui-home-goal`
Cocos Creator：`3.8.8`

## 0. 画质修复进展

2026-07-16 已在指定 Creator 3.8.8 电脑完成资源升级：18 张运行图由旧版低尺寸、索引色输出升级为 `1080x1920` 背景、`1280x400` Logo、`512x768` 角色、`768x328` 按钮皮肤和 `320x320` 图标，透明 PNG 全部改为真彩 RGBA，总计 `3,453,135` 字节并继续放在 `home_common` 分包。

`home-art:sync-upgrade` 保留原 UUID 完成替换，Creator 自动重导了新的纹理尺寸。18 张运行图与批准源图逐项 SHA-256 一致，`home-art:status` 为 `imported`，`home-art:verify-import`、完整微信构建、包体检查和微信开发者工具显示检查均通过。

### 0.1 真机按钮色块与多层阴影修复

对 `C:\work\wxgame\picture\实际图.png` 的 `1206x2622` 真机截图与 `768x328` 按钮原图逐像素比较后确认，屏幕缩放只是次要影响。设计宽度 `640` 映射到截图宽度 `1206`，倍率约 `1.884`；旧逻辑又把 2x 图片约 56 像素的九宫格边缘直接当成 56 个设计单位，使 `132` 高按钮的中心区域只剩约 20 单位，短按钮更严重。

同时，正式 PNG 已包含高光、渐变、边框和阴影，但程序化底色、偏移阴影和顶部高光在图片加载后仍继续显示，并从透明圆角后露出，形成截图中的矩形色块和多层阴影。当前修复让高清皮肤以 2x 内容尺寸、0.5 节点缩放渲染，使 56 源像素正确映射为 28 设计单位；正式皮肤加载成功后关闭三层程序化兜底装饰。图片加载失败时兜底仍保留，按钮点击区域和业务逻辑不变。

## 1. 结论

1. 当前 Cocos 微信构建**没有漏打包图片**。`home_common` 已作为微信分包进入 `cocos-client/build/wechatgame/subpackages/home_common/`，包含 18 张正式图片对应的 SpriteFrame 和原始纹理。
2. 使用正确构建目录打开微信开发者工具时，首页背景、Logo、头像、金币、角色、功能图标和按钮皮肤均能加载。2026-07-16 的实际开发者工具画面已确认高清替换图正在显示。
3. “界面很简单”主要来自资源覆盖范围，而不是构建失败。除首页外的准备页面目前主要由 Cocos `Graphics`、`Label` 和卡片布局绘制，只复用少量公共图标，没有各页面独立的场景、角色、装饰和插画资源。
4. “完全没有图片”首先应检查是否打开了错误项目。仓库根目录 `project.config.json` 指向旧版 `miniprogram/`；Cocos 新版必须导入 `cocos-client/build/wechatgame/`。
5. 旧版图片清晰度确实受 V0 资源预算影响：运行图从母图大幅缩小并压缩，高分辨率手机再次放大后发软。当前运行图已升级，像素不足和索引色量化不再是首页的主要限制。
6. 当前资源加载失败会静默保留程序化 fallback，因此分包、缓存或路径异常时，页面仍能操作，却会看起来像“没有加载图片”。本次正确构建中未复现资源加载失败，但该诊断可见性不足是后续需要改进的风险。

## 2. 构建证据

| 检查项 | 结果 |
| --- | --- |
| 微信产物目录 | `cocos-client/build/wechatgame/` |
| 产物文件 | 147 个，共 `9,970,209` 字节 |
| 主包 | `4,121,077 / 4,194,304` 字节 |
| `home_common` 分包 | 58 个文件，共 `3,472,932` 字节 |
| `home_common` 源资源 | 18 张图片，23 份 Creator 元数据 |
| 构建检查 | `npm run inspect:wechat-build` 通过 |
| 实际显示 | 正确构建目录下首页正式图片已显示 |

Creator 构建日志包含 `WordBankData.generated.ts exceeds 500KB` 的 Babel 格式化降级提示。它针对生成的词库 TypeScript 文件，不是图片加载失败，也不会把图片从产物中移除。

## 3. “没有加载图片”的原因优先级

### P0：打开了旧版项目或错误目录

- 仓库根目录的 `project.config.json` 配置为 `miniprogramRoot: "miniprogram/"`，对应保留的旧版微信客户端。
- Cocos 生成项目的配置文件位于 `cocos-client/build/wechatgame/project.config.json`，项目名是 `word-battle-park-wechatgame`，并声明 `home_common`、`mode_pk` 和 `mode_spell` 分包。
- 若从仓库根目录点击编译或上传，看到的不会是当前 Cocos 构建。

正确做法：在微信开发者工具中单独导入 `cocos-client/build/wechatgame/`，不要导入仓库根目录或 `miniprogram/`。

### P1：其他页面没有独立美术资源

当前 `home_common` 只提供：

- 1 张首页竖屏背景
- 1 张 Logo
- 12 个公共图标/角色资源
- 4 张无文字按钮皮肤

词库、背词、玩法目录、创建/加入/准备房间、结算、战绩、反馈和玩法说明只在局部复用这些图标。页面主体仍是程序化背景、卡片、文字和按钮，没有独立背景、角色动作、场景物件或装饰层，所以功能完整但美术丰富度有限。

### 已修复：运行图片分辨率和压缩率偏低

| 资源 | 升级前运行图 | 当前运行图 | 当前判断 |
| --- | --- | --- | --- |
| 背景 | `750x1334` JPG，170,868 B | `1080x1920` JPG，674,436 B | 已消除主要放大损失；画面本身采用柔和卡通笔触，不等于像素模糊 |
| Logo | `640x200` 索引色 PNG，28,211 B | `1280x400` 真彩 RGBA PNG，467,844 B | 当前显示尺寸有充足像素余量，边缘明显更清楚 |
| 角色 | `192x256` 索引色 PNG，8,951 B | `512x768` 真彩 RGBA PNG，307,057 B | 大尺寸展示不再依赖明显放大 |
| 按钮皮肤 | 单张 `384x164` 索引色 PNG，约 6-8 KB | 单张 `768x328` 真彩 RGBA PNG，约 187-205 KB | 九宫格边缘和高光细节明显改善 |
| 公共图标 | 单张 `192x192` 索引色 PNG | 单张 `320x320` 真彩 RGBA PNG | 当前卡片和顶部控件尺寸足够；继续放大作角色插画仍需独立大图 |

纹理导入配置继续使用线性过滤并关闭 mipmap。线性过滤本身不是错误；旧低分辨率图被放大时会显得更软，新图有足够像素后影响已显著降低。模拟器非整数缩放（例如 121%）仍可能产生轻微柔化，应以 100% 截图或真机作为最终判断。

原图逐张检查发现：新背景是刻意采用柔和笔触的卡通绘景，Logo、按钮、角色和图标在原始像素下清楚。部分透明图边缘存在极轻微彩色边缘，这是源图抠图/绘制边缘，不是 Cocos 构建压缩；在当前明亮背景上不明显。升级只能保留母图已有细节，不能凭空增加母图没有的纹理。

另外，其他页面继续使用主题中的 `960x640` 横屏背景作为通用背景。它在竖屏页面中需要 cover 裁切和缩放，不适合作为高质量竖屏场景。

### P2：加载失败时静默回退

`PreGameUi.visualSlot()` 和按钮皮肤加载都会捕获资源异常并保留程序化 fallback。这样能保证游戏不因图片失败而白屏，但当前没有把 Bundle 名、资源路径和错误原因输出到可见诊断日志。

因此以下情况都可能表现为“页面能用，但没有正式图片”：

- `home_common` 分包加载失败或未进入当前开发者工具项目
- 开发者工具仍使用旧编译缓存
- 打开了旧输出目录
- 图片路径或 Creator 元数据与运行清单不一致

### P2：开发者工具缓存

项目启用了 `md5Cache`，微信开发者工具也会保留编译和文件缓存。重新构建后若工具没有刷新到新 hash，可能暂时继续显示旧画面。此前清理 compile/file 缓存并重新打开正确构建目录后，正式图片恢复显示。

2026-07-21 已复现一个更具体的时序：微信开发者工具保持项目打开时，Creator 外部构建先向临时 `assets/home_common` 输出文件，随后删除该目录并创建最终 `subpackages/home_common`。开发者工具的文件监听日志完整记录了这次搬迁，但当时运行缓存仍按中间状态查找，因而报 `loadSubpackage:fail module not found`，页面则静默显示程序化 fallback。

构建完成后只需点击开发者工具顶部的“普通编译”一次。复测中控制台的 `home_common` 错误随即消失，首页背景、Logo、头像、金币、角色、图标和按钮皮肤全部恢复，并且选择玩法、创建房间和玩法介绍页继续正常显示。这说明产物结构和资源路径正确，不应为这个缓存现象修改 Bundle 名称、`game.json` 或 Creator 元数据。

## 4. 建议的确认步骤

在 `cocos-client/` 执行：

```powershell
npm run home-art:status
npm run home-art:verify-import
npm run verify
npm run build:wechat
npm run inspect:wechat-build
```

然后：

1. 等待 Creator 构建命令完全结束。
2. 在微信开发者工具中点击一次“普通编译”。
3. 确认项目名为 `word-battle-park-wechatgame`。
4. 确认资源管理器存在 `subpackages/home_common`。
5. 确认控制台没有 `loadSubpackage:fail module not found`，再检查首页和其他准备页。
6. 只有普通编译仍失败时，才关闭旧项目、清理编译/文件缓存并重新导入 `cocos-client/build/wechatgame/`。

## 5. 后续改进建议

### 第一阶段：提高可诊断性

- 为 `HomeArtManager` 增加 Bundle 加载开始、成功、失败日志。
- 记录失败资源键、实际路径、重试次数和当前 route。
- 开发版增加只读资源状态页；发布版不显示调试入口。

### 第二阶段：升级高清公共资源（已完成）

- 已导入 `1080x1920` 背景、`1280x400` Logo、`512x768` 角色、`768x328` 按钮皮肤和 `320x320` 图标。
- 已保留九宫格按钮和实时文字，没有将按钮文案烘焙进图片。
- 后续若将图标放大为角色级插画，应提供独立大图，不继续拉伸 `320x320` 图标。

高清资源源码验收门槛已调整为 4 MB，资源仍位于独立 `home_common` 分包，未占用只剩约 73 KB 空间的主包。

### 第三阶段：补齐其他页面美术

为每个准备页建立独立资源清单，至少包含：

- 竖屏场景背景或共享背景变体
- 页面主题插画/角色
- 状态图标和空状态插画
- 房间、词库、战绩和反馈所需装饰层

页面业务文字继续使用实时 `Label`，不要把词库名、分数、玩家名或按钮文字烘焙进图片。

## 6. 最终判断

旧版清晰度低的主要原因已经确认并修复：图片像素偏低、透明图索引色量化以及高 DPI 放大共同造成发软。当前正确构建已包含并显示高清首页资源，运行图没有被 Cocos 再次降采样。若仍感觉“简单”或“软”，优先区分三类情况：是否打开正确的 Cocos 构建目录、模拟器是否使用非整数缩放、当前页面是否本来就没有独立美术资源。下一步画面提升重点是补齐其他准备页面的专属背景和插画，而不是继续盲目放大现有首页图片。
