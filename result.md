# Cocos 新编译版本图片加载与清晰度排查结果

排查日期：2026-07-15  
排查基线：`afad54a`，分支 `feature/pre-game-ui-home-goal`  
Cocos Creator：`3.8.8`

## 0. 画质修复进展

2026-07-15 已按本报告的清晰度结论完成无 Creator 侧资源升级：18 张运行源图由旧版低尺寸、索引色输出升级为 `1080x1920` 背景、`1280x400` Logo、`512x768` 角色、`768x328` 按钮皮肤和 `320x320` 图标，透明 PNG 全部改为真彩 RGBA，总计 `3,453,135` 字节并继续放在 `home_common` 分包。仓库状态应显示 `upgrade-ready`。

本机没有 Cocos Creator，因此没有直接覆盖已导入图片或手写 `.meta`。指定 Creator 3.8.8 电脑需执行 `npm run home-art:sync-upgrade`，等待 Creator 自动重导后执行 `home-art:verify-import`、完整微信构建和目标视口清晰度检查。完成前，下文构建体积仍是旧低清基线的历史证据。

## 1. 结论

1. 当前 Cocos 微信构建**没有漏打包图片**。`home_common` 已作为微信分包进入 `cocos-client/build/wechatgame/subpackages/home_common/`，包含 18 张正式图片对应的 SpriteFrame 和原始纹理。
2. 使用正确构建目录打开微信开发者工具时，首页背景、Logo、头像、金币、角色、功能图标和按钮皮肤均能加载。2026-07-15 的实际开发者工具画面已确认这些图片正在显示。
3. “界面很简单”主要来自资源覆盖范围，而不是构建失败。除首页外的准备页面目前主要由 Cocos `Graphics`、`Label` 和卡片布局绘制，只复用少量公共图标，没有各页面独立的场景、角色、装饰和插画资源。
4. “完全没有图片”首先应检查是否打开了错误项目。仓库根目录 `project.config.json` 指向旧版 `miniprogram/`；Cocos 新版必须导入 `cocos-client/build/wechatgame/`。
5. 图片清晰度受 V0 资源预算影响。运行图从高分辨率母图大幅缩小并压缩，高分辨率手机上会再次放大，背景、Logo、角色和大按钮皮肤最容易显得模糊。
6. 当前资源加载失败会静默保留程序化 fallback，因此分包、缓存或路径异常时，页面仍能操作，却会看起来像“没有加载图片”。本次正确构建中未复现资源加载失败，但该诊断可见性不足是后续需要改进的风险。

## 2. 构建证据

| 检查项 | 结果 |
| --- | --- |
| 微信产物目录 | `cocos-client/build/wechatgame/` |
| 产物文件 | 147 个，共 `6,852,377` 字节 |
| 主包 | `4,121,077 / 4,194,304` 字节 |
| `home_common` 分包 | 58 个文件，共 `355,148` 字节 |
| `home_common` 源资源 | 18 张图片，23 份 Creator 元数据 |
| 构建检查 | `npm run inspect:wechat-build` 通过 |
| 实际显示 | 正确构建目录下首页正式图片已显示 |

Creator 构建日志包含 `WordBankData.generated.ts exceeds 500KB` 的 Babel 格式化降级提示。它针对生成的词库 TypeScript 文件，不是图片加载失败，也不会把图片从产物中移除。

## 3. “没有加载图片”的原因优先级

### P0：打开了旧版项目或错误目录

- 仓库根目录的 `project.config.json` 配置为 `miniprogramRoot: "miniprogram/"`，对应保留的旧版微信客户端。
- Cocos 生成项目的配置文件位于 `cocos-client/build/wechatgame/project.config.json`，项目名是 `word-battle-park-wechatgame`，并声明 `home_common`、`mode_pk` 和 `mode_spell` 分包。
- 若从仓库根目录点击编译或上传，看到的不会是当前 Cocos 构建。

正确做法：在微信开发者工具中单独导入 `cocos-client/build/wechatgame/`，不要导入仓库根目录或 `miniprogram/`。

### P1：其他页面没有独立美术资源

当前 `home_common` 只提供：

- 1 张首页竖屏背景
- 1 张 Logo
- 12 个公共图标/角色资源
- 4 张无文字按钮皮肤

词库、背词、玩法目录、创建/加入/准备房间、结算、战绩、反馈和玩法说明只在局部复用这些图标。页面主体仍是程序化背景、卡片、文字和按钮，没有独立背景、角色动作、场景物件或装饰层，所以功能完整但美术丰富度有限。

### P1：运行图片分辨率和压缩率偏低

| 资源 | 母图 | 当前运行图 | 影响 |
| --- | --- | --- | --- |
| 背景 | `941x1672` PNG，约 2.37 MB | `750x1334` JPG，170,868 B | 在 1080/1242 宽手机上需要放大，JPEG 细节进一步损失 |
| Logo | `1956x804` PNG | `640x200` PNG，28,211 B | 首页显示较大时，高 DPI 屏会放大纹理 |
| 角色 | `1086x1448` PNG | `192x256` PNG，8,951 B | 当前显示尺寸接近源尺寸，高 DPI 屏会明显放大 |
| 按钮皮肤 | `1254x1254` 图集 | 单张 `384x164` PNG，约 6-8 KB | 大按钮依赖九宫格拉伸，边缘和高光细节有限 |
| 公共图标 | `1254x1254` 图集 | 单张 `192x192` PNG | 小图标通常足够，大尺寸角色化展示不够 |

纹理导入配置使用线性放大过滤，并关闭 mipmap。线性过滤能减少锯齿，但在低分辨率图片被放大时会进一步呈现柔和、模糊的观感。

另外，其他页面继续使用主题中的 `960x640` 横屏背景作为通用背景。它在竖屏页面中需要 cover 裁切和缩放，不适合作为高质量竖屏场景。

### P2：加载失败时静默回退

`PreGameUi.visualSlot()` 和按钮皮肤加载都会捕获资源异常并保留程序化 fallback。这样能保证游戏不因图片失败而白屏，但当前没有把 Bundle 名、资源路径和错误原因输出到可见诊断日志。

因此以下情况都可能表现为“页面能用，但没有正式图片”：

- `home_common` 分包加载失败或未进入当前开发者工具项目
- 开发者工具仍使用旧编译缓存
- 打开了旧输出目录
- 图片路径或 Creator 元数据与运行清单不一致

### P2：开发者工具缓存

项目启用了 `md5Cache`，微信开发者工具也会保留编译和文件缓存。重新构建后若工具没有刷新到新 hash，可能暂时继续显示旧画面。此前清理 compile/file 缓存并重新打开正确构建目录后，正式图片恢复显示。

## 4. 建议的确认步骤

在 `cocos-client/` 执行：

```powershell
npm run home-art:status
npm run home-art:verify-import
npm run verify
npm run build:wechat
npm run inspect:wechat-build
```

然后：

1. 关闭微信开发者工具中的旧项目。
2. 清理编译缓存和文件缓存。
3. 重新导入 `cocos-client/build/wechatgame/`。
4. 确认项目名为 `word-battle-park-wechatgame`。
5. 确认资源管理器存在 `subpackages/home_common`。
6. 再检查首页和其他准备页。

## 5. 后续改进建议

### 第一阶段：提高可诊断性

- 为 `HomeArtManager` 增加 Bundle 加载开始、成功、失败日志。
- 记录失败资源键、实际路径、重试次数和当前 route。
- 开发版增加只读资源状态页；发布版不显示调试入口。

### 第二阶段：升级高清公共资源

- 竖屏背景建议至少 `1242x2208`，优先保留高质量 WebP/JPG。
- Logo 建议至少按最大显示物理像素的 1.5-2 倍导出。
- 角色建议提升到约 `512x768`，不要继续使用 `192x256` 作为大角色源图。
- 大按钮皮肤建议至少提升到 `768x328`，继续使用九宫格和无烘焙文字。
- 小图标可保留 `192x192`；需要大尺寸展示的资源单独提供高清版本。

提高资源分辨率前必须调整当前 `home_common <= 350 KB` 的源码验收门槛。主包只剩约 73 KB 空间，高清资源仍应留在独立分包，不能移入主包。

### 第三阶段：补齐其他页面美术

为每个准备页建立独立资源清单，至少包含：

- 竖屏场景背景或共享背景变体
- 页面主题插画/角色
- 状态图标和空状态插画
- 房间、词库、战绩和反馈所需装饰层

页面业务文字继续使用实时 `Label`，不要把词库名、分数、玩家名或按钮文字烘焙进图片。

## 6. 最终判断

当前问题不是单一的“Cocos 没把图片编译进去”。正确构建已经包含并能显示正式首页资源。用户观察到的现象由三部分共同造成：可能打开旧项目、其他页面尚无独立美术覆盖、V0 运行图为了包体预算被过度缩小和压缩。后续应先统一正确的微信导入路径，再增加资源加载日志，最后按分包预算升级高清资源并补齐其他页面美术。
