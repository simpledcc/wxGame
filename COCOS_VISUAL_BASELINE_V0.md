# Cocos 视觉基线与问题审计 V0

## 1. 状态

状态：进行中。

本报告已完成代码、资源、包体、自动化测试和微信开发者工具 iPhone 12/13 模拟器的首页复核。2026-07-11 用户已在微信开发者工具中实际点击“信任并运行”；复核时工具没有再显示该确认弹窗，且小游戏已直接渲染到首页，说明本地运行权限已生效。

当前受控 Windows 自动化可看到模拟器画面、移动鼠标并向开发者工具发送点击，但开发者工具没有把这些合成点击转发到小游戏画布：首页“开始背”“双人 PK”及调试面板均保持原状态。为避免把自动化限制误报为玩家端故障，本报告不将其认定为业务 P0，也不把未实际进入的微信页面写成已验收。

为继续冻结可交互视觉基线，本轮额外构建了独立的 `web-mobile` 调试预览并在浏览器中完成了 Boot、首页、词库、背词、合作选择、两种房间、历史、玩法说明和反馈页的实际点击巡检。该预览使用 `MemoryRuntimePort`，不连接微信云函数；它可证明 Cocos 页面渲染、布局和前端交互，但不能替代双人房间、PK、合作对局、拼词和结算的微信真机验收。

本阶段未修改业务代码、词库、计分、房间协议或云函数。

## 2. 基线身份

| 项目 | 值 |
| --- | --- |
| 分支 | `develop/cocos-migration` |
| 审计起点提交 | `01b96f9` (`docs: add cocos visual gameplay upgrade design`) |
| Cocos Creator | 3.8.8 |
| 目标方向 | 微信小游戏、竖屏 |
| 逻辑分辨率 | 640 × 960 |
| 当前 AppID | `wx063a1823d29bed9e` |
| 审计日期 | 2026-07-11（Asia/Shanghai） |

## 3. 验证命令与结果

| 命令 | 结果 | 证据 |
| --- | --- | --- |
| `npm run verify` | 通过 | 结构、平台、生命周期、生产接口、房间、PK、合作、拼词、主题、运行时、发布检查和 TypeScript 类型检查均通过。 |
| `npm run build:wechat:dry-run` | 通过 | Cocos 3.8.8 构建合约、场景和微信配置有效。 |
| `npm run build:wechat` | 通过 | 2026-07-11 重新生成 `build/wechatgame`；随后包体检查通过。 |
| `npm run inspect:wechat-build` | 通过 | 微信小游戏产物、横竖屏、必需 Bundle 和包体限制通过。 |
| 微信开发者工具项目加载 | 已加载 | 项目 `word-fishing-wechatgame` 可打开，模拟器为 iPhone 12/13 竖屏；首页在本轮复核中正常渲染。 |
| Web 视觉调试构建 | 产物已生成 | `tools/web-visual-build-config.json` 生成 `build/web-visual`（106 文件、13,224,452 bytes）；构建日志记录任务完成。父进程在引擎子进程 SIGTERM 后返回非零，因此该产物仅用于本地审计，不能作为发布构建。 |
| Web 交互巡检 | 已完成部分页面 | `http://127.0.0.1:4173/` 使用 Cocos `web-mobile` 调试产物，已采集页面 PNG 和 Cocos profiler 实测值。 |

自动化测试中的云调用失败、弱网和资源加载失败日志为明确的测试分支，不是本次本地构建失败。

## 4. 包体基线

构建报告：`cocos-client/build/wechatgame-report.json`。

| 包 | 字节 | MiB | 限制/状态 |
| --- | ---: | ---: | --- |
| 主包 | 4,120,338 | 3.93 | 4,194,304 上限，剩余 73,966 bytes（约 72 KB） |
| 所有分包合计 | 2,344,457 | 2.24 | 31,457,280 上限 |
| 全部构建产物 | 6,464,795 | 6.17 | 通过检查 |
| `subpackages/internal` | 689,716 | 0.66 | 已声明 |
| `subpackages/main` | 1,588,378 | 1.51 | 已声明 |
| `subpackages/mode_pk` | 40,748 | 0.04 | 已声明 |
| `subpackages/mode_spell` | 25,615 | 0.02 | 已声明 |

结论：当前主包占用约 98.24%。视觉资源不能直接加入主包；V1 前必须先建立主包安全余量与资源归属门禁。

## 5. 资源归属清单

### 5.1 源资源大小

| 归属 | 文件数 | 字节 | 当前内容 |
| --- | ---: | ---: | --- |
| `scripts/data` | 2 | 920,771 | 词库和拼词模板数据 |
| 核心脚本 | 70 | 288,998 | Store、Service、页面、运行时 UI |
| `theme_default` | 2 | 110,929 | `theme.json` 与一张背景图 |
| `theme_island` | 2 | 100,302 | `theme.json` 与一张背景图 |
| `mode_pk` | 6 | 21,357 | PK、合作捕词脚本，无图片/音频 |
| `mode_spell` | 3 | 13,101 | 拼词脚本，无图片/音频 |
| 场景 | 3 | 32,197 | Boot、Home 和说明 |

### 5.2 当前图片与音频覆盖

| 类型 | 现状 | 结论 |
| --- | --- | --- |
| 背景图 | 2 张 JPG：草地 109,849 bytes、海岛 99,227 bytes | 仅有背景层，缺少竖屏专属场景分层。 |
| 角色 | 0 | 缺少捕词员和萤火虫引导伙伴。 |
| 虫子 Sprite/图集 | 0 | 当前虫子以 `Graphics` 形状绘制。 |
| UI 图集/图标 | 0 | 当前按钮、徽章和图标以程序绘制或文字呈现。 |
| 特效序列帧/粒子贴图 | 0 | 当前只有文本上浮反馈。 |
| 音频资源 | 0 | `AudioService.ts` 为占位服务，暂无可播放资源。 |

### 5.3 最大源文件

| 文件 | 字节 | 风险 |
| --- | ---: | --- |
| `assets/scripts/data/WordBankData.generated.ts` | 885,397 | 最大源文件；Cocos 构建出现 Babel 超过 500 KB 的优化提示。 |
| `theme_default/textures/gameplay-bg.jpg` | 109,849 | 当前默认主题唯一位图。 |
| `theme_island/textures/gameplay-bg.jpg` | 99,227 | 当前海岛主题唯一位图。 |
| `assets/scripts/data/SpellTemplateData.generated.ts` | 35,374 | 拼词模板数据。 |

## 6. 页面截图清单

### 6.1 已采集的微信模拟器画面

| 编号 | 场景 | 观察结果 | 状态 |
| --- | --- | --- | --- |
| S01 | 微信开发者工具 iPhone 12/13 模拟器，PK 画面 | 首轮观察显示中文提示、双方分数、6 个英文目标、底部状态；界面持续显示“正在加载界面…”，倒计时为 0s。 | 已采集；本轮首页复核未复现，保留为待人工复测问题。 |
| S02 | 重新运行确认窗口 | 曾显示“您信任此项目的作者吗？”并提供“稍后运行/信任并运行”。用户随后已实际完成“信任并运行”。 | 已采集；本轮复核时该弹窗未再出现。 |
| S04 | 首页 | iPhone 12/13、100% 缩放下正常显示游戏标题、词库信息、时长选项、开始背/双人 PK/双人合作/换词库、战绩记录、玩法说明、问题反馈和隐私入口。 | 已采集；入口文字和时长按钮在模拟器缩放下偏小，列为 V2 布局优化观察项。 |

说明：微信开发者工具窗口的截图仅作为审计观察证据，不通过绕过工具确认或手工导出内部画布的方式落盘。由于合成点击未被转发，后续页面使用以下 Web 视觉预览补充；微信/真机专属状态仍单独保留。

### 6.2 已落盘的 Web 视觉截图

所有图片位于被 Git 忽略的本地目录 `cocos-client/build/visual-baseline/`。Web 调试构建开启 Cocos profiler，底部半透明性能浮层是审计工具，不是发布版 UI。

| 编号 | 截图文件 | 尺寸 | 已验证状态 | 观察 |
| --- | --- | --- | --- | --- |
| S03 | `S03-boot-privacy-393x852.png`、`S03-boot-privacy-430x932.png` | 393 × 852、430 × 932 | 已采集 | 隐私提示、查看指引、同意、暂不进入均完整可见。 |
| S04 | `S04-home-360x800.png`、`S04-home-393x852.png`、`S04-home-430x932.png` | 360 × 800、393 × 852、430 × 932 | 已采集 | 三种竖屏中入口、时长、金币、反馈和隐私入口完整可见。 |
| S05 | `S05-bank-393x852.png`、`S05-bank-page2-393x852.png` | 393 × 852 | 已采集 | 词库锁定、选择、分页（1/6 到 2/6）和确认按钮可见。 |
| S06 | `S06-study-393x852.png`、`S06-study-hidden-meaning-393x852.png` | 393 × 852 | 已采集 | 显示中文和隐藏中文状态均已实际点击验证。 |
| S07 | `S07-room-pk-430x932.png`、`S07-room-spell-393x852.png` | 430 × 932、393 × 852 | 已采集 | PK 与拼词房间均可进入；输入框出现 Cocos 渲染冲突，见 P1。 |
| 补充 | `S09-coop-select-393x852.png` | 393 × 852 | 已采集 | 合作玩法选择卡可进入，两个“进入房间”入口可点击。 |
| 补充 | `S11-help-393x852.png` | 393 × 852 | 已采集 | 各玩法规则文字完整显示。 |
| S12 | `S12-history-393x852.png` | 393 × 852 | 部分采集 | 三种历史分类和空状态可见；尚无真实拼词详情记录。 |
| S13 | `S13-feedback-393x852.png`、`S13-feedback-validation-393x852.png` | 393 × 852 | 已采集 | 空反馈被本地拦截为“反馈内容太短，请多写一点”；输入框视觉异常见 P1。 |

### 6.3 仍需微信/真机采集的画面

| 编号 | 页面/状态 | 原因 |
| --- | --- | --- |
| S08 | 双人 PK 实际对局 | Web 预览使用内存运行时，不能创建云房间；微信模拟器自动化也无法点击画布。 |
| S09 | 默契捕词赛实际对局 | 需要两名玩家、真实房间快照和团队分数。 |
| S10 | 同舟拼词记实际对局 | 需要两名玩家、真实 QWERTY 输入、提交/超时同步。 |
| S11 | 结算 | 需要完成真实 PK 或合作对局。 |
| S12 | 拼词历史详情 | 需要至少一局真实拼词结算记录。 |

### 6.4 待补齐截图矩阵

| 编号 | 页面/状态 | 必须观察 |
| --- | --- | --- |
| S03 | Boot 隐私授权 | 文本、三个操作、刘海和底部安全区。 |
| S04 | 首页 | Logo、入口、词库、时长、金币、最佳成绩。 |
| S05 | 词库选择 | 长词库名称、锁定、分页和确认返回。 |
| S06 | 背单词 | 隐藏中文、查看本词释义、下一词和底部拇指区。 |
| S07 | 双人房间 | 房间码、玩家席位、准备、邀请和开始状态。 |
| S08 | 双人 PK | 6 个目标、中文提示、倒计时、分数、道具和重叠情况。 |
| S09 | 默契捕词赛 | 团队分数、自己/队友状态和等待状态。 |
| S10 | 同舟拼词记 | 单词空格、自己/队友区域、QWERTY 键盘、提交和跳过。 |
| S11 | 结算 | 胜负/团队结果、分数和跳转。 |
| S12 | 历史记录与拼词详情 | 三个分类、最佳成绩、详情分页和长文本。 |
| S13 | 问题反馈 | 输入框、隐私入口和内容安全错误。 |

单机可达页面已经在上述三种竖屏尺寸中至少采集一次；多人页面的双设备截图仍作为 V5、V6 和 V9 的真机验收证据，也是不让 V0 误把单机内存运行时当成真实对局的必要边界。

## 7. 性能基线

### 7.1 已有性能能力

- `PerformanceService` 已记录帧耗时、慢帧、P95、最大帧耗时、节点数量和按路由聚合。
- `HomePlaceholder` 每帧记录当前路由耗时，每 60 帧记录一次节点数。
- 开发环境首页已有“性能报告”复制入口。
- `npm run test:performance` 通过，证明采样窗口、序列化和重置逻辑正确。

### 7.2 已采集的 Cocos Web 调试运行数据

下表来自同一台电脑、Cocos 3.8.8、`web-mobile` 调试产物的 profiler 截图，是实际渲染瞬时值，不是 P95 或长时间压测结果。

| 场景 | 尺寸 | FPS | 帧耗时 | Draw Call | 三角形 | 纹理内存 | Buffer 内存 |
| --- | --- | ---: | ---: | ---: | ---: | ---: | ---: |
| Boot 隐私页 | 393 × 852 | 58 | 1.73 ms | 13 | 500 | 5.39 MiB | 9.06 MiB |
| 首页 | 393 × 852 | 60 | 1.20 ms | 34 | 876 | 5.14 MiB | 36.07 MiB |
| 背单词 | 393 × 852 | 58 | 1.62 ms | 27 | 654 | 4.99 MiB | 29.32 MiB |
| 词库 | 393 × 852 | 60 | 2.76 ms | 33 | 853 | 5.16 MiB | 未单独记录 |
| 拼词房间 | 393 × 852 | 56 | 1.37 ms | 35 | 704 | 5.23 MiB | 33.82 MiB |

运行时壳的一次性基线测试还导出以下结构节点数：首页首次采样峰值 `49`（60 帧，P95/平均/最大均为 `16.67 ms`，无慢帧）；各路由根节点为 bank `30`、coopSelect `17`、room `32`、pkGame `27`、coopShared `24`、coopSpell `74`、result `8`、history `48`、feedback `17`、help `5`、home `38`。这些数值证明运行时树的当前规模，但来自 `MemoryRuntimePort` 测试环境，不能替代真机峰值。

### 7.3 当前缺失的真实数据

以下指标尚未从真实 Cocos/微信运行时采集，不能用模拟测试代替：

- 首页、背单词、PK、合作和拼词的平均/P95/最大帧耗时。
- 各页面节点峰值。
- 微信开发者工具和真机 Draw Call。
- 连续 3 分钟 PK、拼词快速输入和房间轮询的内存变化。

补充观察：当前微信模拟器运行的是非 DEV 产物，首页未显示“性能报告”调试入口；而且合成点击未进入游戏画布，因此本轮不能导出微信运行时 JSON。`npm run test:performance`、运行时壳的 P95 基线和 Web profiler 共同证明采集机制及页面渲染可用，但不能替代上述真实多人场景数据。

### 7.4 V0 采集目标

| 场景 | 操作 | 最低记录 |
| --- | --- | --- |
| 首页 | 空闲 30 秒 | 性能 JSON、节点峰值、Draw Call。 |
| 背单词 | 连续切换 100 个单词 | 性能 JSON、是否有节点持续增长。 |
| PK | 连续快速点击 30 秒 | 性能 JSON、目标重叠截图、Draw Call。 |
| 合作拼词 | 快速输入/删除/提交 30 秒 | 性能 JSON、键盘响应和节点峰值。 |
| 房间 | 轮询 3 分钟、切后台再返回 | 请求日志、性能 JSON、状态一致性。 |

## 8. 已知问题清单

| 优先级 | 问题 | 证据与根因 | V 阶段 |
| --- | --- | --- | --- |
| P1 | PK 首轮观察中出现“正在加载界面…”遮罩 | S01 实测；在用户授权后的首页复核未复现。需要人工从首页进入 PK 并导出调试控制台，确认是否为旧构建缓存、Bundle 加载失败或运行时状态残留。 | V0 复测并定位，修复归入相关模块。 |
| P0 | 主包仅剩约 72 KB 余量 | 构建报告主包 4,120,338 / 4,194,304 bytes。 | V1 前。 |
| P1 | PK 虫子可能相撞/重叠 | `PkWordTarget` 独立在共享横向边界内往返，没有车道、区段或最小间距调度。 | V4。 |
| P1 | 视觉资源覆盖不足 | 当前只有两张背景图；无角色、虫子图集、UI 图集、特效贴图和音频。 | V1–V8。 |
| P1 | 单词数据体积过大 | `WordBankData.generated.ts` 885,397 bytes，构建出现 Babel 大文件提示。 | V1 包体治理。 |
| P1 | 运行时输入框渲染冲突 | 浏览器实际进入两种房间与反馈页时重复报告 `Can't add renderable component to this node because it already have one`；`RuntimeUi.edit()` 先通过 `panel()` 添加 `Graphics`，随后又在同一节点添加 `EditBox`。房间和反馈截图出现输入框后方残留的大字号占位文字。 | V1 公共 UI 基础设施。 |
| P1 | 真实性能基线仍不完整 | 已有 Web 实际 FPS/帧耗时/Draw Call 与运行时壳 P95/节点基线；仍无微信真机 PK、合作、拼词、3 分钟轮询和真实内存数据。 | V0、V8。 |
| P1 | 当前开发者工具自动化无法驱动小游戏画布 | 运行时首页可见，但多次点击首页及开发者工具调试入口均不改变状态。该现象只在受控自动化环境中确认，尚不能视为玩家端缺陷。 | V0 真机/人工复测。 |
| P2 | Web 调试截图有 profiler 覆盖层 | Cocos debug 构建底部显示 FPS/Draw Call 统计，妨碍截图底部内容；微信发布构建没有该层。 | 审计工具限制，不进入发布问题表。 |
| P2 | UI 表现偏工具化 | 主要由运行时 `Graphics`、文字和统一矩形按钮构建，缺少角色、模式插画、图标和状态动画。 | V1–V7。 |
| P2 | 音频能力未落地 | 只有占位 AudioService，无音频资源和音乐/音效分离控制。 | V8。 |

## 9. 视觉验收检查表

V0 结束前必须逐项确认：

- [x] 当前分支、提交、Cocos 版本、方向和 AppID 已记录。
- [x] 自动化测试、构建干跑和包体报告已记录。
- [x] 资源归属、图片/音频覆盖和最大源文件已记录。
- [x] PK 虫子重叠的代码根因已记录。
- [x] 至少一张真实微信开发者工具模拟器画面已审计。
- [x] 已在授权后复核首页的当前运行画面。
- [x] Boot 至首页的全新运行截图已采集（Web 调试预览，360/393/430 竖屏）。
- [x] 单机可达的主要页面截图矩阵已采集并落盘。
- [x] Web 实际帧耗时、Draw Call、运行时壳 P95 和节点基线已记录。
- [ ] 微信真机 PK、默契捕词、同舟拼词、结算和拼词详情截图已采集。
- [ ] PK 加载遮罩问题已在当前微信构建中复现并用调试控制台定位。

## 10. V0 完成门槛

在以下待办完成前，V0 保持“进行中”，不能进入 V1：

1. 在可响应的微信开发者工具或真机创建真实双人房间，补齐 S08–S11、S12 拼词详情和两台设备截图。
2. 导出真实首页、PK、拼词和房间的性能 JSON，并记录真机 Draw Call、3 分钟内存变化和房间轮询日志。
3. 对 S01 的“正在加载界面…”执行一次从首页进入 PK 的人工复测，导出调试控制台，给出可复现步骤、日志和根因。
4. 在 V1 修复 `RuntimeUi.edit()` 的可渲染组件冲突前，不以当前房间/反馈输入框截图作为界面质量通过证据。
5. 完成上述微信真机证据后，再将 V0 标记完成并进入 V1。
