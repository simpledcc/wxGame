# Cocos 第一版可用版本并行开发总计划书

日期：2026-07-13

## 1. 文档用途

本文档是《词斗乐园单词比拼》Cocos 第一版可用版本的总计划。它用于多人、多电脑和多个 Codex 任务并行开发，并保证“游戏准备前界面”和“游戏内容”能够在明确契约下独立推进、最终对接。

Codex 的唯一启动入口是 `AGENTS.md`。新电脑或新任务只需先阅读该文件；它负责安全同步、当前目标解析和后续文档路由。本总计划不承担逐日进度记录。

当前首页专项执行文件：

- `COCOS_PRE_GAME_FOUNDATION_TARGET_TASK.md`

相关长期约束：

- `COCOS_ARCHITECTURE_REVIEW.md`
- `COCOS_MIGRATION_DESIGN.md`
- `COCOS_VISUAL_GAMEPLAY_UPGRADE_DESIGN.md`
- `COCOS_RUNTIME_UI_SHELL.md`
- `COCOS_WECHAT_BUILD_PIPELINE.md`
- `CODEX_HANDOFF.md`

## 2. 当前工程事实

- 主开发分支为 `develop/cocos-migration`。
- 唯一客户端位于 `cocos-client/`；旧版 `miniprogram/` 已删除，`cloudfunctions/` 作为生产后端继续保留。
- Cocos Creator 目标版本为 3.8.8。
- 当前逻辑设计分辨率为 `640x960`，微信构建方向为竖屏。
- `Home.scene` 是持久运行壳；轻量 `RuntimeScreenFactory` 按 route 分发到 Home、学习、房间和支持页 Builder。
- 房间、PK、默契捕词、同舟拼词、词库、背词、战绩、反馈和隐私已有代码实现。
- `mode_pk` 和 `mode_spell` 已具有独立 Asset Bundle/微信分包边界。
- 用户已确认当前代码能够在手机上启动运行。本地没有 Creator 或微信开发者工具时，不重复把“手机能否启动”作为每个代码 Goal 的阻塞项。

## 3. 第一版产品目标

第一版可用版本必须形成一条完整体验：

```text
Boot/隐私
  -> 童趣竖屏首页
  -> 选择词库或赛前练习
  -> 创建/加入房间
  -> 选择并完成现有玩法
  -> 结算与战绩
  -> 返回首页
```

产品定位：

> 双人单词游戏为主，赛前学习和词库准备为辅。

第一版不是只换一张首页图片，也不是重写现有业务。它要完成：

1. 准备前界面的统一美术和交互。
2. 现有三种玩法的内容表现升级。
3. 两个模块通过 route、Store、主题 Token 和 Bundle 契约对接。
4. 形成可持续迭代、可回滚、可交接的第一版代码。

## 4. 三条并行工作流

### 4.1 A 线：游戏准备前界面

负责范围：

- Boot 和隐私入口的品牌一致性。
- 首页、当前词库、赛前练习、玩法入口、战绩和辅助入口。
- 创建/加入房间之前的视觉层级和导航意图。
- 通用按钮、图标槽、卡片、顶部信息栏、Toast、Loading、Modal。
- 竖屏安全区、动态文字和触控尺寸。
- 首页背景、Logo 临时方案、角色/图标资源规范。

不负责：

- 云函数协议。
- 房间同步和计分。
- 玩法内部状态机。
- PK/合作/拼词的服务端校正。

### 4.2 B 线：游戏内容

负责范围：

- PK、默契捕词和同舟拼词的玩法场景表现。
- 单词目标、运动调度、防重叠、反馈、计时和 HUD。
- 合作状态、拼词键盘、等待与切题表现。
- 三种模式的结算数据呈现适配。
- 玩法 Bundle 内资源、对象池、动画和性能。

不负责：

- 首页导航结构。
- 顶部玩家/金币/词库信息。
- 隐私和反馈入口。
- 修改全局 Store、Router 或主题契约，除非经过对接评审。

### 4.3 C 线：对接与第一版集成

负责范围：

- 冻结共享接口和 route 契约。
- 处理 A/B 两线都需要修改的共享文件。
- 合并、冲突解决、回归测试和版本标签。
- 从首页进入房间、玩法、结算再返回首页的完整链路。
- 资源归属、主包/分包和发布配置检查。

## 5. 模块所有权

| 区域 | 默认所有者 | 主要路径 |
| --- | --- | --- |
| 首页和准备前 UI | A 线 | `RuntimeScreenFactory.ts` 的非玩法部分、`HomeScene.ts`、通用 UI 组件 |
| 主题 Token 和通用视觉 | A 线，C 线评审 | `themes/**`、`components/ui/**` |
| PK/默契捕词 | B 线 | `assets/bundles/mode_pk/**` |
| 同舟拼词 | B 线 | `assets/bundles/mode_spell/**` |
| Store、Router、App | C 线 | `store/**`、`core/App.ts`、`core/SceneRouter.ts` |
| 房间协议和云服务 | 冻结，C 线审批 | `services/**`、`domain/Room*`、`cloudfunctions/**` |
| 构建和包体 | C 线 | `tools/*build*`、Bundle meta、构建配置 |
| 汇总文档和状态表 | C 线单一维护者 | 总计划、交接、进度矩阵 |

同一时刻一个共享文件只能有一个明确所有者。Cocos `.scene`、Prefab、Bundle 根 meta 和生成数据不得由两个分支同时改动。

## 6. 对接契约

### 6.1 Route 契约

第一版保留现有 route 名称：

| 用户入口 | 目标 route | 说明 |
| --- | --- | --- |
| 首页 | `home` | 持久运行壳 |
| 赛前练习 | `study` | 不改内部业务名 |
| 选择词库 | `bank` | 保留来源页返回契约 |
| 创建/加入房间 | `room` | 首页只导航，不直接调用云函数 |
| 合作模式选择 | `coopSelect` | 保留现有模式选择 |
| 玩法目录 | `help` 或经评审新增的目录 route | 不误入具体玩法 |
| 战绩记录 | `history` | 使用真实记录 |
| 问题反馈 | `feedback` | 保留隐私和内容安全 |
| PK | `pkGame` | 由 `mode_pk` 注册 |
| 默契捕词 | `coopShared` | 由 `mode_pk` 注册 |
| 同舟拼词 | `coopSpell` | 由 `mode_spell` 注册 |
| 结算 | `result` | 使用现有结算 Store |

任何 route 增删必须由 C 线评审，并同时更新 Router、Store、静态测试和运行时测试。

### 6.2 状态和服务契约

- View 不直接调用 `wx.*`、数据库或云函数。
- 当前词库、金币、玩家显示名、时长和历史记录继续来自现有 Store/Service。
- 不恢复自定义昵称 UGC，不伪造等级、金币、奖励或战绩。
- A 线只表达“创建/加入意图”；真正的房间动作仍由 Room 控制器执行。
- B 线只消费标准化房间快照和玩法 Store，不复制房间状态。
- 本地即时反馈和云端权威校正的先后关系保持不变。

### 6.3 视觉契约

- A 线先交付 Theme Token、按钮状态、卡片、文字和图标槽规范。
- B 线使用语义 Token，不直接依赖首页颜色常量。
- 动态文字不烘焙进背景图。
- 所有按钮的可见区域、`UITransform` 和触控区域必须一致。
- 所有动画具有销毁、回收和后台暂停路径。

### 6.4 资源和 Bundle 契约

| 资源 | 归属 |
| --- | --- |
| Boot、首页、通用 UI | 主包或轻量主题 Bundle |
| PK/默契捕词专用资源 | `mode_pk` |
| 拼词专用资源 | `mode_spell` |
| 仅设计参考的整页图片 | `docs/`，禁止进入运行包 |
| 大背景 | 对应主题 Bundle，压缩 JPG/PNG |
| 图标和小组件 | 按页面复用关系进入图集或通用资源目录 |

移动 Cocos 资源必须携带 `.meta`。不得手工重建其他负责人资产的 UUID。

## 7. 总阶段计划

### M0：基线和共享契约

目标：让并行开发基于同一个提交和同一套接口。

交付：

- 基线提交 SHA。
- A/B/C 文件所有权表。
- route、Store、视觉 Token 和 Bundle 契约。
- 当前已知测试异常和风险表。

通过标准：各分支可以独立开发，不需要复制业务状态或修改对方模块。

### M1：两条样板并行开发

A 线：

- 完成首页视觉基础设施和高保真首页样板。
- 完成创建/加入、赛前练习、词库、战绩和辅助入口。

B 线：

- 选择 PK 作为玩法样板。
- 完成目标表现、HUD、反馈和基础运动治理。

通过标准：首页样板和 PK 样板分别通过本模块代码测试，并遵守共享契约。

### M2：模块功能完成

A 线：

- 补齐 Boot、合作选择、房间准备、词库/学习和辅助页面的共享视觉基础。
- 不要求一次重做全部子页面高保真，但必须可进入、可返回、无布局阻断。

B 线：

- 完成 PK、默契捕词、同舟拼词和结算表现。
- 完成对象池、动画生命周期和性能测试。

通过标准：两条线都可以从测试入口独立运行，业务协议无变化。

### M3：第一次对接

任务：

1. 从 A 线首页进入现有房间和 B 线玩法。
2. 完成玩法后进入 Result，再返回新版首页。
3. 对齐 Loading、Toast、错误、主题和按钮状态。
4. 检查 route 销毁、房间释放、轮询和 Bundle 重试。
5. 解决共享文件冲突，不在模块分支互相复制代码。

通过标准：Home -> Room -> Game -> Result -> Home 完整链路在运行时测试中通过。

### M4：第一版可用候选

任务：

- 全量代码测试和类型检查。
- 主包/分包、资源重复和源文件预算检查。
- 长文字、窄屏、连续点击和重复进出测试。
- 修复 P0/P1 阻断问题。
- 更新交接、版本说明和已知限制。

通过标准：代码层没有阻断问题，所有现有业务入口保留，第一版可被外部环境构建验证。

### M5：外部集成与发布验收

该阶段由具备 Creator、微信开发者工具和真机环境的负责人执行：

- Creator 导入和微信构建。
- 微信开发者工具与双设备流程。
- 性能、截图、包体、隐私和开发版上传。

M5 不属于每个本地代码 Goal 的重复验收。只有准备发布候选时统一执行。

## 8. Git 和多人协作规则

建议分支：

```text
feature/pre-game-ui-<owner>
feature/gameplay-content-<owner>
integration/first-playable
```

规则：

1. 两台电脑和两个 Codex 任务不得同时写同一个个人分支。
2. 每个分支只修改其所有权范围。
3. 共享文件通过小型 PR 进入集成分支。
4. 个人分支允许推送 WIP，集成分支禁止 WIP 和强制推送。
5. 每次接续先读取目标文件、进度文件、`git log` 和 `git diff`。
6. 不使用本机 stash 作为跨电脑交接手段；阶段结果必须 commit 并 push。
7. 每个提交写明模块和目标，例如 `feat(home): build pre-game action hierarchy`。

每个任务交接至少记录：

```text
Branch:
Base commit:
Latest commit:
Owner/module:
Done:
Next:
Modified shared files:
Tests:
Known blockers:
```

## 9. 验证策略

### 9.1 每个代码 Goal 必须执行

- 相关阶段测试。
- Runtime shell 静态与执行测试。
- TypeScript 类型检查。
- 发布静态检查。
- 与修改范围对应的包体/资源契约测试。

### 9.2 本地无引擎时不重复执行

用户已确认当前基线可以在手机运行。仅修改代码和文档、且没有外部工具时，以下内容不是当前 Goal 的阻塞项：

- 重新安装或启动 Cocos Creator。
- 重新证明 Boot -> Home 能在手机打开。
- 重新生成真机截图、预览二维码或开发版上传。
- 部署云函数或修改微信后台。

代码不得破坏相关配置；最终 M5 仍会统一复验。

## 10. 第一版完成定义

- 首页明确突出创建房间和加入房间。
- 赛前练习、词库、玩法说明、战绩、隐私和反馈可达。
- 所有显示数据来自现有 Store，不使用假金币、假等级或假记录。
- 三种现有玩法仍可完成并进入结算。
- 首页与玩法使用统一 Token 和交互反馈。
- A/B 两线通过冻结接口对接，没有重复状态源。
- 玩法资源维持 Bundle/分包边界。
- 代码层测试、类型和发布静态检查通过。
- 外部发布验收由 M5 单独记录，不与本地代码完成度混淆。

## 11. 当前执行入口

当前先执行 A 线首页目标：

```text
请按照 COCOS_PRE_GAME_FOUNDATION_TARGET_TASK.md 开始或继续当前 Goal。
只实现下一个未完成阶段，更新进度文件，完成代码验证后提交个人分支。
不要修改玩法协议、云函数或其他开发者负责的 Bundle。
```
