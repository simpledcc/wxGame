# Cocos 真实手机运行目标任务说明书

## 1. 目标

本任务的最终目标是：让《词斗乐园单词比拼》的 Cocos 版本在真实微信小游戏环境中稳定运行，并完成以下主要能力的手机验收：

1. 启动、隐私授权、首页和基础页面正常。
2. 开始背、词库选择、单词学习和历史记录正常。
3. 双人 PK 可以创建房间、邀请玩家、准备、开始、对战和结算。
4. 双人合作可以创建房间、两名玩家准备后开始、完成合作和结算。
5. 双人小船拼词可以两名玩家进入同一房间、实时看到房间状态、填写单词、提交、切换单词和结算。
6. 网络抖动、切后台、返回前台、重复点击、超时和离开房间不会导致卡死或错误跳转。
7. Cocos 产物可以被微信开发者工具导入、预览，并上传开发版本。

本说明书的执行顺序是“先启动，再单人，再房间，再多人玩法，最后性能和发布”。在真实手机运行前，不切换正式上传目录，不删除旧版 `miniprogram/`，不改写已经稳定的云函数接口。

## 2. 当前基线

基线分支：`develop/cocos-migration`

当前稳定提交：`99c881c`

本机工具：

- Cocos Creator 3.8.8
- 微信开发者工具
- Node.js 与项目本地 TypeScript 测试工具

当前已经确认：

- `npm run verify` 全部通过。
- 房间、PK、双人合作、双人拼词、主题切换、历史记录、隐私流程和路由生命周期已经通过代码级测试。
- Cocos Creator 可以启动并生成微信小游戏构建产物。

当前阻塞：

- 主包约 `6,425,670` 字节，超过微信小游戏 `4MB` 限制。
- 生成的 `game.json` 没有 `subpackages`。
- `mode_pk` 和 `mode_spell` 没有被输出为微信分包。
- 微信开发者工具预览失败，错误为 `80051 source size exceed max limit 4MB`。

因此，当前不能把“自动化测试通过”当作“真实手机可以运行”。真实手机验收必须从阶段 1 的可导入、可预览构建开始。

## 3. 总体阶段

| 阶段 | 名称 | 目标 | 通过标准 |
| --- | --- | --- | --- |
| 0 | 基线冻结 | 保留旧版并固定工具、分支、测试入口 | 旧版仍可上传，工作树和分支来源清晰 |
| 1 | 最小可运行包 | 先让微信开发者工具和手机打开 Cocos 版本 | Boot -> 隐私 -> Home，无黑屏、无脚本错误 |
| 2 | 单人核心 | 先打通开始背、词库、学习、历史 | 单人主要流程可完整完成 |
| 3 | 房间基础 | 打通创建、加入、邀请、准备和轮询 | 两台手机进入同一房间，状态一致 |
| 4 | 双人 PK | 打通 PK 操作、同步、结算和历史 | 两台手机完成一局 PK |
| 5 | 双人合作 | 打通合作模式、同步、结算和历史 | 两台手机完成一局合作 |
| 6 | 双人小船拼词 | 打通四空拼词、提交标记、切词和记录 | 两台手机完成多题拼词 |
| 7 | 稳定性与性能 | 验证弱网、后台恢复、重复点击和卡顿 | 目标设备没有阻断性问题 |
| 8 | 发布验收 | 截图、隐私、版本、开发上传 | 产物通过开发版上传和内部验收 |

每次只执行一个阶段。阶段未通过时，不进入下一阶段，也不把失败原因包装成“已完成”。

## 4. 阶段 0：基线冻结

### 任务

1. 确认当前分支是 `develop/cocos-migration`，记录最新提交。
2. 确认根目录 `project.config.json` 仍然指向旧版 `miniprogram/`。
3. 确认 `cloudfunctions/`、`miniprogram/` 没有被 Cocos 构建覆盖。
4. 运行：

```powershell
cd C:\work\wxgame_cocos\cocos-client
npm install
npm run verify
npm run build:wechat:dry-run
```

5. 为旧版保留可回滚标签或分支，例如 `release/v1.0-legacy-uploadable`。

### 验收

- `npm run verify` 通过。
- 旧版上传目录没有被修改。
- 当前 Cocos 构建输出只允许写入 `cocos-client/build/`。
- 记录 Cocos Creator、微信开发者工具、微信基础库版本。

## 5. 阶段 1：先让真实手机启动

这是最高优先级阶段。阶段目标不是一次完成所有玩法，而是先得到一个可以被手机打开的最小 Cocos 包。

### 任务 A：修复主包和分包

1. 在 Cocos Creator 中确认 `theme_default`、`theme_island`、`mode_pk`、`mode_spell` 都是有效 Asset Bundle。
2. 将 `mode_pk` 和 `mode_spell` 的微信小游戏压缩类型设置为 `Mini Game Subpackage`。
3. 确认构建后的 `game.json` 声明对应的 `subpackages`，并且生成文件位于声明的目录。
4. 重新构建并检查：

```powershell
npm run build:wechat
npm run inspect:wechat-build
```

5. 如果主包仍然超过 4MB，按顺序处理：

- 移除主包不需要的主题和玩法资源。
- 把仅在玩法页面使用的资源放进对应玩法 Bundle。
- 压缩或拆分过大的词库数据，保持 `bankId` 和拼词模板结果不变。
- 检查 Cocos 引擎模块裁剪和 separate engine 方案。
- 每次只改变一种打包策略，并保留构建报告。

不能在构建后手工改包来伪造分包。当前 Cocos Creator 3.8.8 命令行构建对本项目两个玩法 Bundle 未稳定输出目标目录，因此阶段 1 使用受脚本控制的发布归一化步骤：`tools/normalize-wechat-subpackages.js` 只在构建目录内移动 Creator 已生成的 `mode_pk`/`mode_spell` Bundle，同步 `game.json`、`settings.*.json` 和分包入口，并由 `inspect-wechat-build` 做路径、包体和入口校验。这个步骤必须作为 `npm run build:wechat` 的固定流水线执行，不能手动修改构建产物。

### 任务 B：微信开发者工具预览

1. 将 `cocos-client/build/wechatgame/` 作为小游戏项目导入。
2. 检查 AppID、横屏设置和构建目录。
3. 使用开发者工具生成预览二维码。
4. 用一台真实手机扫码打开。

### 阶段验收

- 手机可以打开 Boot 页面。
- 隐私协议弹窗可以显示，点击同意后进入 Home。
- 点击拒绝不会初始化云服务或进入需要个人信息的功能。
- Home 页面无黑屏、白屏、无限加载、脚本异常。
- 能从 Home 返回或退出，不出现重复场景和无法点击。
- 保留手机截图、开发者工具 Console 日志和构建报告。

如果阶段 1 未通过，后续玩法测试没有意义。

## 6. 阶段 2：单人核心功能

### 流程

1. Home -> 开始背。
2. 开始背 -> 换词库 -> 选择词库 -> 返回开始背。
3. 隐藏中文后切换单词，中文继续隐藏。
4. 点击“显示当前单词含义”只显示当前单词中文。
5. 点击“下一个”后，当前显示状态和单词索引正确刷新。
6. 完成单词学习后进入历史记录，记录和最佳成绩可查看。
7. 反馈页面可以填写、提交和返回；内容仍然经过安全检查。

### 手机验收

- 所有按钮都能通过手指点击，不依赖鼠标悬停。
- 长文本不溢出，不遮挡按钮。
- 横屏旋转、窄横屏和不同安全区域下布局不重叠。
- 快速连续点击不会重复扣金币、重复切页或重复发起请求。

## 7. 阶段 3：房间基础

使用两个真实微信账号和两台手机，禁止用同一账号模拟“两个玩家”作为最终证据。

### 流程

1. 玩家 A 创建房间。
2. 玩家 A 看见六位房间码并复制/分享。
3. 玩家 B 通过房间码或邀请加入。
4. 两边都能看到两名玩家和准备状态。
5. 两边分别点击准备。
6. 房主开始游戏，双方进入对应玩法。
7. 一方切后台再回来，房间状态能够恢复。
8. 一方退出房间，另一方看到清晰的状态变化。

### 验收

- 房间码不会为空、截断或被旧房间覆盖。
- 双方玩家不会重复显示。
- 双人合作和双人拼词不能被机器人替代开始。
- 开始按钮只在服务端允许的状态下可用。
- 轮询只存在一个实例，进入游戏或结算后停止。

## 8. 阶段 4：双人 PK

### 验收流程

1. 两人创建或加入 PK 房间。
2. 两人准备后开始。
3. 双方看到相同的单词、目标和倒计时。
4. 点击目标后立即出现本地反馈，云端返回后允许校正。
5. 快速点击、重复点击、网络延迟和超时不会造成分数重复结算。
6. 游戏结束进入结果页。
7. 结果可以进入历史记录，双方记录不互相覆盖。

### 重点日志

- `roomId`、`requestId`、场景、动作名称、耗时和错误分类。
- 不记录 openid、联系方式、反馈正文、答案明文或完整隐私信息。
- 发现卡顿时同时保留性能 JSON 和开发者工具 Console 日志。

## 9. 阶段 5：双人合作

### 验收流程

1. 两名真实玩家进入合作房间。
2. 两边选择相同词库和游戏参数。
3. 两人都准备后才允许开始。
4. 双方完成合作目标。
5. 双方看到一致的总分、剩余时间和结束状态。
6. 结束后进入结果页和历史记录。

### 重点检查

- 词库和游戏参数由房间服务端快照确定，不能以本地选择覆盖服务端。
- 双方时间显示使用同一个服务端时间基准或同一快照时间。
- 一方超时、离开、切后台时，另一方不会卡在等待状态。
- 双人合作不显示机器人入口。

## 10. 阶段 6：双人小船拼词

### 验收流程

1. 两名真实玩家进入拼词房间并准备。
2. 游戏开始前使用预生成的缺空模板，不在开始时临时随机生成。
3. 双方看到同一个单词和同一组四个空格。
4. 每个玩家填写自己负责的空格，并能看到对方已填写/未填写的状态。
5. 点击确认后显示“已提交”标记，不能重复提交或修改已确认内容。
6. 另一名玩家未提交时，时间到达后系统自动结算当前题并切换下一题。
7. 切换新单词时，旧单词的四个字母、确认状态和颜色全部清空。
8. 正确进入下一题，错误扣分；最终只展示总分，但历史记录保留每题的填写结果和出错玩家。

### 重点检查

- 双方只以服务端快照作为题目、时间、提交状态和结算结果的最终依据。
- 房间轮询或订阅收到新快照后，UI 必须按 `roundId`/题目索引重置本地草稿。
- 慢请求、旧请求和离开房间后的回调不能修改新题目。
- 不能把上一个单词的字母带入下一个单词。
- 两台手机的倒计时允许存在很小的渲染差异，但结算必须一致。

## 11. 阶段 7：稳定性和性能

### 必测场景

1. Home 空闲 30 秒。
2. PK 快速连续点击 30 秒。
3. 拼词快速输入、删除、提交 30 秒。
4. 房间轮询保持 3 分钟。
5. 切后台 10 秒后返回。
6. 弱网、断网、恢复网络。
7. 快速进入/退出多个页面。
8. 低配置 Android 手机和常用 iPhone 各测试一次。

### 性能证据

每台手机记录：

- 手机型号、系统版本、微信版本、基础库版本。
- 主题、玩法、网络类型。
- 平均帧耗时、最大帧耗时、50ms 以上慢帧数量、P95。
- 节点峰值、页面进入耗时、云请求耗时。
- 开发者工具 Console 日志和游戏内性能 JSON。

性能报告只用于开发诊断，发布包不能暴露开发性能按钮或敏感日志。

## 12. 阶段 8：发布验收

### 发布前检查

- Cocos 构建产物通过 `npm run inspect:wechat-build`。
- 主包小于 4MB，所有分包总和小于微信限制。
- `mode_pk`、`mode_spell` 均为已声明分包。
- 无 `.map`、`cloudfunctions`、旧版 `miniprogram`、`node_modules` 混入产物。
- 隐私协议、适龄提示、游戏名称、截图和当前版本一致。
- 不显示公开昵称、聊天、签名或其他未审核 UGC 入口。
- 保留旧版可上传包和当前 Cocos 开发包。

### 上传顺序

1. 先上传微信开发者工具开发版本。
2. 用测试账号和两台手机完成阶段 2 至阶段 7。
3. 保存版本号、构建报告、截图和日志。
4. 再提交审核，不直接替换旧版稳定上传目录。
5. 审核通过后再考虑切换正式上传根目录。

## 13. 每次交给 Codex 的执行指令模板

后续每次只指定一个阶段，使用下面的模板：

```text
请按照 COCOS_REAL_PHONE_TARGET_TASK.md 执行阶段 N：<阶段名称>。

要求：
1. 先读取 CODEX_HANDOFF.md、COCOS_MIGRATION_DESIGN.md、COCOS_MIGRATION_COMPLETION_MATRIX.md、COCOS_RELEASE_QA.md 和本说明书。
2. 检查当前分支、工作树、Cocos Creator 版本和微信开发者工具状态。
3. 只实现本阶段要求，不提前修改无关玩法。
4. 先运行现有测试，再修改代码；修改后运行本阶段测试和完整 npm run verify。
5. 如果需要真实手机或微信后台操作，明确列出我需要操作的步骤，不得假设已经完成。
6. 记录通过项、失败项、日志位置、构建产物位置和下一步建议。
7. 未通过时不要标记阶段完成，也不要切换正式上传目录。
```

推荐执行顺序：

```text
阶段 1：最小可运行包
阶段 2：单人核心功能
阶段 3：房间基础
阶段 4：双人 PK
阶段 5：双人合作
阶段 6：双人小船拼词
阶段 7：稳定性和性能
阶段 8：发布验收
```

## 14. 完成定义

只有同时满足以下条件，才算“主要功能可以运行”：

1. 一台真实手机能稳定启动并进入 Home。
2. 单人背词流程可以完成并保留历史。
3. 两台真实手机可以完成 PK、双人合作和双人小船拼词各一局。
4. 每种多人玩法至少完成一次正常结算、一次超时或断线恢复测试。
5. 拼词新题目不会继承旧字母、旧确认状态或旧计时。
6. 构建包通过微信大小、分包和文件内容检查。
7. 所有阻断性错误都有日志、复现步骤和修复记录。
8. 旧版上传版本仍可回滚。

在满足以上条件之前，只能称为“Cocos 开发验证版”，不能称为“真实手机稳定版”或“可正式发布版”。
