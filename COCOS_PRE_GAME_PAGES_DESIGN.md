# Cocos 统一赛前界面设计说明书

日期：2026-07-15

## 1. 目标

将首页之外的非玩法界面统一到《词斗乐园》竖屏首页的视觉语言，同时完整保留现有 Store、Scene、房间服务、云函数和玩法 Bundle 行为。

本轮覆盖：

- 创建入口后的玩法目录
- 玩法确认与创建配置
- 创建房间、加入房间和房间大厅
- 赛前练习
- 选择词库
- 玩法目录
- 战绩列表和拼词详情
- 问题反馈
- 比赛结算
- 首页设置弹层和微信隐私入口继续沿用现有同风格实现

不修改：

- PK、默契捕词、同舟拼词的游戏内画面
- 房间、计分、同步和云函数协议
- AppID、云环境、数据库和微信构建配置
- 真实词库、金币、玩家名和战绩数据来源

## 2. 统一视觉系统

### 2.1 页面骨架

所有准备页面使用 `640` 固定逻辑宽度与动态可视高度。`640x960` 仍是最小设计基线；手机长宽比高于基线时使用 Cocos `ResolutionPolicy.FIXED_WIDTH` 扩展内部画布高度，页面背景、场景层和安全区随真实可视高度铺满：

```text
RouteRuntimeScreen
├─ {Route}Scenery (full viewport)
└─ {Route}SafeArea (dynamic height)
   ├─ {Route}Header
   │  ├─ BackButton (80x80)
   │  ├─ PageTitle
   │  └─ PageSubtitle
   ├─ StatusCard / MainCard
   ├─ ContentControls
   └─ PrimaryActions
```

背景继续由 `HomePlaceholder` 和 `ThemeRouteRules` 以 cover 方式加载 `homeBackground`。页面叠加轻量程序化场景：天空、云层、远山、草地、学习小路、树叶和花朵；它不包含文字、不阻塞点击，也不替代后续正式位图。顶栏锚定安全区顶部，分页和主操作按页面需要锚定内容区或安全区底部。

### 2.2 颜色职责

- 橙色 `homeCreate`：创建、开始、返回首页等最重要命令。
- 蓝色 `homeJoin` / `homeBank`：加入、词库、提交等明确下一步。
- 绿色 `homePractice`：练习、准备、默契玩法。
- 紫色 `homeCatalog`：拼词玩法和规则信息。
- 黄色 `homeHistory`：战绩、错题和金币解锁。
- 浅色 `homeCard`：状态、输入、分页和次要操作。

颜色不替代文字和禁用状态；所有业务按钮仍使用真实 `Button.interactable`。

### 2.3 控件规范

- 可点击控件设计高度至少 `80`，在 360 宽设备约为 `45px`。
- 页头返回按钮固定 `80x80`，不使用文字胶囊。
- 主操作使用图标、标题和一句结果说明。
- 状态卡不伪装为按钮。
- 输入框的 `EditBox` 宿主不带 `Graphics`；背景放在独立子节点。
- 所有动态文字使用固定容器和 `Label.Overflow.SHRINK`。
- 禁用按钮由公共点击守卫阻止回调，不依赖平台是否过滤点击事件。

## 3. 页面设计

### 3.1 七张参考图与运行时页面映射

| 参考图 | 页面 | 运行时 route / 状态 | 当前行为 |
| --- | --- | --- | --- |
| 图一 | 玩法目录 | `coopSelect` | 仅“准备体验模式”开放，其他七种玩法禁用并显示“筹备中” |
| 图二 | 创建配置 | `room + roomEntryIntent=create` | 显示所选模式、当前词库、换词库、自动准备与创建按钮 |
| 图三 | 加入房间 | `room + roomEntryIntent=join` | 输入 6 位房间码并调用现有加入接口 |
| 图四 | 准备房间 | `room + roomId/room` | 房间码、复制、邀请、词库、玩家、准备、开始和离开 |
| 图五 | 选择词库 | `bank` | 真实词库大卡、分页、锁定、金币解锁和来源页返回 |
| 图六 | 战绩记录 | `history` | 全部/PK/合作/拼词筛选、最近、最佳、真实列表和拼词详情 |
| 图七 | 赛前练习 | `study` | 单词、释义、隐藏策略、错词、大号下一词与换词库 |

### 3.2 玩法目录、创建、加入与准备房间

主页“创建房间”不直接创建云端房间，固定流程为：

```text
Home 创建房间
  -> coopSelect 玩法目录
  -> room 创建配置
  -> createRoom 云函数
  -> room 准备房间
  -> 双方准备后 startGame
```

玩法目录保留八个明确位置，第一版仅开放“准备体验模式”。其他玩法使用真实禁用按钮，不能触发隐藏路由或创建房间。后续开放玩法时复用相同的配置和准备房间结构。

同一 `RoomRuntimeScreen` 根据 `roomEntryIntent` 和会话状态呈现三种标题：

| 状态 | 标题 | 主引导 |
| --- | --- | --- |
| 创建入口 | 创建房间 | 生成房间码并等待好友 |
| 加入入口 | 加入房间 | 输入好友的 6 位房间码 |
| 已接受/已进入会话 | 当前玩法名 | 房间码、玩家、准备和开始状态 |

创建配置布局顺序：所选玩法、当前词库、流程提示、创建按钮、自动准备开关。换词库后必须返回创建配置，并保留自动准备选择。

加入布局顺序：输入说明、6 位房间码输入、邀请提示和加入按钮。

准备房间布局顺序：房间码、复制/邀请、词库与玩法、玩家列表、状态、准备、开始和离开。页面不显示手动刷新按钮，房间状态继续由现有后台轮询同步。

已有会话时创建和加入按钮禁用，避免覆盖当前会话。当前准备体验流程要求两名真实玩家，不在页面提供机器人入口。

### 3.3 赛前练习

- 顶部显示真实当前词库，并可直接更换。
- 中央单词卡突出英文、中文释义和进度。
- 上一词、随机、查看当前释义、加入错题、更换词库、显示/隐藏后续中文均保留。
- “下一个”使用页面最大的拇指操作按钮；查看当前释义只影响当前词，整体中文开关影响后续词。
- 当前词库没有音标或发音资源，因此不伪造音标，也不展示无功能的发音按钮。
- 绿色用于学习动作，黄色用于错题，蓝色用于词库。

### 3.4 选择词库

- 顶部状态卡显示真实当前词库、解锁状态和金币。
- 每页 4 个大词库卡，显示真实名称、单词数、选中和锁定状态。
- 首页/末页禁用对应分页按钮。
- 底部区分“解锁所选”和“确定选择”，返回路径仍由 `bankReturnRoute` 决定。

### 3.5 旧合作入口兼容（当前不展示）

- H5 的创建入口只展示图一对应的八项玩法目录，不再把旧双卡选择页作为可见流程。
- H6 删除旧双卡页面遗留的默契捕词、同舟拼词和换词库控制器处理器；底层玩法 Bundle、房间模式和云协议仍保留兼容。
- 后续启用合作玩法时应从玩法目录重新接入图二创建配置、图四准备房间和统一词库入口，不增加机器人、道具或新合作规则。

### 3.6 玩法说明

- 使用单一高可读内容卡承载现有六类规则。
- 目录图标和标题强化页面定位，正文保持左对齐。
- 规则内容仍由 `HelpScene` 提供，UI 不复制第二份业务文案状态。

### 3.7 战绩与详情

- 顶部提供“全部、PK、合作、拼词、其他”五个位置；“其他”在没有对应业务类型前保持禁用。
- “最近记录”和“最佳成绩”均从真实 `HistoryStore` 计算，不使用示例数据。
- 列表显示结果、时间、词库和得分；仅拼词战绩可进入轮次详情。
- 列表和详情分别保留分页禁用状态。
- 详情使用同一页头返回逻辑，关闭详情不会离开战绩页。

### 3.8 问题反馈

- 先说明数据用途，再提供反馈正文和可选联系方式。
- 提交按钮保留校验、忙碌态、销毁保护和错误恢复。
- 隐私保护指引作为独立次操作，继续调用微信平台能力。

### 3.9 比赛结算

- 奖杯图标、结果、总分和玩家得分集中在主卡。
- “返回首页”为橙色主操作，“查看战绩”为黄色复盘操作。
- 两个操作继续执行现有房间释放和结果 Store 逻辑。

## 4. 代码边界

视觉基础位于：

- `cocos-client/assets/scripts/components/ui/PreGameUi.ts`

页面装配位于：

- `cocos-client/assets/scripts/components/ui/RuntimeScreenFactory.ts`

业务控制器继续位于各 `scenes/*Scene.ts`。H5 为 `RoomScene` 增加创建/加入/大厅展示状态和自动准备偏好，为 `StudyScene` 增加整体中文显示开关，为 `HistoryScene` 增加“全部”筛选；这些状态仍引用现有房间、词库和战绩 Store，没有复制云端房间或业务记录。

## 5. 自动化验收

必须保持：

- 每个统一页面存在 `{Route}SafeArea` 和 `{Route}Header`。
- 360x800、393x852、430x932 下可见按钮高度不低于约 44px。
- `360x800`、`393x852`、`430x932` 按固定宽度换算后的完整动态画布均无可见节点越界。
- 页面根、背景、场景层、加载遮罩和隐私首屏高度必须等于当前动态可视高度，不允许上下黑边。
- 输入框宿主无 `Graphics` 冲突。
- 微信原生 `EditBox` 占位必须为空；字段名称和填写说明由普通 Cocos `Label` 显示，真实输入文本使用左上锚点，避免原生层重影或偏移。
- 创建/加入/大厅标题随真实状态变化。
- 无历史记录时显示完整空状态卡，不保留空白列表或可点击的幽灵行。
- 原有按钮路由、房间操作、练习、词库、战绩和反馈测试全部通过。
- `npm run verify` 全量通过。

## 6. 后续美术替换

当前页面复用已验证的程序图标和主题背景。后续可以在不改变节点和点击逻辑的前提下替换：

- 页面主题背景
- 词库、练习、合作、战绩和反馈图标
- 结算奖杯与角色装饰

专用位图不得包含烘焙文字，必须继续满足主包和 Bundle 预算。

## 7. 与参考图的必要差异

1. 参考图三使用纯数字键盘，但当前生产房间码是 6 位大写字母与数字组合，例如 `VYVBCU`。客户端保留字母数字输入，避免破坏现有房间兼容性。
2. 参考图四有手动刷新意图；当前项目已有后台轮询，页面不再显示刷新按钮，减少误触和重复云请求。
3. 参考图含示例昵称、金币、词库和战绩；运行时一律绑定真实 Store 数据，空数据展示空状态。
4. 参考图七含发音按钮和音标；当前词库无对应真实资源，因此暂不展示，后续接入正式音频服务后再开放。
5. 本阶段只实现程序化布局和资源槽，不从参考合成图裁切图片，也不把带文字的参考图放入运行包。
6. 参考图五的筛选控件不进入当前运行界面。现有词库目录没有完整的学段、年级、册次和类型结构化字段，因此不展示不可用的假筛选；后续应先扩展真实目录元数据，再启用可交互筛选。

## 8. H6 赛前逻辑精简

本阶段不改变七页布局，也不接入美术资源，只整理页面背后的控制逻辑：

- 玩法目录只保留当前可见的“准备体验模式”动作；筹备中卡片不绑定旧合作玩法处理器。
- 词库只在图二创建配置中选择，图一不再重复显示词库状态或保留换词库入口。
- 创建和加入使用互斥表单，不为当前入口构造另一套隐藏控件。
- 房间会话建立后立即销毁入口表单，只保留图四所需的房间码、邀请、玩家、准备、开始和离开控件。
- 房间页面不保留无 UI 的机器人难度和手动刷新控制器；房间同步继续由后台轮询完成。
- H6 当时保留底层 `RoomSessionService`；H8.1 已进一步删除其中仅供赛前主动添加机器人的入口，房间/计分/同步协议和玩法 Bundle 仍保持冻结。

## 9. H7 赛前选项清理

- 赛前页面不提供、不保存也不显示总游戏时长。
- 创建房间配置只保留玩法与词库，不接受机器人难度或总时长参数。
- 准备大厅只显示房间码、玩法、两名玩家准备状态、邀请、准备、开始和离开。
- 玩家行统一显示“玩家1/玩家2”，不在赛前识别或展示机器人身份。
- 服务端返回的房间时长只供游戏内部倒计时兼容；玩法内部机器人、倒计时和结算本阶段不修改。

## 10. H8 全屏视觉适配

- `BootScene` 与持久化 `Home.scene` 在首帧切换为固定宽度策略，长屏高度由设备窗口比例计算并限制在 `960-1440` 逻辑像素。
- 运行背景、页面根、加载遮罩、隐私首屏、弹层和安全区统一读取动态高度，解决旧完整显示策略产生的上下黑边。
- 七张参考图对应页面全部接入统一程序化场景层和按钮高光；首页、词库、战绩与页头/底部操作按动态安全区重新锚定。
- 创建、加入和准备房间在长屏中整体贴近页头，避免标题与表单之间出现大块无意义空白。
- `393x852` 长屏运行时模拟会完整遍历创建、加入、词库返回、练习、战绩和反馈，并检查所有可见节点没有越界。
- 本阶段只完成准备部分；游戏内场景、规则、时长、机器人兼容、计分与同步逻辑没有修改。

## 11. H8.1 双真人赛前边界

- Cocos 赛前控制器、`RoomSessionService` 和 `RoomService` 不再提供添加机器人动作，pending action 与按钮可用性也不包含机器人分支。
- 创建、加入、邀请恢复和准备大厅仍使用同一真实房间会话；所有可见模式都必须有两名真实玩家才能开始。
- 旧房间快照里的 `isBot`、`duration`，原始云函数类型以及玩法内部机器人/倒计时逻辑仅作为兼容层保留，赛前页面不能调用或展示它们。
- 首页、房间、练习页面中不再引用的处理器和隐藏文案写入已删除，现有可见按钮、路由和真实数据绑定保持不变。
- 后续单人模式应作为独立玩法目标设计，不复用或重新开放本阶段删除的赛前测试机器人入口。

## 12. H8.2 房间同步反馈

- 房间轮询继续在后台运行，不提供手动刷新按钮，也不在每轮请求时闪动“正在同步房间”。
- 首次取得房间快照前可以显示一次进入/读取状态；同步失败必须显示错误，并说明后台会自动重试。
- 已有快照时，状态卡稳定显示当前准备条件，不被正常轮询状态覆盖。
- 开始按钮在条件不足时保持禁用色；两名真实玩家都准备后恢复主操作高亮，并显示“双方已准备，点击开始游戏”。

## 13. H8.3 多玩法赛前命名

- 玩法目录进入配置使用 `openModeSetup`，不再以临时体验或具体游戏类型命名。
- 房间控制器使用 `createConfiguredRoom` 和 `startSelectedMode`，表达“按当前配置创建”和“启动已选玩法”。
- 赛前会话边界使用 `startPreparedMode`，便于未来不同玩法共享准备大厅。
- `RoomService.startGame`、云函数 `startGame/catchFish`、协议字段和游戏内 `Fishing*` 代码是现有生产/玩法兼容层，本阶段不重命名。
- 新玩法应注册到玩法目录并复用通用赛前链路，再路由到自己的 Bundle；不得在赛前控制器重新引入捕鱼、捕虫或其他单一玩法命名。

## 14. H8.4 首页层级定稿

- 首页保持批准的纵向结构：创建房间、加入房间为两项全宽主入口，赛前练习/选择词库与玩法目录/战绩记录组成两行辅助入口。
- 头像、金币和设置使用直接图标或紧凑信息胶囊，不再放入大块白色卡片；按钮皮肤、图标与点击区域必须完整重合。
- 首页调整不得改变真实玩家、金币、词库、路由或隐私入口的数据绑定。

## 15. H8.5 其他准备页视觉层级

### 15.1 共用页头与卡片

- 非首页准备页使用 `560x92` 深色半透明页头承载标题与副标题，返回按钮保持 `80x80` 透明图标热区，不额外绘制文字胶囊。
- 关键状态和表单使用带顶部彩色强调条的卡片；强调色表达页面职责，不能替代标题、状态文本或真实禁用态。
- 长屏增加内容组之间的垂直留白，不放大字体，不让底部操作漂离拇指可达区域。

### 15.2 各页面层级

- 词库：顶部只显示当前词库、解锁和金币状态；下方标题明确“单元词库 · 选择本次学习内容”，不显示没有真实数据支持的筛选控件。
- 背词：当前词库与“更换”并排，学习进度、英文和中文集中在主卡；“查看当前释义”和大号“下一个”保持单手连续操作。
- 玩法目录：当前可用玩法使用全宽重点卡，其余筹备玩法使用稳定双列网格；禁用卡不响应点击。
- 创建房间：按“已选玩法、当前词库、流程提示、创建、自动准备”排序；加入房间使用独立大表单，房间码字段名称和字母数字说明始终可见。
- 准备大厅：房间码、词库、玩家和准备条件分别使用状态卡；两名真实玩家准备后才高亮开始按钮，轮询仍在后台运行。
- 结算：奖杯、结果、总分和玩家得分集中在主卡；返回首页与查看战绩保持明确主次。
- 战绩：最近、最佳、列表和拼词详情沿用真实数据；无记录时显示不可点击的图标、标题与提示组成的完整空状态。
- 反馈：反馈内容和联系方式均使用独立字段标题；微信原生输入框占位为空，避免原生层文字与 Cocos 标签叠加；提交和隐私保护指引在表单下方分组。
- 玩法说明：使用单张顶部对齐的高可读规则卡，目录图标、摘要与正文形成稳定阅读顺序。

### 15.3 H8.5 验收

- 自动化测试必须覆盖页头背景、透明返回、强调卡、玩法目录网格、历史空状态、反馈字段标题和输入文字锚点。
- 微信开发者工具必须逐页检查词库、背词、玩法目录、创建/加入房间、战绩、反馈和玩法说明；不得出现图标越界、文字遮挡、原生输入重影或应用运行错误。
- H8.5 只修改准备页装配与展示测试，不改变玩法 Bundle、云函数、旧客户端、房间协议、计分、同步、AppID 或游戏内部规则。

## 16. H8.6 最终参考图对齐

### 16.1 首页

- 创建房间和加入房间保持全宽主入口，但宽度收紧为 `520` 设计像素并增加高度；四个辅助入口保持两列，统一为更高、更容易点击的卡通按钮。
- 玩家、金币使用紧凑胶囊；“和好友一起比拼单词实力”使用独立橙色副标题条。
- 当前词库使用独立白色状态条，右侧“更换”为真实按钮；长词库名必须使用 `SHRINK` 并为按钮保留固定空间。
- 角色装饰只能占用第二行辅助入口与底部隐私/反馈入口之间的空白。空白不足时按比例缩小，短屏仍不足时隐藏，不能覆盖任何按钮。

### 16.2 玩法目录和房间

- 玩法目录固定为一列八行，不折叠、不切换双列；每行由模式信息和独立“立即体验/筹备中”按钮组成。
- 创建房间使用“已选模式、当前词库、开始条件”三张带标题签和图标的框；提示必须说明“创建后邀请好友”和双方准备后的下一步。
- 准备房间将房间码、当前词库、两名玩家和当前状态拆成独立区域。玩家只显示系统安全名称和准备状态，不显示旧机器人身份。
- 所有返回按钮使用蓝色可见按钮背景；房间轮询继续在后台运行，不恢复手动刷新入口。

### 16.3 词库、练习和战绩

- 词库每页保留四行真实目录数据，右侧用“已选择/选择/需解锁”状态徽标，不添加缺少真实元数据的筛选器。
- 赛前练习把当前词库、更换、学习卡、查看释义、错词、整体中文开关和大号“下一个”按单手操作顺序排列。
- 战绩顶部使用真实最近记录和最佳成绩；每条记录的“详情”是独立按钮，仅在现有业务允许时启用。

### 16.4 验收

- `360x800`、`393x852`、`430x932` 下所有可见按钮换算后高度不得低于 `44px`。
- 首页副标题、词库条、主按钮、角色和底部入口不得重叠。
- 自动化必须遍历首页、玩法目录、创建/加入、词库返回、练习、战绩、反馈和房间状态。
- 核心源码预警线调整为 `1.52 MB`，只用于容纳最终准备页结构；微信主包 `4 MiB` 硬门槛保持不变。
- 不修改 `mode_pk`、`mode_spell`、`cloudfunctions`、`miniprogram`、房间/计分/同步协议、AppID 或游戏内部规则。

## 17. H8.7 赛前界面视觉精修

### 17.1 共用页头与卡片质感

- 非首页页头继续使用可见蓝色返回按钮，但标题图标必须复用 `home_common` 的正式语义图标，不再用单个汉字充当最终图标。
- 标题、副标题和分隔线形成稳定的三层阅读顺序；白色文字使用克制描边保证在高亮背景上清晰，不增加字号或挤压安全区。
- 通用卡片保留原有圆角和投影，并增加轻量内描边；分区标题签增加独立阴影，使标题签、卡片正文和背景之间边界清楚。

### 17.2 按钮和状态反馈

- 所有文字按钮、图标按钮和正式皮肤按钮在按下时只移动内容 `2` 个设计像素，不改变点击区域或外框尺寸。
- 禁用状态必须同时降低底图、文字和正式图标的视觉权重，不能只把底图染灰后留下高亮文字或彩色图标。
- 彩色紧凑按钮的文字增加细描边；正式大按钮标题和副标题保持独立字号与描边，不烘焙动态文字。
- 战绩筛选必须显示当前真实筛选项，选中标记随“全部、PK、合作、拼词”切换；不可用的“其他”仍保持禁用且不产生假数据。

### 17.3 验收

- 自动化验证卡片内描边、页头正式图标、标题层次、按钮按压位移、禁用文字状态和战绩筛选标记。
- `360x800`、`393x852`、`430x932` 的按钮点击区域、图标包含关系和文字边界继续满足 H8.6 门槛。
- Creator/微信检查覆盖首页、词库、练习、玩法目录、创建/加入/准备房间、结算、战绩、反馈和说明页；重点比较卡片层次、文字清晰度和禁用/按下状态。
- 本阶段不新增位图，不修改 H4 元数据、玩法 Bundle、云函数、旧客户端、房间/计分/同步协议、AppID 或游戏内部规则。

### 17.4 第二轮：真实状态视觉化

- 共用按钮选中态由控制器真实状态驱动，统一使用主题色底面、4px 内收描边和反白内容；禁用优先级高于选中态，避免不可操作控件仍显得可点击。
- 词库卡片选中态跟随 `bankPickerSelectedBankId`，中文显示跟随 `StudyStore.showMeaning`，自动准备跟随 `roomAutoReady`，准备与开始跟随房间快照和 `RoomRules` 可用性，战绩页跟随真实筛选模式。
- 学习卡显示由当前单词序号和总数计算的主题色进度条；进度限制在 `0..1`，无单词时清空，不伪造学习数据。
- 两张玩家卡增加“已准备/等待中”徽标，房间状态卡在满足开始条件时显示可开始指示点；它们只读取真实玩家 `ready` 状态和既有开始条件。
- 加入房间码与反馈输入框在原生 EditBox 开始/结束编辑时切换聚焦描边，不增加原生 placeholder，不改变校验、长度和提交逻辑。
- Creator/微信验收除布局外需逐项触发上述状态，确认底色、描边、文字、图标和徽标同步变化且没有遮挡。

### 17.5 第三轮：操作结果持续反馈

- 词库页底部解锁按钮必须读取当前选择项的真实可解锁/已解锁状态；无需解锁时按钮禁用并显示“无需解锁”，避免仍呈现可消费金币的错觉。
- 练习页错词按钮必须读取当前单词是否已存在于真实错题库；加入成功后立即切换为选中态并显示“已在错题库”，切换到其他单词时重新计算。
- 房间页未装配的旧玩家汇总文字不再保留；玩家卡徽标和状态卡继续作为唯一可见准备状态，避免重复信息。
- 本轮不改变解锁价格、错词存储、房间快照、玩法或云协议。Creator/微信需检查禁用文字、选中描边和长词库名称是否清晰且不重叠。

### 17.6 第四轮：输入长度与重复卡片收敛

- 房主与玩家卡使用同一个共用构造器，保持现有节点名、尺寸、头像、准备/等待徽标和控制器绑定，避免两份结构继续漂移。
- 高度至少 100 设计像素的 EditBox 在右下角显示实时 `当前长度/最大长度`；当前覆盖六位房间码和 300 字反馈正文，64px 联系方式输入框不显示计数，避免文字拥挤。
- 计数只读取真实 `EditBox.string`，在 `text-changed` 后更新；反馈提交成功清空正文时必须同步复位到 `0/300`。
- Creator/微信需检查计数不遮挡原生输入文字、光标、聚焦边框和多行正文，尤其覆盖空值、满长度及输入法弹起状态。

### 17.7 第五轮：表单主操作实时反馈

- 加入房间页初始提示六位字母数字格式；输入过程中显示剩余位数，标准化结果达到六位后显示“可以加入”，同时启用主按钮。空值、半截或含无效字符的结果不得呈现可点击状态。
- 反馈页沿用现有 `4-300` 字规则：空值显示填写引导，少于四字显示过短提示，达到要求后显示可提交状态并启用主按钮；请求中禁用，成功清空后恢复空值禁用状态，失败后保留内容并允许重试。
- 禁用、启用和请求中状态只改变现有按钮视觉与动态文字，不新增假进度、假数据或新的业务校验，不改变房间码、反馈云请求及返回协议。
- Creator/微信需检查房间码空值/三位/六位以及反馈空值/短内容/有效内容/请求中/成功后的文字、按钮底图、原生输入层和软键盘关系。

### 17.8 第六轮：玩家卡与首页工具弹层

- 房间玩家卡正文只显示“玩家 1/玩家 2（你）”或“等待加入”，准备与等待状态只由右上徽标表达；不得在正文和徽标同时重复“已准备/未准备”。
- 首页玩家弹层使用紧凑身份徽标呈现“系统安全身份”，不伪造公开昵称、等级或头像数据；正式头像仍由现有安全资源槽加载。
- 设置弹层显示 `home_common` 正式设置图标；音效按钮在音效开启时使用共用选中态，静音时恢复普通态，按钮文案和状态说明同步读取真实 `SettingsStore`。
- Creator/微信需检查两张玩家卡的名称、徽标、头像之间无重叠，并检查玩家/设置弹层在打开、音效切换和关闭状态下图标、文字、选中描边与点击区域完整。

### 17.9 第七轮：练习操作图标与玩法徽标

- 练习页“查看当前释义”“标记错词”“显示/隐藏后续单词中文”统一使用共用图标动作按钮；正式资源分别复用练习、词库语义图标，加载失败时使用现有程序化语义图标兜底。
- 错词按钮继续跟随真实错题库显示“标记错词/已在错题库”和选中态；中文开关继续跟随真实学习会话显示对应文字和选中态，图标接入不得替代或伪造业务状态。
- 玩法目录每行的“双人”信息使用紧凑状态徽标，与“立即体验/筹备中”主操作保持分层，不增加尚未实现的单人模式或可用状态。
- Creator/微信需检查 252px 双列练习按钮中图标、六字标题和描边不重叠，并检查中文开关、玩法徽标在禁用/选中/正式图片加载完成后仍位于点击区域内。

### 17.10 第八轮：反馈状态与说明阅读层级

- 反馈页的空值引导、长度错误、可提交、提交中、成功与失败文字共用表单底部固定状态带；动态文字继续使用现有 `FeedbackStatus` 标签，状态带不得遮挡联系方式输入框或表单外主按钮。
- 玩法说明按 `01-06` 显示“编号标题 + 下一行正文”，段落之间保留统一空行；内容仍来自现有真实练习、对战、合作、房间和战绩规则，不新增未实现玩法。
- 状态带和说明层级只调整程序化背景与运行时文字，不改变反馈内容校验、云提交、隐私入口、说明路由或任何协议。
- Creator/微信需检查反馈状态带在原生键盘弹起、长错误信息和提交状态下不重叠，并检查短屏/长屏中六段说明全部可见、行距稳定且没有底部截断。

### 17.11 第九轮：房间准备操作图标统一

- 创建配置的“创建后自动准备”和准备大厅的“我准备好了”统一使用共用语义图标操作按钮，复用 `home_common` 正式练习图标并保留程序化图标兜底。
- 按钮正文只承担动作说明，不再把勾号字符嵌入文字模拟图标；图标、标题和选中描边必须位于同一点击区域内。
- 自动准备选中态继续读取真实 `roomAutoReady`，大厅准备选中态继续读取本机玩家的房间快照；取消准备或切换配置后必须同步恢复，不新增本地假状态。
- 本轮只统一视觉结构，不改变创建、准备、取消准备、开始游戏的处理器及房间协议。
- Creator/微信需检查 390px 自动准备按钮和 440px 大厅准备按钮在普通/选中/禁用/请求中及正式皮肤加载后，图标、标题、选中环和点击区域均完整且无重叠。

### 17.12 第十轮：词库选中信息去重

- 词库列表的选中状态只使用共用选中描边和右侧“已选择”徽标，不再在词库标题前添加勾号字符。
- 标题区域完整保留给教材、年级和单元名称；长名称继续使用 `SHRINK`，不得因重复状态字符提前缩小。
- 选中描边和徽标继续读取真实 `bankPickerSelectedBankId`，分页、解锁、确定选择和返回来源不变。
- Creator/微信需检查已选择、可选择、需解锁三种列表行，确认标题、正式词库图标、徽标和描边分层清楚且没有重叠。

### 17.13 第十一轮：动态文案职责收敛

- 首页词库条的固定小标题显示“当前词库”，动态大标题只显示真实教材、年级和单元名称，不重复添加“当前词库：”前缀。
- 房间首次同步时，玩家卡正文只显示本机身份或空位信息；“正在进入房间”和同步错误统一由“当前状态”卡表达，不在玩家卡重复过程状态。
- 已取得快照后的空玩家位只显示“等待加入”，准备/等待徽标继续承担准备状态，不在正文重复玩家序号和状态。
- 本轮不改变词库选择、房间同步、重试、准备或开始逻辑。Creator/微信需检查超长词库名以及首次进入/同步失败/等待玩家三种房间状态，确认卡片文字无重复、无跳动、无截断。

### 17.14 第十二轮：玩法说明入口闭环

- 玩法目录页头右侧提供熟悉的 `?` 规则按钮，进入现有 01-06 编号玩法说明；按钮在 `360x800` 下的实际触控尺寸不得小于 44px。
- 页头标题和副标题必须为规则按钮预留独立宽度，不得与按钮点击区域重叠；规则按钮仍位于页头背景范围内。
- 说明页标题使用“玩法说明”，返回操作回到玩法目录，模式选择列表保持原滚动顺序和可用状态。
- 删除没有可见控件绑定的 Home 隐藏说明处理器。Creator/微信需检查目录页头、说明正文和往返切换，确认标题、图标、按钮和返回动作清晰连贯。

### 17.15 第十三轮：房间状态色点分层

- 房间“当前状态”卡沿用左侧状态点：双方可开始时显示绿色，请求执行中或后台同步错误时显示橙色，普通等待状态不显示色点。
- 绿色与橙色状态必须互斥；开始请求发出后立即从绿色切换为橙色，完成或失败后根据最新真实快照恢复。
- 状态文字继续承担具体含义，色点只用于快速扫描，不替代错误、重试、等待或可开始文案。
- Room 控制器仍由运行时动态装配，不增加无效 Inspector 元数据。Creator/微信需检查等待、双方准备、开始请求和后台重试四种状态下色点与文字同步且不闪烁。

### 17.16 第十四轮：首页词库条单一点击区

- 首页当前词库条整体保持一个 Button 点击区域，右侧“更换”只作为彩色视觉徽标，不在父按钮内部嵌套第二个 Button。
- 点击词库名、图标、空白区域或“更换”徽标均属于同一个词库条操作，不产生重复路由、双重按压态或不同禁用态。
- 徽标使用紧凑尺寸并为长词库名称保留固定宽度；词库名继续使用 `SHRINK`。
- 所有赛前控制器字段继续由运行时工厂装配，不保留没有场景序列化用途的 Inspector 装饰器。Creator/微信需检查词库条按下时底图、图标、标题和徽标整体反馈一致。

### 17.17 第十五轮：练习页词库条单一点击区

- 赛前练习页顶部当前词库条使用一个完整 Button，右侧“更换”只作为视觉徽标，不再把同一路由拆成并排的两个点击状态。
- 正式词库图标、动态词库名、固定“当前词库”副标题和更换徽标位于同一条卡片内；标题与副标题保留固定文字列，长名称使用现有 `SHRINK`，不得侵入徽标区域。
- 第十五轮曾保留页面底部“更换词库”快捷入口；第十九轮最短高度审计发现它与主操作重叠，因此由 17.21 的单一顶部入口规则取代。
- Creator/微信需检查普通、按下及超长词库名状态，确认顶部卡片不存在双重按压、文字遮挡或点击空洞。

### 17.18 第十六轮：动态文字列边界

- 创建房间配置页的真实词库名称与右侧“更换”按钮使用独立水平区域，名称继续使用 `SHRINK`，其文字框不得进入按钮点击区域。
- 战绩列表行固定分为标题/时间与词库、分数、详情操作三段；标题和元信息共用左列，分数保持居中，详情按钮保持完整触控尺寸，相邻区域至少保留 8 个设计像素。
- 列表行继续绑定真实模式、结果、完成时间、词库、分数和详情可用状态，不截断数据源、不伪造短文案，也不改变分页和详情规则。
- Creator/微信需用长词库名、四位以上分数及可用/禁用详情按钮检查文字缩放、列间距、按钮描边和卡片内边距。

### 17.19 第十七轮：首页工具弹层垂直节奏

- 玩家信息弹层继续只展示真实安全身份：标题、正式头像、真实显示名、身份徽标和关闭按钮按从上到下的单一阅读顺序排列，相邻内容不得贴合或覆盖。
- 设置弹层保持正式设置图标、标题、真实音效状态、音效切换和关闭操作；两个按钮继续使用 80 设计像素高度，不能为了留白缩小触控区域。
- 玩家弹层内容间距不得小于 4 设计像素，设置状态到音效按钮、音效按钮到关闭按钮不得小于 8 设计像素；按压阴影和选中环不得侵入相邻文字。
- Creator/微信需检查玩家弹层长显示名，以及设置弹层音效开启、静音、按下和关闭状态，确认文字、图标、描边、阴影与面板底边均无碰撞。

### 17.20 第十八轮：密集卡片与表单边界

- 玩法目录每行继续展示正式图标、标题、副标题、“双人”徽标和操作按钮；标题与副标题使用独立文字列，其右边界距离徽标至少 8 设计像素，不通过删减真实文案换取空间。
- 反馈表单从顶部说明到状态带保持固定阅读顺序，相邻说明、隐私文字、字段标题、原生输入框和状态带之间至少保留 4 设计像素。
- 联系方式输入框与状态带必须保留至少 8 设计像素，状态带不得覆盖输入边框、光标或文字；原生输入层继续位于 SafeArea，不移动到卡片子树。
- Creator/微信需检查八条玩法的最长标题/副标题和反馈空值、短内容、有效内容、提交中、成功及失败状态，确认徽标、按钮、键盘和状态文字无碰撞。

### 17.21 第十九轮：练习页最短高度重排

- `640x960` 是练习页必须支持的最短逻辑视口；顶部词库条、学习卡、释义/错词双列操作、中文开关和“下一个”按钮必须完整位于 SafeArea 且互不覆盖。
- 顶部词库条继续作为完整单一换库入口。删除底部重复换库按钮，避免同一路由占用主操作空间；词库页返回来源仍为练习页。
- 顶部词库条和三组操作按钮保持 80 设计像素高度，在 `360px` 宽目标设备上仍满足 44px 触控门槛；不得通过缩小触控区解决布局问题。
- 学习卡内单词、释义与上一条/随机操作保留至少 2 设计像素，外部卡片与操作链也保留至少 2 设计像素；长屏继续由既有 `stretch` 公式展开。
- Creator/微信需同时检查 `640x960` 预览以及 `360x800`、`393x852`、`430x932` 设备，覆盖中文显示/隐藏、释义揭示、错词选中和下一个单词状态。

### 17.22 第二十轮：创建配置与准备大厅最短高度重排

- 创建配置页在 `640x960` 下按已选模式、当前词库、开始条件、创建房间、自动准备顺序排列，相邻区域至少保留 8 设计像素，自动准备完整位于 SafeArea。
- 准备大厅按房间码、当前词库、双玩家卡、当前状态、准备、开始顺序排列，相邻区域至少保留 7 设计像素；玩家卡压缩到 220 高但不删除头像、身份或准备徽标。
- 准备按钮保持 80、开始按钮保持 90 设计像素高度，在 `360px` 宽设备继续满足 44px 触控门槛；不得缩小按钮或隐藏真实状态解决布局问题。
- 页头返回是创建配置和大厅的唯一离开入口。删除底部重复“离开房间”按钮，忙碌态继续禁用页头返回，不改变房间释放、返回首页或待处理动作规则。
- Creator/微信需在 `640x960` 以及三种目标设备检查创建配置、等待第二人、双方准备、请求中和同步错误状态，确认卡片、徽标、按钮阴影及 SafeArea 底部无碰撞。

### 17.23 第二十一轮：词库页最短高度重排

- `640x960` 下继续每页展示四张真实词库卡，不通过减少行数解决空间；每行保留正式词库图标、教材/单元标题、单词数/解锁副标题、状态徽标和选中环。
- 词库状态卡高度为 80、词库卡为 96、分页箭头触控高度为 80、解锁与确定操作高度为 80，在三种目标设备继续满足 44px 门槛。
- 状态卡与第一行至少 4 设计像素，词库行之间至少 8，第四行与分页至少 8；底部双操作完整位于 SafeArea 并保留 8 像素底边。
- 分页标签、前后箭头、选中状态、解锁可用性、金币回滚和返回来源继续读取真实状态，不因布局压实改变每页数量或业务规则。
- Creator/微信需检查第一页、中间页、最后一页以及已选择、可选择、需解锁三类卡片，覆盖长词库名、禁用箭头和双底部按钮的文字/图标/阴影边界。

### 17.24 第二十二轮：辅助页面最短高度重排

- `640x960` 下加入房间页保持正式图标、标题、格式提示、六位输入框、邀请说明和主操作的完整阅读链，相邻区域至少保留 4 设计像素；不缩小 110 像素主按钮，不改变六位校验、计数或加入请求。
- 玩法目录继续一次展示八个真实入口，每张卡和操作按钮均保持 80 设计像素高度，页头到首行及各行之间至少保留 4 像素，末行完整位于 SafeArea。
- 战绩页保留五个筛选、两张摘要卡、四条记录和分页。短屏记录卡为 86 高、步进 94，长屏平滑恢复 102/110；标题、记录和分页至少保留 4 像素，记录行之间保持 8 像素。
- 反馈表单卡与共享页头至少保留 4 像素，表单到提交、提交到隐私操作至少保留 8 像素，隐私操作保持 80 高并位于 SafeArea；全部原生输入、状态带和提交逻辑不变。
- 帮助卡与共享页头至少保留 4 像素，在最短视口底部保留 20 像素，继续完整展示 01-06 的真实说明内容。
- Creator/微信需在三种目标设备检查八行目录、四条战绩、反馈键盘/状态、帮助长文和加入房间输入焦点，确认阴影、选中环、禁用态、文字缩放及 SafeArea 无视觉碰撞。

### 17.25 第二十三轮：首页最短高度重排

- 首页 SafeArea 高度低于 1000 设计像素时使用紧凑布局；普通长屏继续保持既有 Logo、主按钮和双列按钮尺寸，不因短屏适配降低当前手机视觉层次。
- `640x960` 下完整保留顶部玩家/金币/设置、Logo 与副标题、当前词库、创建房间、加入房间、赛前练习、选择词库、玩法目录、战绩记录、隐私保护和问题反馈，不能通过隐藏功能入口解决空间不足。
- 紧凑 Logo 高 108，词库条高 80，创建/加入高 96，四个双列入口高 80，底部双操作高 84；功能区之间至少保留 4 设计像素，继续满足 `360px` 宽设备的 44px 点击门槛。
- Logo 与副标题彩带允许 7 像素受控叠合以保持品牌整体感；其他卡片和按钮不得互相覆盖。装饰角色仅在扩展入口与底部操作之间至少有 136 像素可用空间时显示。
- Creator/微信需同时比较紧凑与长屏首页，检查正式 Logo/按钮九宫格、长词库名、金币位数、角色显隐、按下态、底部双操作及微信胶囊安全距离。

### 17.26 第二十四轮：动作按钮文字层次

- 所有带副标题的赛前动作按钮必须把标题与说明放在独立矩形中，两者边界至少保留 2 设计像素，不依赖透明背景或字体视觉空白掩盖重叠。
- 120 及以上高度使用 `24/-32` 标题/副标题基线，96-119 使用 `20/-26`，更紧凑按钮使用 `16/-22`；标题框高为按钮的 40%，副标题框继续为 30%。
- 标题字体、说明字体、正式图标、按钮外框、九宫格皮肤、按下偏移、选中环和禁用态保持不变；不得通过删副标题或缩小点击区解决排版问题。
- 通用可见 UI 合约遍历所有动作按钮并验证文字间距，因此长屏和 `640x960` 的首页、词库、练习、房间、玩法、战绩及反馈状态都必须同时通过。
- Creator/微信需检查中英文混排、长词库名、两行说明、按下态和禁用态，确认实际字体字形不贴合、描边不串行、说明文字不压住底部九宫格边框。

### 17.27 第二十五轮：共享页头文字层次

- 共享页头保持 116 设计像素高度、31px 标题和 15px 副标题；标题、分隔线、副标题必须形成从上到下的独立层次，不允许依赖透明线条掩盖矩形相交。
- 分隔线位于 `y=-7`，与标题框至少间隔 2 像素；副标题使用 `y=-29/h=24` 文本框，与分隔线至少间隔 2 像素。
- 左侧语义图标底边与副标题上边至少间隔 2 像素。返回按钮、图标板、标题/副标题宽度和玩法目录 `?` 规则按钮保持原位置与点击尺寸。
- 通用可见 UI 合约必须检查所有带 Header Divider 的页面，长屏和 `640x960` 重建均不得出现标题压线、图标压字或副标题进入页头底边。
- Creator/微信需逐页检查标题描边、半透明分隔线、长副标题缩放和正式图标透明边缘，确认实际渲染没有粘连、锯齿或文字偏心。

### 17.28 第二十六轮：紧凑卡片标题标签

- 高度小于 120 设计像素的短状态卡使用 24 像素标题标签、14px 标题和 18px 语义图标，避免原有 58/22/38 规格占据大部分正文空间。
- 高度达到 120 及以上的配置卡、选项卡继续保持 58 像素标签、22px 标题和 38px 语义图标，不能因为短卡适配降低主要配置区的视觉层级。
- 紧凑标签与正文矩形至少保留 4 设计像素垂直间距；词库状态、房间词库模式、房间状态及战绩摘要不得出现标签压字、正文贴边或语义图标侵入正文。
- 卡片颜色、阴影、边框、正文文案、状态数据、点击区和交互逻辑保持不变；本轮只调整共享标签及三处紧凑状态正文的内部排版。
- Creator/微信需同时检查长屏与 `640x960` 下的大卡片和短卡片，确认正式图标透明边缘、标题缩放、正文基线和九宫格边框之间没有视觉碰撞。

### 17.29 第二十七轮：词库信息层次

- 首页当前词库条使用固定说明和真实词库名两行结构：15px“当前词库”位于上层，27px 动态名称位于下层，两个文本矩形至少保留 4 设计像素间距。
- 正式词库图标、326 像素动态名称列和“更换”徽标形成三列结构，相邻列至少保留 4 像素。长名称继续使用 `SHRINK`，不得压入图标或徽标，也不得把徽标改成嵌套按钮。
- 词库页 80 高状态卡的正文金币图标使用 `32x32` 并下移，必须同时避开 24px 紧凑标题标签和状态文字列；标签中原有 18px 语义图标保持不变。
- 首页词库条仍是唯一完整点击区，词库状态卡继续读取真实金币、选择和解锁状态；本轮不改变文案、按钮尺寸、数据绑定、路由或业务规则。
- Creator/微信需检查普通词库名、超长词库名、错题库、八位以上金币及长/短屏，确认标题/名称字形、正式图标透明边缘和“更换”徽标不存在视觉粘连。

### 17.30 第二十八轮：创建配置卡片层次

- “已选模式”卡的主标题和说明使用独立矩形，至少保留 4 设计像素；正文标题、说明及 92px 正式图标均不得进入 58px 大标题标签。
- “当前词库”卡的 66px 正文图标下移并与标题标签至少保留 4 像素；图标与 300px 动态词库名称列、名称列与 118px“更换”操作也分别至少保留 4 像素。
- 动态词库名称继续使用 `SHRINK`，不能通过删减真实名称或缩小“更换”触控区解决空间问题；两张卡的外框、标签规格、正式图标和颜色保持不变。
- 长屏和 `640x960` 使用同一卡片内部合约，必须同时验证标题标签/正文、图标/文字、文字/操作边界，创建、换词库、返回和自动准备逻辑不变。
- Creator/微信需检查长模式名、长词库名、按下态、正式图标透明边缘和文字描边，确认实际字体与九宫格卡片边框之间不粘连。

### 17.31 第二十九轮：准备玩家卡层次

- 房主与玩家卡的标题标签只展示角色标题，不在标签内重复头像；每张卡正文只保留一个 72px 正式/回退头像，避免同一语义图标重复争夺视觉焦点。
- 正文使用左头像、右状态与玩家名两列结构。标题标签分别与头像、准备/等待徽标至少保留 4 设计像素；状态徽标与玩家名也至少保留 4 像素。
- 头像列与右侧徽标、玩家名至少保留 4 像素横向间距。玩家名继续显示真实安全身份文案，准备状态只由“已准备”或“等待中”徽标表达。
- 两张卡继续保持 `264x220`、16px 双列间距及原有颜色；不得改变房主/玩家归属、准备状态计算、房间轮询或开始条件。
- Creator/微信需检查等待加入、双方未准备、单方准备和双方准备状态，确认头像透明边缘、徽标描边、玩家名缩放和卡片标题之间无粘连。

### 17.32 第三十轮：准备大厅工具卡

- 房间码卡使用 216px 房间码列与两个等宽 `112x80` 的复制、邀请操作；三列之间和卡片右边至少保留 8 设计像素，不使用宽度不一或贴边按钮。
- 房间码正文与 24px 紧凑标题标签至少保留 4 像素，继续显示真实六位房间码；复制和邀请保留原有处理函数、忙碌禁用态和 80px 触控高度。
- 当前词库卡的标签与动态词库名至少保留 4 像素，文字右侧至少保留 8 像素卡内边距；长词库名继续使用缩放显示。
- 当前状态卡的标题标签分别与绿色可开始指示灯、橙色提醒指示灯和动态状态文字至少保留 4 像素；指示灯与文字列、文字与右卡边也至少保留 8 像素。
- Creator/微信需检查房间码占位/真实值、复制与邀请按下/禁用态、长词库名、普通等待、后台重试和可开始状态，确认文字、指示灯和按钮边框无粘连。

### 17.33 第三十一轮：强调卡装饰轨

- 所有共享强调卡使用左侧 6px 竖向强调条，不再使用会穿过顶部标题的横向色带；竖条距卡片左边 8 像素，高度为卡片高度减 24。
- 强调条继续使用页面动作色和 3px 圆角，但必须与正式图标或第一列正文至少保留 4 设计像素，不能依赖图层覆盖掩盖交叉。
- 玩法目录标题与说明文本矩形至少保留 4 像素；80px 玩法行仍完整保留图标、标题、说明、双人徽标和操作按钮。
- 加入房间、战绩行/详情、反馈表单和帮助卡统一继承左强调条，不改变卡片尺寸、内容位置、点击区、状态或路由。
- Creator/微信需逐页检查不同高度强调卡的竖条长度、圆角、颜色和图标间距，确认透明图标、文字描边及卡片内框均不与强调条粘连。

### 17.34 第三十二轮：学习卡正文节奏

- 学习卡按“进度标题、实时状态、进度条、单词、释义、双按钮”从上到下形成单一阅读顺序；进度标题与状态、状态与进度条至少保留 4 设计像素，不能发生矩形重叠。
- 进度条与单词、单词与释义、释义与底部双按钮至少保留 6 像素。英文单词继续使用 58px 字号，中文释义继续使用 29px 字号，长内容沿用现有缩放策略。
- “上一个”和“随机”保持同一基线、`220x80` 尺寸及原有行为，并与 `540x360` 卡片底边保留 8 像素内边距。
- 长屏和真实 `640x960` 最小视口使用同一组内部间距约束，不以压缩正文、隐藏进度或缩小触控区换取适配。
- Creator/微信需检查长英文单词、中文释义显示/隐藏、不同学习进度文字及双按钮按下态，确认实际字体、进度条和卡片边框之间无粘连。

### 17.35 第三十三轮：学习卡页内导航按钮

- “上一个”和“随机”采用共享图标动作按钮结构，图标与标题位于独立矩形中，不再把 `←`、`↻` 等字符混入可见标题充当最终图标。
- “上一个”复用正式词库图标槽，“随机”复用正式练习图标槽；资源未加载时继续使用既有程序化回退，不新增位图、Bundle 或 importer 元数据。
- 两个按钮保持 `220x80`、原位置和同一基线，图标与纯标题至少保留 8 设计像素，标题、图标和按压偏移不得进入相邻按钮或学习卡底边。
- 点击行为继续调用真实 `previousWord` 和 `randomWord` 学习逻辑；自动化必须证明上一个、恢复和随机操作实际改变进度，而不只验证节点存在。
- Creator/微信需检查正式图标加载前后、普通与按下态、长屏和三种目标设备，确认图标透明边缘、标题居中、按钮皮肤和卡片内边距一致。

### 17.36 第三十四轮：首页金币胶囊

- 首页金币胶囊按“正式金币图标、真实动态金币数、加号圆徽标”从左到右排列；加号不得继续作为贴在数字旁的裸文本。
- 加号使用 `34x34` 蓝色圆徽标和 26px `+`，徽标只负责视觉提示，不挂载 Button；整枚 `206x80` 金币操作仍是唯一点击区，并继续进入真实词库/金币消费页面。
- 金币图标与数字、数字与加号、加号与金币操作右边均至少保留 4 设计像素。数字使用固定 82px 列和 `SHRINK`，不得伪造上限或截断真实余额。
- 长屏、真实 `640x960` 和九位金币状态使用同一内部边界；共享状态徽标新增的可选字号不得改变其余准备状态、身份或筛选徽标的默认 14px 规格。
- Creator/微信需检查普通与九位金币、正式金币图标加载前后、按下态及三种目标设备，确认加号圆度、数字缩放、描边和胶囊边框没有粘连。

### 17.37 第三十五轮：首页安全玩家身份

- 首页玩家胶囊使用“真实 Store 系统名 + 系统安全身份”双行结构，不使用参考图中的示例昵称、等级、经验或任何没有真实数据来源的信息。
- 主名使用 21px、固定 152px `SHRINK` 文字列；安全身份使用 13px 次级文字列，两者至少保留 4 设计像素，并完整位于 `178x62` 深色胶囊内。
- 头像按钮、玩家胶囊和金币胶囊按从左到右排列，相邻区域至少保留 8 像素。玩家胶囊不挂载 Button，正式头像仍是唯一玩家信息入口，避免相邻或嵌套双点击区。
- 本轮不得恢复昵称编辑、公开身份资料或等级系统；点击头像继续只打开现有真实玩家信息弹层，关闭和路由行为保持不变。
- Creator/微信需在长屏、真实 `640x960` 和三种目标设备检查正式头像加载前后、双行文字基线、胶囊描边与金币区域间距，确认没有碰撞或错误点击反馈。

### 17.38 第三十六轮：准备大厅复制与邀请按钮

- 房间码右侧“复制”和“邀请”使用共享紧凑图标动作按钮结构，图标与纯标题位于独立矩形中，不再由纯文字占据整枚按钮。
- 两个按钮保持 `112x80` 和原有颜色；图标与标题至少保留 4 设计像素。邀请挂载正式好友加入图标，复制使用独立“码”程序化回退，待正式复制资源获批后可在同一槽位替换。
- 房间码、复制、邀请三列继续保持至少 8/14/8 像素间距；不得扩大按钮、压缩真实六位房间码或减少右卡边距解决内部布局。
- 复制和邀请继续调用真实剪贴板与分享处理器，并在任一房间动作执行时同步禁用和降低视觉权重；不得增加重叠点击区或改变邀请协议。
- Creator/微信需在长屏、真实 `640x960`、真实/占位房间码、普通/按下/禁用状态检查紧凑标题缩放、正式邀请图标透明边缘和按钮描边。

### 17.39 第三十七轮：创建配置换库按钮

- 创建配置“当前词库”卡的“更换”操作使用共享紧凑图标动作按钮结构，挂载正式词库图标，并将“更换”作为独立纯标题。
- 操作保持 `118x80`、原练习色和真实换库处理器；图标与标题至少保留 4 设计像素，不通过缩小触控区或隐藏标题解决紧凑布局。
- 300px 动态词库名称列继续使用 `SHRINK`，与换库按钮至少保留 4 像素；标题标签、66px 正文词库图标和动态名称原有垂直层次保持不变。
- 点击换库继续进入真实词库页，返回来源仍为创建配置；不得改变当前词库、自动准备、房间创建设置或请求协议。
- Creator/微信需在长屏、真实 `640x960`、长词库名、普通与按下态检查正式词库图标透明边缘、紧凑标题缩放和按钮/卡片边框间距。

### 17.40 第三十八轮：战绩详情紧凑图标按钮

- 共享动作按钮在宽度小于 120 设计像素时使用 28px 图标、12px 左内边距、6px 图文间距和 8px 右内边距，不能继续套用大按钮比例导致内容出界。
- 战绩列表每行“详情”由纯文字按钮升级为正式战绩图标和独立“详情”标题，保持原 `92x80` 点击区、表面色和真实详情处理器。
- 图标与标题至少保留 4 像素；图标左边至少保留 8 像素，标题右边至少保留 4 像素。标题/元信息、动态分数和详情操作三列继续保留至少 8 像素。
- 紧凑规则同时适用于既有 `112x80` 复制/邀请和 `118x80` 换库操作；不得改变它们的外框、点击逻辑、状态或资源槽。
- Creator/微信需在长屏、真实 `640x960`、长记录标题、四位以上分数、普通/按下/禁用状态检查正式图标透明边缘、标题缩放和三列间距。

### 17.41 第三十九轮：战绩摘要卡单图标层次

- “最近记录”和“最佳成绩”卡的紧凑标题标签已经分别包含正式战绩、金币图标，正文不得再次重复同一语义图标争夺视觉焦点。
- 两张 `272x110` 卡的真实摘要文字居中显示，文字框由 190px 扩为 236px，左右各保留 18px 内边距；最近记录继续完整承载模式名和本地化时间两行文本。
- 标题标签中的正式图标、标题、描边和色彩保持不变；最佳成绩继续读取真实动态分数，不伪造记录、不截断为固定示例。
- 长屏和真实 `640x960` 必须同时保持标题/正文间距、单图标结构和居中文字边界，不通过缩小卡片或字号解决长时间文本。
- Creator/微信需检查空记录、普通记录、长本地时间及四位以上最佳分数，确认标题图标透明边缘、摘要缩放和卡片内框不存在视觉粘连。

### 17.42 第四十轮：共享页头四像素呼吸层次

- 所有非首页共享页头的标题底边到分隔线上边、语义图标底边到副标题上边均由至少 2 设计像素提升到至少 4 设计像素，不能依赖描边透明区制造假间距。
- 分隔线和副标题整体下移 2 像素；页头仍保持 `116px` 高度，返回按钮仍为 `86px`，图标板、标题/副标题宽度和所有点击逻辑不变。
- 战绩页五个 `104x80` 筛选分段整体下移 2 像素，使页头与筛选栏间距达到 4 像素，同时不得压缩筛选栏到摘要卡的间距。
- 通用可见 UI 合约必须在 Bank、Study、玩法目录、Room、Result、History、Feedback、Help 的长屏和已覆盖最短屏树上执行新页头门槛。
- Creator/微信需逐页检查标题描边、分隔线、副标题和正式图标透明边缘，并在战绩页检查页头、筛选选中线与摘要卡形成清晰连续的阅读节奏。

### 17.43 第四十一轮：练习页四像素完整阅读链

- 真实 `640x960` 下，练习页从共享页头、当前词库条、学习卡、释义/错词双操作、中文开关到“下一个”必须形成连续阅读链，相邻外部边界至少保留 4 设计像素。
- 学习卡使用 `540x360` 四像素网格规格；卡内“上一个/随机”继续为 `220x80`，与卡底保留 8 像素，不缩小单词、释义、进度或触控区域。
- “下一个”与 SafeArea 底部至少保留 4 像素，当前实现为 6 像素；顶部词库条和三组页面操作继续保持 80 像素高度。
- 长屏继续使用既有 stretch 展开，不因最短屏重排降低长屏留白；换库返回、释义显示、错词选中、上一个、随机和下一个均沿用真实状态与处理器。
- Creator/微信需在三种目标设备检查长单词、释义显示/隐藏、错词选中、按钮按下和长词库名，确认卡片内外间距、描边与 SafeArea 底边形成稳定节奏。

### 17.44 第四十二轮：首页品牌区与配置区间距

- 长屏首页品牌副标题彩带底边到当前词库条顶边由 2 设计像素提升到 4 设计像素，不能让彩带描边与词库卡阴影形成粘连。
- 只将长屏当前词库条下移 2 像素；Logo、彩带、词库条尺寸、正式词库图标、标题、动态名称、更换徽标和整体点击区均保持不变。
- 词库条底边到创建房间主按钮顶边继续保留至少 8 像素，当前长屏实现为 13 像素，不以挤压主操作换取品牌区留白。
- `640x960` 紧凑布局继续使用原坐标并保持既有 4 像素彩带/词库间距，所有首页动作和底部辅助入口不得因此移动或隐藏。
- Creator/微信需同时检查长屏和紧凑首页的正式 Logo、彩带、长词库名、词库条阴影及创建房间皮肤，确认品牌到配置再到主操作的阅读层次稳定。

### 17.45 第四十三轮：词库页四像素完整纵向链

- 词库页从共享页头、80px 状态卡、四个 `540x96` 词库卡、80px 分页到 80px 解锁/确定双操作形成完整纵向链，相邻外部边界至少保留 4 设计像素。
- 状态卡和四个词库卡整体下移 2 像素；卡片宽高、正式图标、动态标题/说明、状态徽标、选中环和点击区域均保持不变。
- 长屏与真实 `640x960` 都必须覆盖页头/状态、状态/首卡和末卡/分页边界；最短屏回归继续覆盖分页/底部操作和 SafeArea 底边。
- 翻页、选择、解锁可用/无需解锁、持久化失败回滚和确定返回继续使用真实状态与处理器，不通过隐藏状态卡或按钮换取留白。
- Creator/微信需检查第一页、中间页、末页、长词库名、已选择/可选择/需解锁及按钮禁用状态，确认卡片阴影、选中环、分页和底部双操作节奏稳定。

### 17.46 第四十四轮：首页金币胶囊完整四像素内边距

- 首页金币工具继续按正式金币图标、真实动态金币数、蓝色加号圆徽标排列，三段相邻边界和加号/操作右边均至少保留 4 设计像素。
- `34x34` 加号徽标向左内收 2 像素，26px `+`、`206x80` 整体操作、82px 动态数字列和深色胶囊均保持不变。
- 加号徽标不得挂载 Button；整枚金币操作仍是唯一点击区并进入真实词库/金币消费页面，不增加嵌套或相邻点击目标。
- 长屏、真实 `640x960` 和九位金币状态执行同一内部边界契约，数字继续使用 `SHRINK`，不得通过截断或伪造余额换取间距。
- Creator/微信需检查正式金币图标加载前后、普通/九位金币、按下态和三种目标设备，确认加号圆度、右侧留白、数字缩放和胶囊描边稳定。

### 17.47 第四十五轮：反馈页十二八八底部节奏

- 反馈表单卡底边到提交按钮顶边至少保留 12 设计像素，提交按钮到隐私入口、隐私入口到 SafeArea 底边均至少保留 8 设计像素。
- 提交按钮上移 1 像素、隐私入口上移 3 像素；`560x94` 提交、`560x80` 隐私操作和 `560x500` 表单卡尺寸均保持不变。
- 表单内部说明、隐私提示、字段标题、原生内容/联系方式输入和状态带的既有 4/8 像素边界继续有效，不移动原生输入到卡片子树。
- 空值、短内容、有效内容、提交中、成功清空、失败重试、字数计数和隐私入口继续使用真实状态与处理器。
- Creator/微信需在三种目标设备和键盘弹起/收起状态检查表单底边、双操作阴影、按下态及 SafeArea 留白，确认视觉层次稳定且输入层不漂移。

### 17.48 第四十六轮：玩法说明六行规则目录

- 玩法说明不再用一个居中的长文本承载六类规则；在既有外层强调卡内拆成六行“编号徽标、标题、简短说明”，不创建嵌套卡片。
- 六个 `44x34` 编号徽标分别复用练习、创建、加入、词库、目录和战绩主题色；18px 标题与 14px 说明独立排版，徽标/标题及标题/说明至少保留 3 设计像素。
- 真实 `640x960` 下相邻规则至少保留 8 设计像素；长屏根据卡片高度均匀扩大行距，不拉伸徽标与文字本身。
- 六条规则事实仍由 `HelpScene` 单一维护，返回操作仍进入玩法目录；不得借视觉拆分修改计分、限时、房间条件或战绩规则。
- Creator/微信需在三种目标设备检查六种编号颜色、正式目录图标、长短说明缩放和卡片底部留白，确认目录可快速扫描且没有文字粘连。

### 17.49 第四十七轮：加入房间八像素输入链

- 加入房间卡按正式图标、标题、房间码格式提示、原生输入、邀请提示和主操作形成单向阅读链，相邻节点至少保留 8 设计像素。
- 标题上移 1 像素，`500x104` 原生输入和邀请提示各下移 3 像素；`560x600` 卡片、`500x110` 加入操作及其他节点尺寸不变。
- 输入框继续位于卡片节点内并保留聚焦层、计数、六字符格式校验、禁用/请求状态和真实加入处理器，不为排版复制输入层或点击区。
- 长屏与真实 `640x960` 使用同一六节点链路断言；页头/卡片及卡片/SafeArea 边界继续满足既有约束。
- Creator/微信需检查空值、部分输入、完整房间码、输入聚焦和键盘弹起/收起状态，确认提示、输入边框、计数和主操作之间没有视觉粘连。

### 17.50 第四十八轮：玩家信息弹层无重叠阅读链

- 玩家信息弹层从 `500x360` 扩展到 `500x400`，仅重新分配标题、头像、真实姓名、安全身份徽标和关闭操作，不新增虚构玩家字段。
- 标题/头像、头像/姓名、姓名/身份及身份/关闭分别至少保留 11/11/8/9 设计像素，消除旧布局中标题矩形与 96px 头像的 5px 重叠。
- `464x364` 内容区在标题上方保留 13px、80px 关闭操作下方保留 12px；正式头像加载和回退状态必须使用同一几何。
- 首页头像继续作为唯一玩家信息入口；打开时继续读取 Store 的真实显示名，身份文案保持“系统安全身份”，不得加入示例昵称、等级或金币。
- Creator/微信需在三种目标设备检查正式头像加载前后、短/长真实玩家名、弹层遮罩、关闭按下态和上下留白，确认五层内容无重叠和漂移。

### 17.51 第四十九轮：设置弹层平衡内边距

- 设置弹层从 `500x330` 扩展到 `500x350`，现有设置图标/标题、真实音效状态、切换操作和关闭操作作为一组上移 7 设计像素。
- `464x314` 内容区在图标上方保留 19px、关闭操作下方保留 18px，修复旧布局关闭按钮仅距内容底边 1px 的贴边问题。
- 图标/状态、标题/状态、状态/切换及切换/关闭仍至少保留 11/13/10/8px；两个操作继续保持 `80px` 高度。
- 音效状态必须继续读取和写入真实 SettingsStore/AudioService，选中态跟随持久化静音值，不增加假设置或新业务开关。
- Creator/微信需在三种目标设备检查开启、静音、按下、关闭和重新打开状态，确认按钮皮肤/阴影不贴底且状态文字不跳动。

### 17.52 第五十轮：战绩详情八像素分页链

- 战绩详情标题上移 8 设计像素，页码、上一页和下一页作为一个分页组下移 6 设计像素；正文与外层 `560x680` 卡片不移动。
- 标题/正文从 1px 提升到 9px，正文/80px 分页操作从 2px 提升到 8px；标题距卡片顶部 27px，分页操作距底部 12px。
- `500x490` 详情正文继续使用 `SHRINK` 和真实战绩内容，拼词详情仍按每页三轮分页，不通过删减数据换取留白。
- 上一页、下一页、页码和关闭返回列表继续使用现有 HistoryScene 处理器与禁用状态，不修改筛选、历史存储或玩法结算。
- Creator/微信需检查普通 PK、合作和三页拼词详情，确认长文本缩放、页码、禁用态、正式按钮阴影及卡片上下边界没有粘连。

### 17.53 第五十一轮：反馈卡内八像素表单链

- 反馈卡内按提示、隐私说明、内容标题、原生内容输入、联系方式标题、原生联系方式输入和状态带形成七节点阅读链，相邻边界至少保留 8 设计像素。
- 多行内容输入从 `500x202` 收紧到 `500x196` 并上移 2px，联系方式标题上移 2px；`560x500` 外层卡片与其他节点不移动。
- 外部表单/提交/隐私/SafeArea 继续保持 12/8/8px；卡内提示到状态带从旧 8/10/7/5/6/8px 统一为至少 8px。
- 原生 EditBox 继续挂在 SafeArea、保持聚焦环和 300 字计数；校验、联系方式、提交中/成功/失败、清空和隐私处理器均不改变。
- Creator/微信需在三种目标设备及键盘弹起/收起状态检查两层原生输入、计数、状态带和字段标题，确认卡内完整链无粘连或漂移。

### 17.54 第五十二轮：词库卡真实说明与六八八节奏

- 四张词库卡必须创建并显示真实动态副标题，格式为“单词数 · 已解锁/未解锁”；不得因初始空字符串省略副标题节点。
- 每个 `540x96` 词库项的标题从 `y=18` 调整到 `21`，副标题从 `y=-22` 调整到 `-24`，形成顶部 6px、标题/说明 8px、说明/底部 8px 的文字链。
- 动态词库名继续使用 300px `SHRINK` 列，副标题使用独立 300x32 矩形；右侧状态徽标、正式图标和点击范围保持不变。
- 翻页、选中环、已选择/选择/需解锁徽标、金币解锁失败回滚和确认返回行为继续使用现有真实数据与处理器。
- Creator/微信需在三种目标设备检查普通/超长词库名、已解锁/未解锁/选中状态和翻页，确认真实说明可见且标题、徽标、图标之间无粘连。

### 17.55 第五十三轮：学习卡完整八像素阅读链

- `540x360` 学习卡保持位置、尺寸和两枚底部操作不变，只收紧三个动态文字矩形：进度状态 `120x32→120x24`、英文单词 `480x80→480x76`、中文释义 `480x72→480x68`。
- 学习进度标题、状态、进度条、单词、释义和上一页/随机操作从旧 `4/4/6/6/6px` 间距统一为至少 8px；两枚底部操作继续保持 8px 卡内底边距。
- 单词与释义继续使用 `SHRINK`；显示/隐藏释义、查看当前释义、前后翻页、随机、错词标记、学习进度和更换词库均沿用真实 Store 与现有处理器。
- 长屏和真实 `640x960` 运行时树都必须通过完整链断言；Creator/微信需检查普通/超长单词、单行/双行释义、已标错与隐藏释义状态。

### 17.56 第五十四轮：玩法说明规则行八像素节奏

- 六个规则行的标题从 `430x28@y+17` 收紧为 `430x24@y+20`，正文继续使用 `430x36@y-18`，标题/正文间距由 3px 提升为 8px。
- 六枚编号徽标、正文矩形、自适应行距、顶部说明图标/摘要和 `560px` 卡片边界保持不变；最短高度下相邻规则行继续至少分隔 8px。
- 标题和正文继续使用 `SHRINK` 并显示 `HELP_RULES` 的真实内容；返回玩法目录、页头、安全区和主题样式不变。
- 长屏与真实 `640x960` 运行时树都必须校验真实文案、编号/标题列、标题/正文和行间边界；Creator/微信需确认六行目录清晰可扫读。

### 17.57 第五十五轮：玩法卡七八九文字节奏

- 八张 `548x80` 玩法卡的标题从 `236x34@y=18` 收紧为 `236x28@y=19`，说明继续使用 `236x28@y=-17`，形成顶部 7px、标题/说明 8px、说明/底部 9px。
- 每张卡的说明、`双人` 徽标和右侧操作继续形成三个独立列，相邻边界至少 8px；左侧强调条、64px 正式图标、标题列和右侧操作位置不变。
- 第一项“准备体验模式”继续可进入创建配置，另外七项继续明确显示“筹备中”且不可点击；玩法说明入口和返回目录行为不变。
- 长屏与真实 `640x960` 运行时树必须逐卡验证上中下节奏及三列边界；Creator/微信需检查八张卡的扫描层次、禁用态和长标题缩放。

### 17.58 第五十六轮：创建配置双卡八像素边界

- 已选模式卡的 `92x92` 正式图标从 `y=-18` 下移到 `-21`，当前词库卡的 `66x66` 正式图标从 `y=-23` 下移到 `-26`，两者与彩色标题标签的间距由 5px 提升为 8px。
- 动态词库名矩形保持中心和 `300px` 宽度，仅把高度从 54px 收紧到 46px，使标题标签/名称间距由 4px 提升为 8px；长名称继续 `SHRINK`。
- 模式图标/标题、模式标题/说明、词库图标/名称、名称/更换操作的主列边界均至少 8px；卡片、正式图标尺寸、创建/更换/自动准备操作和真实数据绑定不变。
- 长屏与真实 `640x960` 运行时树都必须通过双卡边界断言；Creator/微信需检查普通和超长词库名、模式摘要与正式图标的视觉平衡。

### 17.59 第五十七轮：准备大厅玩家与状态八像素层次

- 当前状态卡的绿色就绪灯和橙色提醒灯从 `y=-10` 下移到 `-13`，与紧凑标题标签的间距由 5px 提升为 8px，并保留 7px 卡内底边距。
- 房主/玩家两张卡必须保持标题标签/头像、标题标签/状态、状态/真实名称、头像/状态和头像/名称至少 8px；现有几何实际提供 16-47px，不移动头像、名称或状态徽标。
- 房间码、当前词库和当前状态三张紧凑卡的标题标签/正文均至少 8px；复制、邀请、后台刷新、准备选择、高亮开始和错误提醒逻辑不变。
- 长屏与真实 `640x960` 运行时树都必须检查等待、单方准备、双方准备和错误状态；Creator/微信需确认绿色/橙色灯、玩家名和按钮层次清晰。

### 17.60 第五十八轮：首页当前词库条八像素层次

- 首页当前词库的真实名称从 `326x34@y=-14` 调整为 `320x34@y=-15`，标题/名称间距由 7px 提升为 8px，名称/“更换”徽标由 5px 提升为 8px。
- 正式词库图标/名称继续至少相隔 8px；紧凑首页的名称/卡片底边保持 8px，长屏则保留更多呼吸空间。
- 动态名称继续使用固定 320px `SHRINK` 列；整条词库条仍是唯一点击区，“更换”徽标不嵌套第二个按钮，真实选中词库和返回刷新逻辑不变。
- 长屏与真实 `640x960` 运行时树都必须检查标题、普通/超长名称、正式图标和徽标边界；Creator/微信需确认 Logo、词库条与主按钮之间层次清晰。

### 17.61 第五十九轮：共用页头八九八与十二像素层次

- 所有共用页头的正式图标板从 `y=14` 上移到 `18`；标题从 `x=62,y=18,w=310` 调整为 `x=66,y=22,w=302`，标题右边界保持不变。
- 标题/分隔线形成 8px、分隔线/副标题形成 9px、图标/副标题形成 8px，图标/标题横向形成 12px；标题上方保留 14px，副标题下方保留 15px。
- 高对比页头背景、86px 返回按钮、正式图标、分隔线、文字描边、安全区和各页面真实标题/副标题不变。
- 共用组件测试与长屏/真实 `640x960` 的 Bank、Study、CoopSelect、Room、History、Feedback、Help 路由均必须通过完整几何；Creator/微信需逐页确认标题不拥挤且返回入口清晰。

### 17.62 第六十轮：共用卡片标题标签八像素图文边界

- 共用 `sectionCard` 的大卡和紧凑卡继续使用原彩色标题标签、正式语义图标和 `22/14px` 标题字号，只重新分配图标后的文字矩形。
- 紧凑标签的图标/标题间距由 3px 提升为 11px，大标签由 1px 提升为 9px；图标左侧与标题右侧均至少保留 8px，避免正式透明 PNG 的可见边缘贴字。
- 卡片尺寸、标签高度、图标槽位置、主题颜色、描边、真实文案、按钮和数据处理器不变；长标题继续使用 `SHRINK`，不得通过截断或假文案换取间距。
- 基础组件测试必须覆盖两种标签尺寸，全路由可见 UI 合约必须检查实际挂载的每个带正式图标标题标签；Creator/微信需确认词库状态、房间码、当前词库、当前状态、战绩摘要等卡片的图文层次稳定。

### 17.63 第六十一轮：紧凑标题标签四像素上下留白

- `24px` 紧凑彩色标题标签内的正式图标槽从 `18x18` 收紧到 `16x16`，标题矩形高度从 20px 收紧到 16px；字号继续为 14px 并使用 `SHRINK`。
- 图标和标题的上下内边距由 3/2px 统一为 4px；大标签的图标/标题上下内边距继续保持 10/8px，第六十轮的横向八像素边界继续有效。
- 房间码、当前词库、当前状态、词库状态和战绩摘要等紧凑卡共用该规格；卡片、标签、标题中心、主题色、实际文案和页面处理器不变。
- 基础组件与全路由可见 UI 合约必须同时检查标题标签的水平和垂直边界；Creator/微信需确认缩小后的正式图标仍清晰，并且不贴近圆角描边。

### 17.64 第六十二轮：共用按钮回退边框状态一致性

- 所有由 `RuntimeButtonVisual` 管理的程序化按钮必须在每次状态刷新后重绘原配置的 2px 圆角边框，不得因 `Graphics.clear()` 只剩填充和阴影。
- 普通按钮及正式九宫格图片加载失败回退按钮在正常、按下、禁用和选中状态均保持边缘层次；正式皮肤成功加载时继续关闭程序化背景和高光，不允许出现双边框。
- 边框使用按钮创建时已有的主题描边色、线宽和主矩形圆角，不新增节点、不修改热区、文字、图标、按压位移或处理器；隐私门禁等未使用该组件的旧按钮不纳入本轮。
- 基础测试必须记录每次刷新后的描边，全路由可见 UI 合约必须检查所有实际挂载的共用按钮；Creator/微信需检查普通、按下、禁用、选中及断网图片失败状态的边缘清晰度。

### 17.65 第六十三轮：紧凑图标操作八像素横向边界

- 宽度小于 120px 的 `actionButton` 正式图标槽从 28px 收紧到 24px，图标/标题间距从 6px 提升到 8px；图标左侧固定 12px，标题右侧至少 8px。
- `92px` 战绩详情、`112px` 复制/邀请和 `118px` 更换词库等紧凑操作必须同时满足图标左边、图文之间、标题右边至少 8px，不得只检查节点没有相交。
- 按钮尺寸、圆角、九宫格皮肤、24px 高清图标清晰度、标题字号、点击热区、按压位移、禁用态及真实处理器不变。
- 基础测试必须覆盖最窄 92px 操作，全路由可见 UI 合约必须检查每个实际挂载的图标/标题对；Creator/微信需确认缩小后的正式图标与 27px 紧凑标题视觉平衡。

### 17.66 第六十四轮：状态徽标八四像素文字内边距

- 常规 `14/16px` 状态徽标的文字矩形从 28px 高收紧到 26px，在不变的 `34px` 胶囊内形成 8px 左右、4px 上下内边距。
- 首页金币加号使用 26px 大符号，继续保留 28px 文字矩形，避免在 `34x34` 圆徽标中被额外缩小；大符号仍遵守既有四像素工具栏边界。
- 双人、已准备、等待中、玩家身份、词库更换、帮助编号等常规徽标共用新规格；胶囊尺寸、颜色、描边、位置、真实状态和处理器不变。
- 基础测试必须覆盖常规和大号两类徽标，全路由可见 UI 合约必须检查实际 34px 胶囊；Creator/微信需确认短文字垂直居中且大号加号仍清晰。

### 17.67 第六十五轮：纯文字按钮与选中线垂直层次

- 共用 `80px` 纯文字按钮的 Label 矩形从 72px 收紧为 48px，文字中心、字号、按钮尺寸、圆角、点击热区、按压状态和处理器不变。
- 玩法目录的“立即体验/筹备中”因此获得 16px 上下留白；战绩筛选文字与 6px 选中线之间形成 4px 间隔，选中线与按钮底边保留 6px。
- 不得用移动文字或缩小点击区规避重叠；五个战绩筛选与八个玩法操作必须共用同一内部规格，禁用项仍显示原状态。
- 基础测试必须冻结 80/48px 关系，运行时测试必须覆盖全部玩法操作和真实选中筛选；Creator/微信需确认短文字垂直居中且选中线不再穿入文字区域。

### 17.68 第六十六轮：副标题操作按钮自适应垂直节奏

- 带副标题的共用 `actionButton` 不再按 `-22/-26/-32px` 三档突变，而是按按钮高度的四分之一确定副标题中心；按钮尺寸、文字矩形高度、字号和水平列不变。
- `80px` 操作保留 8px 底边距，`90-95px` 操作通过 18px 标题中心保持至少 8px 标题/副标题间距，`96-146px` 操作逐步增加到约 10-15px 底部留白。
- 首页正式九宫格主按钮继续要求副标题至少离开皮肤边缘 10px；词库、练习、创建、加入、房间、反馈等短按钮至少保留 8px，不允许文字贴住圆角或下描边。
- 基础测试必须冻结标题间距和底边距，全路由可见 UI 合约必须检查所有实际副标题操作；Creator/微信需确认短按钮不拥挤、长主按钮仍有更宽松的视觉重心。

### 17.69 第六十七轮：首页玩家与金币信息底板对齐

- 首页顶栏的真实玩家信息胶囊从 62px 增高为 64px，与相邻金币胶囊同高并共享同一中心轴；头像和设置仍使用透明 80px 点击区。
- 玩家名和“系统安全身份”继续使用真实 Store 数据/固定安全文案及原字号、原中心位置；新框体形成 5px 顶部、5px 行间、6px 底部节奏。
- 头像/玩家胶囊和玩家/金币胶囊继续分别至少相隔 8px，玩家胶囊不增加 Button，头像仍是唯一玩家信息入口，金币条仍是单一词库/金币入口。
- 长屏与 `640x960` 最短屏测试必须冻结两胶囊同高同轴、真实名称 `SHRINK` 和文字内边距；Creator/微信需确认顶部四组元素处于一条稳定视觉基线。

### 17.70 第六十八轮：正方形图标控件统一圆形视觉

- 共用 `iconButton` 的程序化底板半径从固定 18px 改为控件边长的一半；所有正方形图标操作因此使用圆形视觉，而不是随尺寸增大而变得方正。
- 86px 页头返回键、80px 翻页键、首页头像/设置及其他图标按钮共用该规则；图标槽尺寸、正式资源、文字 fallback、颜色、描边和状态反馈不变。
- 圆形仅改变程序化视觉轮廓，节点仍保留原正方形点击热区，不得缩小触控门槛或移动页面轴线；透明头像/设置在正式资源下继续保持无重复底板。
- 基础测试必须冻结 64/86px 半径，全路由可见 UI 合约必须检查每个实际正方形图标控件；Creator/微信需确认返回、分页和顶栏图标形成一致的圆形交互语言。

### 17.71 第六十九轮：大型圆形控件符号可读性

- `80/86px` 圆形图标控件内的文字 fallback 上限从 30px 提升到 34px，继续位于不变的 48px 图标槽中，符号矩形四周至少保留 7px。
- 共用返回、词库/战绩/详情翻页、详情关闭和玩法问号均使用新字号；`64px` 小控件继续按 `size*0.46` 保持约 29px，不被强行放大。
- 有正式 Sprite 的头像、设置继续隐藏 fallback；本轮不得用文字符号覆盖正式图片，也不修改圆形底板、点击热区、按压位移、禁用态或处理器。
- 基础测试必须冻结页头返回符号字号，全路由合约必须检查所有实际可见的大型 fallback；Creator/微信需确认箭头和问号清晰但不顶住圆形边缘。

### 17.72 第七十轮：按钮高光状态一致性

- 共用按钮顶部的程序化高光只在按钮可交互、未按下且未使用正式九宫格皮肤时显示；按下和禁用状态不得继续保留静态亮线。
- 松开可交互的程序化回退按钮后恢复高光；正式皮肤成功加载后继续隐藏程序化背景和高光，不因松开或刷新重新出现。
- 普通操作和圆形图标控件共用该状态规则；按钮尺寸、圆角、正方形触控热区、内容按压位移、颜色、图标、文字、处理器和路由均不变。
- 基础测试必须覆盖回退操作与圆形图标的正常、按下、松开、禁用和正式皮肤状态；Creator/微信需确认禁用按钮不再显得可点击，按压反馈层次自然。

### 17.73 第七十一轮：透明图标控件去除悬空高光

- `transparent` 图标控件关闭程序化底板时，必须同步关闭底板顶部高光，不得在正式图标或文字 fallback 上方留下孤立亮线。
- 首页头像、设置和玩法目录的说明问号共用该规则；普通 `card` 与高对比 `join` 图标按钮继续保留第七十轮的正常、按下和禁用高光反馈。
- 透明控件的 80px 正方形热区、圆形视觉基准、正式 Sprite、fallback 字号、按压内容位移、处理器和路由均不变；本轮不新增底板或装饰节点。
- 基础测试必须覆盖透明控件正常/按下/松开，运行时测试必须检查三个真实入口；Creator/微信需确认图标边缘干净且不再出现悬浮白线。

### 17.74 第七十二轮：圆形按钮高光弦宽约束

- 圆形 `iconButton` 顶部 4px 高光宽度使用 `size/2-4`，不再沿用矩形按钮的 `size-16`；64/80/86px 控件分别显示 28/36/39px 高光。
- 高光位于圆顶下方的窄弦区域，完整厚度必须落在圆弧内，不得穿过描边或悬在底板之外；透明控件仍按第七十一轮完全隐藏高光。
- 圆形半径、正方形点击热区、图标槽、正式 Sprite、文字 fallback、按压位移、禁用反馈和处理器均不变；本轮只改变内部反光线宽度。
- 基础测试必须冻结 64/86px 两档，全路由合约必须检查每个正方形图标控件；Creator/微信需确认返回、分页等圆形按钮的高光短而居中、边缘干净。

### 17.75 第七十三轮：玩法说明入口圆形可点击层次

- 玩法目录右上角的 `?` 使用 80px 高对比 `join` 圆形底板，不再作为无底板的孤立字符；它与左侧返回键共同形成明确的页头操作层。
- 控件保留 `x=229,y=14`、80px 正方形热区、40px 圆形半径和 36px 问号槽，既有标题/副标题预留边界及进入玩法说明、返回目录逻辑不变。
- 首页头像和设置仍保持透明，因为正式圆形图片已经提供完整轮廓；不得为了机械统一重复叠加程序化底板。
- 运行时测试必须冻结问号底板、高光、圆形半径、文字边界和路由；Creator/微信需确认左右操作视觉平衡且问号明显可点击。

### 17.76 第七十四轮：强调卡片色条与双边框分层

- 共用 `accentCard` 左侧色条从 `6 x (height-24)` 收紧为 `4 x (height-36)`，并由卡片左边缘内缩 8px 调整为 3px；色条必须落在外框与内描边之间的独立通道，不再覆盖内描边形成局部粗线。
- 色条右边缘与内描边路径保留 1px 净距。80px 玩法目录行中色条与 64px 正式图标的间距由 5px 提升到 11px；加入房间、结算、战绩、反馈和说明等高卡片沿用同一规则。
- 卡片外框、圆角、内描边、主题色、内容位置和点击行为全部不变；不得通过移动图标、文字或缩小热区为色条腾空间。
- 基础组件测试必须冻结色条尺寸和内描边净距，真实玩法目录与战绩行必须同时检查图标列边界；Creator/微信需确认色条轻巧、连续且不与双边框粘连。

### 17.77 第七十五轮：卡片顶部高光与内描边分层

- 通过 `card()` 创建的卡片顶部 4px 高光使用独立的 10px 顶部内缩，不再复用按钮的 7px 内缩；高光上边缘与横向内描边路径保留 3px 净距，避免两层重合成局部粗亮线。
- 普通文字按钮、图文操作按钮和圆形图标按钮继续沿用各自现有高光位置及正常、按下、禁用、正式皮肤状态，不得为了卡片分层改变按钮反馈。
- 卡片尺寸、圆角、内描边、顶部高光宽度和透明度、左侧强调色条、标题标签、正文及业务操作全部不变；本轮只调整卡片高光的纵向层次。
- 基础组件测试必须冻结 3px 净距，全路由可见 UI 合约必须检查每个带内描边的实际卡片；Creator/微信需确认卡片顶部边缘清晰但不过亮。

### 17.78 第七十六轮：玩法目录规则按钮文字安全区

- 玩法目录页头在保留右侧 80px 规则圆形按钮的前提下，将标题/副标题容器从 `250/348` 收紧为 `230/334`；两条文字矩形的右边缘均必须停在按钮左侧至少 8px。
- 此安全区按标题、副标题节点的真实位置和宽度计算，不得继续使用忽略节点中心偏移的近似常量；后续调整页头中心时必须同步保持该边界。
- 当前文案、居中对齐、字号、描边、分隔线、规则按钮位置/热区、高光和进入说明页的行为全部不变；其他无右侧操作的共用页头保持原宽度。
- 运行时测试必须同时验证标题和副标题的 8px 净距；Creator/微信需确认问号入口醒目，但不会压迫或遮挡页头文字。

### 17.79 第七十七轮：玩法目录页头双操作同轴

- 玩法目录右侧 80px 规则圆形按钮由局部 `y=14` 调整到 `y=0`，与左侧 86px 返回键共享同一水平中心轴，避免左右入口一高一低。
- 规则按钮必须完整落在 96px 页头底板内，上下各保留 8px；不得像旧位置一样越出底板顶部 6px。下移后与第一张玩法卡仍至少保留 22px。
- 标题/副标题的 8px 横向安全区、圆形半径、问号、高光、正方形热区和进入说明页的行为全部不变；本轮只修正纵向视觉平衡。
- 运行时测试必须冻结左右操作同轴和上下对称留白；Creator/微信需确认页头左右重量均衡且规则入口没有贴近第一张卡片。

### 17.80 第七十八轮：筹备中玩法分类色层次

- 玩法目录七个未开放条目的卡片左侧 4px 色条不再统一使用 `surface` 米白色，而按模式类别使用橙、紫、黄、蓝、绿等现有主题色；八行整体至少形成五组可辨识色彩。
- 分类色只作用于卡片强调色条。右侧“筹备中”按钮继续保持 `interactable=false`、统一禁用填充、禁用文字/图标权重和点击守卫，不得因彩色色条产生可进入的假象。
- 卡片底色、正式图标、标题/副标题、双人徽标、按钮位置和未来模式数据保持不变；不新增临时位图或模拟开放状态。
- 运行时测试必须同时冻结色彩多样性和七个按钮的真实禁用状态；Creator/微信需确认目录更有层次，但未开放项仍明显不可操作。

### 17.81 第七十九轮：玩法图标与强调色语义统一

- 分类色条必须与每行既有正式图标语义一致：准备体验/PK 使用绿色 Practice，魔法/Boss 使用紫色 Catalog，宝物使用黄色 History，搭桥使用橙色 Create，造塔使用蓝色 Bank，合作塔防使用蓝色 Join。
- 不得只检查颜色数量；自动化必须将完整八行主题 Token 序列与当前主题配置逐项比较，避免颜色仍丰富但模式之间发生错配。
- 七个“筹备中”按钮继续保持真实禁用；图标资源、标题、副标题、双人徽标、卡片尺寸和未来开放逻辑不变。
- Creator/微信需确认色条与对应图标形成同一视觉语义，且相邻的蓝色 Bank/Join 仍能依靠图标和文案明确区分。

### 17.82 第八十轮：目录按钮外观与热区分层

- 玩法目录八行右侧按钮的可见背景、高光、圆角和文字容器使用 `126x64`，在 80px 卡片内上下各保留 8px，不得继续用等高按钮覆盖卡片边框和顶部高光。
- 按钮右边缘与卡片外框至少保留 8px，左边缘与“双人”徽标至少保留 8px；标题、副标题、图标、强调色条及卡片尺寸不变。
- 可见高度收紧不能缩小触控目标。按钮节点继续使用 `126x80` 热区，仅 `RuntimeButtonVisual` 使用 64px 高度；在 360px 宽设备缩放后仍须满足至少 44px 触控高度。
- 七个“筹备中”按钮的禁用状态和首行“立即体验”的路由不变。运行时测试必须分别冻结视觉几何、卡片留白和触控几何，Creator/微信需确认按钮像卡片内操作而不是附着在卡片边框上。

### 17.83 第八十一轮：大厅工具卡底部安全区

- 房间码卡中的房间码、复制和邀请三列必须共享同一纵向中心；两个 80px 操作按钮不得越出 104px 卡片，按钮本体距底框保留 6px，阴影也不得明显穿出圆角边界。
- `当前词库`和`当前状态`两张紧凑卡片的单行正文分别使用 28px、26px 高度，并统一上移到 `y=-15`；正文与顶部页签保留 12px，距底框保留 7px。
- 卡片、页签、字号、横向列宽、图标、状态指示器和热区不变；复制、邀请、后台刷新、准备及开始游戏逻辑均不属于本轮修改范围。
- 运行时测试必须在最小和长屏房间布局冻结三列基线及 6/7px 底部净距；Creator/微信需确认工具行不再下沉，三张卡片的正文视觉重心一致。

### 17.84 第八十二轮：词库状态图文同轴

- 词库页顶部 80px“词库状态”卡中的独立金币图标和动态状态正文统一使用 `y=-13`，不得继续出现图标比文字低 4px 的双基线。
- 动态正文使用 36px 单行容器，距 24px 紧凑页签至少 8px，距卡片底部至少 8px；当前几何分别为 10px 和 9px。金币图标对应净距为 12px 和 11px。
- 页签、卡片、图标尺寸、正文字号和横向列宽不变；真实金币数量、词库解锁/选择、翻页和返回行为不属于本轮修改范围。
- 运行时测试必须冻结图文同轴及上下安全区；Creator/微信需确认状态卡内容垂直居中、底部不拥挤，并与下方词库条目保持清晰层级。

### 17.85 第八十三轮：战绩详情按钮外观与热区分层

- 战绩记录行右侧 92px 宽“详情”按钮的可见背景、高光、图标和文字使用 64px 高度；最短屏 86px 卡片上下各保留 11px，长屏 102px 卡片上下各保留 19px。
- 可见外观收紧不能缩小热区。详情按钮节点继续使用 80px 高度，使 360px 宽设备缩放后仍满足至少 44px 触控门槛。
- 标题、时间/模式信息、分数列、正式 History 图标、详情路由和分页逻辑不变；本轮只让紧凑工具操作从卡片边框中独立出来。
- 运行时测试必须分别冻结 64px 外观、80px 热区和至少 8px 卡片净距；Creator/微信需确认详情按钮不再贴框，同时仍清晰可点。

### 17.86 第八十四轮：战绩摘要双卡正文同轴

- “最近记录”和“最佳成绩”两张 110px 摘要卡的正文统一使用 `y=-22` 中心轴，不得继续出现双行摘要比单行分数低 2px 的错位。
- 最近记录使用 46px 容器承载模式名称和本地化时间两行文字，距底框保留 10px；最佳成绩维持 44px 容器和 11px 底距，两者均不得低于 8px。
- 页签、图标、卡片尺寸、正文字号和横向布局不变；真实模式名、时间、最佳分数、空状态和筛选逻辑不属于本轮修改范围。
- 运行时测试必须在长屏和最短屏冻结双卡基线及底部安全区；Creator/微信需确认两张摘要卡视觉重心一致，时间第二行不会贴住圆角。

### 17.87 第八十五轮：战绩空状态三层节奏

- 战绩空状态卡的正文区 History 图标、主文案和说明文字形成独立三层；图标到主文案至少 8px，主文案到说明至少 8px，不能继续沿用 6px/5px 的拥挤间距。
- 主文案使用 `y=-15`、38px 容器承载 26px 字号和 31px 行高；当前两段净距为 8px/9px，说明文字距底框维持 37px。
- 测试和实现必须区分页签内 History 图标与卡片直属正文图标。卡片、页签、图标、文字内容、空/有记录切换和筛选行为不变。
- Creator/微信需在真实无记录状态确认主文案不缩字、图文层级清楚，说明文字没有贴近主文案或卡片底框。

### 17.88 第八十六轮：反馈状态带内外安全区

- 反馈表单底部状态带使用 `y=-225`、34px 高度，保持原顶边位置并使状态带距联系方式输入框和卡片底框均为 8px。
- 动态状态文字与状态带同轴，使用 26px 容器承载 18px 字号和 22px 行高，上下各保留 4px；不得继续用 34px 文字容器只留下 1px 内距。
- 状态带宽度、颜色、边框和消息内容不变；空内容、校验失败、可提交、提交中、成功、失败及离开页面后的生命周期逻辑不属于本轮修改范围。
- 运行时测试必须覆盖状态带/文字同轴、内外安全区及完整提交状态；Creator/微信需确认短消息垂直居中，边框不会压住文字或贴近卡片圆角。

### 17.89 第八十七轮：反馈顶部图文底边对齐

- 反馈卡顶部 48px 正式图标使用 `y=206`，与 `y=202`、40px 高的提示文字共享 `y=182` 底边；不得继续让图标向下多伸 8px。
- 图标和提示文字分别距下一行隐私说明 8px，图标顶部距卡片顶框保留 20px；图标尺寸、提示字号和横向图文列不变。
- 单行与多行 EditBox 锚点继续遵循现有 Cocos 适配约束，本轮不得借视觉调整修改原生输入层。提示、隐私文案、校验和提交逻辑不变。
- 运行时测试必须冻结图文底边和两条隐私说明安全区；Creator/微信需确认顶部图标不再下沉，第一行和第二行之间有清楚层次。

### 17.90 第八十八轮：战绩筛选文字与选中标记分层

- 战绩页“全部、PK、合作、拼词、其他”五个 80px 筛选按钮保持原触控区域，48px 文字容器由 `y=0` 上移到 `y=4`，按钮顶部到文字保持 12px，文字到底部选中标记保持 8px。
- `58x6` 选中标记继续位于 `y=-31`，距按钮底部保持 6px；不得通过缩小标记、触控节点或文字字号换取间距。
- 当前筛选的标记和选中外观继续同步切换，“其他”继续保持不可点击。筛选动作、记录数据、分页、路由和禁用反馈均不改变。
- 运行时测试必须同时冻结顶部、文字/标记和底部三段边界；Creator/微信需确认五个按钮文字重心一致，选中标记清晰但不粘连文字或底框。

### 17.91 第八十九轮：分页文字与圆形按钮水平安全区

- 词库页、战绩列表和战绩详情的居中页码文字容器统一由 120px 收紧到 112px，继续保持 44px 高度、原字号、中心轴和 `SHRINK` 规则。
- 页码文字容器与左右两个 80px 圆形翻页按钮之间分别保留 9px 净距，不得继续沿用 5px 边界，也不得通过缩小按钮热区换取空间。
- 前后翻页按钮位置、圆形外观、禁用状态、页数计算、记录内容和点击处理均不改变；本轮只统一三个赛前分页组件的横向视觉节奏。
- 运行时测试必须冻结三组分页控件的左右边界；Creator/微信需确认页码居中、箭头与页码互不粘连，并在长页数和禁用箭头状态下仍保持清楚层次。

### 17.92 第九十轮：帮助规则编号与文字列安全区

- 帮助页六条规则的标题和说明文字列统一由 `x=22` 右移到 `x=27`，编号徽标右边到文字列左边从 3px 提升到 8px。
- 标题和说明继续使用同一水平中心轴及 430px 宽度，卡片右边仍保留 38px；不得通过压缩真实规则文案、缩小字号或缩小编号徽标换取图文间距。
- 既有 8px 标题/说明垂直间距、规则行间距、摘要图文区、卡片尺寸、编号颜色和返回玩法目录逻辑均不改变。
- 运行时测试必须在长屏和最短屏冻结编号/文字边界及标题/说明同轴；Creator/微信需确认六行图文列整齐，长文不贴近编号或卡片右边。

### 17.93 第九十一轮：创建说明与大厅紧凑卡安全区

- 创建配置页“开始条件”卡的说明文字列由 `x=28` 调整到 `x=29`，52px 正式练习图标与文字列之间由 7px 提升到 8px，卡片右侧仍保留 30px。
- 准备大厅“当前词库”和“当前状态”文字由 `y=-15` 上移到 `y=-14`，两张紧凑卡的正文底边由 7px 提升到 8px。
- 已准备/需关注状态点保持 `y=-13` 中心轴并由 `30x30` 收紧到 `28x28`，同时保留页签间距、8px 底边以及与状态文字相同的底边；状态颜色和显隐逻辑不变。
- 运行时测试必须在长屏和最短屏冻结创建说明图文边界，以及词库正文、状态正文和两类状态点的底边；Creator/微信需确认紧凑卡内容不贴底、状态点不过大。

### 17.94 第九十二轮：首页金币工具三列安全区

- 首页顶部真实金币数值节点由 `x=17,w=82` 调整为 `x=15,w=76`，34px 加号徽标由 `x=82` 左移到 `x=78`；金币图标、数值和加号仍位于同一工具胶囊内。
- 金币图标到数值、数值到加号、加号到按钮右边三处边界均不得小于 8px，不再保留原有约 16/7/4px 的失衡分配。
- 数值继续读取真实 `WordBankStore` 金币并使用 `SHRINK`，必须覆盖九位数值；加号仍只是整块金币入口的视觉提示，不新增嵌套按钮或充值逻辑。
- 运行时测试必须冻结三列边界、九位真实数值和整块跳转词库行为；Creator/微信需确认普通值与长值都不会贴住加号或右侧圆角。

### 17.95 第九十三轮：大厅玩家卡双列视觉中心

- 准备大厅两张玩家卡中的 72px 正式头像由 `y=-5` 下移到 `y=-27`，与右侧“已准备/等待中”徽标和真实安全玩家名称组成的整体内容块共享同一视觉中心。
- 头像不再比右侧内容高 22px；头像尺寸、横向位置、角色页签、状态徽标和名称文字框均保持不变，卡片底部仍保留充足安全区。
- 房主/玩家归属、真实名称缩放、准备/等待切换、房间轮询和开始条件不属于本轮修改范围，不得因视觉居中改变状态计算。
- 运行时测试必须在长屏和最短屏冻结头像与状态/名称组合中心，并继续检查页签、底框和两列横向边界；Creator/微信需确认左右视觉重量均衡。

### 17.96 第九十四轮：首页玩家身份文字节奏

- 首页 64px 玩家信息胶囊中的真实昵称框由 `y=12,h=30` 调整为 `y=13,h=26`，安全身份框继续使用 `y=-17,h=18`。
- 两行文字形成精确 `6/8/6px` 顶部、行间和底部节奏，替代原有 `5/5/6px`；使用整数像素坐标，避免为追求等分引入半像素文字模糊。
- 21px 昵称的 25px 行高必须完整放入 26px 文字框，13px 安全身份的 17px 行高继续放入 18px；真实长名称仍用 `SHRINK`，不得缩小固定字号或伪造昵称。
- 运行时测试必须在长屏、最短屏和长安全名称状态冻结三段边界；Creator/微信需确认两行文字层级清楚、上下留白均衡。

### 17.97 第九十五轮：设置弹层图标与标题分列

- 首页设置弹层的 330px 标题框由 `x=28` 右移到 `x=49`，与 `x=-150`、52px 的正式设置图标由原先横向重叠 13px 改为精确 8px 净距。
- 标题框右边距弹层内容边界仍保留 18px；图标和标题继续共享 `y=112` 中心轴，字号、文字框高度和正式图标尺寸均不改变。
- 音效状态文案、开启/静音操作、选中反馈、设置持久化和关闭按钮不属于本轮修改范围，不得因标题分列改变交互。
- 运行时测试必须冻结图标/标题和标题/右边两处边界，并继续覆盖设置弹层完整纵向链与音效状态；Creator/微信需确认标题行不再粘连。

### 17.98 第九十六轮：大厅房间码工具卡底部安全区

- 房间码正文、复制和邀请三列统一由 `y=-6` 上移到 `y=-4`，继续共享同一水平轴，避免工具卡内容整体下沉。
- 房间码正文框由 48px 收紧为 44px，保持原顶边、30px 字号和 216px 宽度；复制、邀请按钮节点继续保留 `112x80` 热区。
- 两个操作按钮距 104px 卡片底框的净距由 6px 提升到 8px；房间码、卡片、页签、横向列宽、图标及按钮可见样式不变。
- 复制房间码、邀请好友、房间数据及协议不属于本轮修改范围。运行时测试冻结新的八像素边界；Creator/微信需确认三列对齐且按钮阴影不贴底框。

### 17.99 第九十七轮：大厅玩家卡上下节奏统一

- 大厅两张玩家卡高度由 220px 收紧为 218px，继续使用 `y=2` 中心轴；头像、已准备/等待中徽标、真实安全名称和左右列位置全部不变。
- “当前词库”卡底边到玩家卡顶边、玩家卡底边到“当前状态”卡顶边由 7px 同时提升到 8px，与赛前页面的基础间距节奏一致。
- 卡片高度收紧后仍保留充足的标题页签、头像、徽标、姓名和底框安全区，不得通过移动真实内容或缩小触控目标换取外部间距。
- 房主/玩家归属、准备状态、轮询和开始条件不属于本轮修改范围。运行时测试必须冻结 218px 卡高及最短大厅完整八像素链；Creator/微信需确认三层卡片边界均匀。

### 17.100 第九十八轮：词库页最短屏主链统一

- 四个词库条目由 96px 收紧为 88px；在 360px 宽目标设备缩放后仍约为 49.5px，高于 44px 触控门槛，不得再通过缩小热区换取间距。
- 条目标题由 `y=21,h=42` 调整为 `y=19,h=38`，副标题由 `y=-24,h=32` 调整为 `y=-22,h=28`，继续保留 `6/8/8px` 顶部、行间和底部节奏。
- 状态卡和第一条词库轴线分别下移 4px，分页轴线下移 2px，使最短屏页头、状态卡、四条词库和分页之间全部至少保留 8px。
- 词库名称、真实单词数、选中/解锁徽标、翻页、解锁和返回逻辑不变。运行时测试必须冻结 88px 热区、内部文字边界和完整八像素主链；Creator/微信需确认列表更通透且文字不缩挤。

### 17.101 第九十九轮：首页主操作链八像素节奏

- 最短屏首页的副标题、当前词库、创建、加入、学习和扩展入口轴线依次调整为紧凑偏移 `67/135/229/329/423/511`，相邻主内容全部至少保留 8px，底部扩展入口到隐私/反馈区保留 9px。
- 最短屏创建和加入入口由 96px 收紧为 92px，在 360px 宽设备上仍约为 51.8px，高于 44px 触控门槛并继续大于 80px 辅助入口。
- 长屏当前词库、学习和扩展入口偏移调整为 `174/587/714`，补齐副标题/词库及下方双列入口的八像素边界；长屏主按钮高度和正式皮肤不变。
- 标题、副标题、图标、角色显隐、当前词库数据、隐私/反馈入口和所有路由逻辑不变。运行时测试必须分别冻结长屏与最短屏完整主链；Creator/微信需确认层级仍突出创建/加入且卡片不再粘连。

### 17.102 第一百轮：玩法目录卡片外观与热区分层

- 八张玩法卡片外观由 80px 收紧为 78px，中心行距由 84px 调整为 86px，可见卡片之间由 4px 提升到 8px；每行布局节点继续保持 80px。
- 正式/兜底图标及右侧按钮外观由 64px 收紧为 62px，在 78px 卡片内上下各保留 8px；右侧操作节点继续保持 80px 热区，360px 宽设备上仍约为 45px。
- 标题与副标题分别调整到 `y=18/-18`，在可见卡片内形成 `7/8/7px` 顶部、行间和底部节奏；“双人”徽标调整到 `y=-14`，距底边保留 8px。
- 分类色条、正式图标语义、玩法文案、帮助入口、首行真实路由和其余七个禁用按钮均不改变。运行时测试必须分别冻结 78px 外观、80px 热区、62px 内部视觉和八像素行距；Creator/微信需确认目录更通透且禁用项仍明确。

### 17.103 第一百零一轮：首页底部辅助区安全边界

- 首页隐私保护和问题反馈按钮由 84px 收紧为 80px，中心轴由 `safeBottom+46` 调整为 `safeBottom+48`；按钮顶边保持不变，因此与上一排扩展入口的 9px 边界不改变。
- 两个按钮距安全区底边由 4px 提升到精确 8px；在 360px 宽设备上仍约为 45px，高于 44px 触控门槛。
- 正式按钮皮肤、图标、文字、左右列宽及点击区域保持一致；隐私打开中的禁用/恢复状态和反馈页面跳转逻辑不变。
- 运行时测试必须在长屏和最短屏冻结 80px 高度及八像素底部边界；Creator/微信需确认底部按钮不贴屏幕边缘且顶部节奏未被破坏。

### 17.104 第一百零二轮：赛前练习外观与热区分层

- 赛前练习页“当前词库”“查看当前释义”“显示/隐藏后续单词中文”和“下一个”四个操作使用 72px 可见外观，同时保留 80px 布局与点击节点；在 360px 宽目标设备上继续满足不低于 44px 的实际触控门槛。
- 页头、当前词库、学习卡、释义操作、中文开关、下一词和安全区底边之间的可见净距在长屏与最短屏均不得小于 8px。间距计算以 `RuntimeButtonVisual` 的可见几何为准，不使用透明热区挤压视觉节奏。
- `PreGameUi.actionButton` 统一负责不足 80px 外观的最小点击热区，战绩详情原有 64px 外观/80px 热区不得再依赖页面局部覆写。按钮皮肤、图标、文字排版和点击位置仍以传入的可见尺寸为准。
- 词库跳转、释义显示、中文开关、上一词、随机、下一词和错词标记逻辑均不变。运行时测试必须同时冻结四个操作的 72/80px 双层几何、五段八像素边界和最终安全区净距；Creator/微信需确认透明热区不产生视觉粘连或误触遮挡。

### 17.105 第一百零三轮：赛前练习双列操作等高

- 同处一行的“查看当前释义”和“标记错词”必须共享 `y` 轴、252px 宽度和 72px 可见高度；不得保留左侧 72px、右侧 80px 的不对称外观。
- 两个按钮的布局与点击节点均保持 80px 高度，在 360px 宽目标设备上继续满足触控门槛；透明热区只用于输入，不得改变皮肤、边框、图标或文字的可见边界。
- 错词按钮继续使用战绩强调色和选中环，点击后必须保留“已在错题库”文案及选中状态。释义显示、错词计数、持久化和其余练习操作逻辑均不变。
- 运行时测试必须在长屏和最短屏同时冻结两个按钮的同轴、等高、视觉/热区分层及真实选中转换；Creator/微信需确认双列视觉重量一致，阴影和边框不再一高一低。

### 17.106 第一百零四轮：战绩列表完整纵向节奏

- 战绩页“全部、PK、合作、拼词、其他”五个筛选使用 56px 可见按钮和 80px 布局/点击节点；选中标记、筛选文字和禁用状态必须跟随 56px 外观排布，不得继续用 80px 色块与摘要卡发生可见重叠。
- 页头、筛选、左右摘要卡、列表标题、四条记录、分页按钮和安全区底边组成一条完整纵向链路。长屏与最短屏的每一处可见净距均不得小于 8px，间距计算使用 `RuntimeButtonVisual` 的可见几何。
- 摘要卡保持 272x110px，记录卡保持 86-102px，分页按钮保持 80px；不得通过缩小记录内容、详情热区、字体或分页控件换取间距。共享普通按钮统一负责不足 80px 外观的最小热区，玩法目录不得保留页面局部覆写。
- 筛选选中、真实记录、空状态、详情、分页边界和返回行为均不变。运行时测试必须同时冻结五个筛选的 56/80px 分层和长短屏完整链路；Creator/微信需确认筛选不再压住摘要卡，记录列表上下节奏连续且分页不贴底。

### 17.107 第一百零五轮：反馈与帮助页头边界统一

- 反馈页不再随长屏额外上移表单；500px 表单卡在最短屏下移 4px，提交和隐私操作保持原绝对轴线。页头、表单、提交、隐私操作和安全区底边之间统一至少保留 8px。
- 反馈输入框、提示、隐私说明、状态带、94px 提交按钮、80px 隐私按钮及所有提交状态不变；不得通过压缩原生输入区域或状态反馈换取页头间距。
- 帮助页规则卡整体下移 4px，页头/规则卡间距由 4px 提升到 8px；卡片高度仍按 `min(820, safeHeight-140)` 计算，最短屏底部继续保留 16px，六条规则的编号、标题、正文和行距不变。
- 运行时测试必须分别冻结反馈页和帮助页的长屏/最短屏外部链路，并继续覆盖反馈生命周期与帮助规则内容；Creator/微信需确认两页主体不再贴住页头，底部也没有压屏或漂浮失衡。

### 17.108 第一百零六轮：房间页头与首卡八像素边界

- 创建配置和准备大厅的内容组统一下移 3px，使页头底边到首张“已选模式”或“房间码”卡顶边由 5px 提升为精确 8px；加入表单继续保留原有更宽松的页头边界。
- 三种房间入口的卡片尺寸、内部图文和按钮热区不变，最短屏底部仍须保留至少 8px；不得通过压缩主卡、操作按钮或真实房间状态换取页头间距。
- 运行时测试必须在长屏和最短屏分别冻结创建、加入、准备大厅的页头/首卡边界，并继续覆盖内部八像素链路和底部安全区；Creator/微信需确认页面主体与页头分离清晰且底部操作不贴边。

### 17.109 第一百零七轮：词库卡片文字等距节奏

- 四个 88px 词库卡的标题矩形由 `y=19,h=38` 调整为 `y=18,h=36`，与不变的 `y=-22,h=28` 副标题共同形成精确 `8/8/8px` 顶部、行间和底部留白。
- 标题字号继续为 27px、行高为 32px，副标题字号继续为 15px；不得通过缩小真实词库名、单词数量、卡片或触控节点换取留白，长名称继续使用 `SHRINK`。
- 运行时测试必须在长屏和最短屏冻结四行标题容器及三段八像素边界；Creator/微信需确认标题和副标题上下视觉重量均衡、长名称清晰且未贴框。

### 17.110 第一百零八轮：战绩记录双行文字垂直居中

- 四个战绩记录卡的标题/摘要矩形由 `y=22,h=32`、`y=-20,h=34` 调整为 `y=17,h=28`、`y=-18,h=26`；最短屏 86px 行内形成 `12/8/12px`，长屏 102px 行内形成 `20/8/20px`。
- 标题与摘要继续使用 18/13px 字号和 22/17px 行高；不得缩小真实模式、时间、成绩、详情按钮或卡片热区，横向标题/成绩/详情三列保持不变。
- 运行时测试必须在长屏和最短屏冻结四行文字框、八像素行距及相等上下内边距；Creator/微信需确认两行文字视觉居中、没有贴近上框，并与成绩和详情列保持同轴平衡。

### 17.111 第一百零九轮：战绩筛选文字与选中线等距

- 五个战绩筛选的文字轴由 `y=4` 调整为 `y=7`，六像素选中线由 `y=-21` 调整为 `y=-16`；56px 可见按钮内部形成精确 `9/8/9px` 顶部、文字/选中线和底部留白。
- 布局与点击节点继续保持 80px，不得为内部留白缩小目标设备热区；全部、PK、合作、拼词的真实筛选切换和“其他”禁用态保持不变。
- 运行时测试必须冻结可见/触控双层几何、三段内部边界和选中状态转换；Creator/微信需确认文字与短选中线整体居中，选中线不再贴近按钮底框。

### 17.112 第一百一十轮：战绩记录图标与文字列分离

- 四个战绩记录的标题/摘要矩形由 `x=-82,w=286` 调整为 `x=-64,w=258`，消除与 `x=-230,w=58` 正式战绩图标原有的 24px 重叠。
- 图标右边到文字左边、文字右边到成绩左边均保持精确 8px；真实模式和时间继续使用 `SHRINK`，不得移动或缩小成绩、详情按钮、卡片和目标设备热区换取文字空间。
- 运行时测试必须在长屏和最短屏冻结四行、两类文字框的双侧八像素边界，并保留上一轮垂直居中检查；Creator/微信需确认长模式名和时间不会压住图标，四列视觉层级清晰。

### 17.113 第一百一十一轮：帮助规则编号与文字同轴

- 六条帮助规则的标题/正文轴由 `y+20/y-18` 调整为 `y+22/y-16`，组合文字块整体上移 2px，与对应编号徽标共享精确视觉中心。
- 标题/正文矩形、18/14px 字号、八像素行距、规则间距、编号/文字八像素横向边界和全部真实规则文案不变；不得移动编号或压缩文字换取同轴效果。
- 运行时测试必须在长屏和最短屏冻结六组编号/文字组合中心，并继续检查行距和规则间距；Creator/微信需确认编号与两行说明成组对齐，不再出现文字整体轻微下沉。

### 17.114 第一百一十二轮：反馈状态文字横向安全区

- 反馈页 `500x34px` 状态带保持不变，状态文字矩形由 500px 收窄为 468px，在带内形成左右各 16px、上下各 4px 的对称安全区。
- 空态、格式校验、提交中、成功和服务端失败状态继续使用原文案、18px 字号、颜色切换及提交生命周期；不得通过缩小字号、状态带或表单宽度换取边界。
- 运行时测试必须同时冻结横向 16px 与纵向 4px 内边距；Creator/微信需使用较长校验和失败文案确认文字不贴边、不越过圆角边框，且状态切换不引发布局跳动。

### 17.115 第一百一十三轮：帮助摘要图文横向成组

- 帮助页摘要文字由 `x=52,w=360` 调整为 `x=40,w=428`，与保持 `x=-218,w=72` 的目录图标形成精确 8px 图文边界，替代原有 54px 断裂感。
- 摘要行在 560px 卡片内形成左右各 26px 的对称边界；摘要文案、17px 字号、垂直轴、正式图标、六条真实规则及返回玩法目录行为均不改变。
- 运行时测试必须在长屏和最短屏同时冻结八像素图文边界与左右对称内边距；Creator/微信需确认摘要图标和文字读作同一信息组，文字完整且不贴近卡片右框。

### 17.116 第一百一十四轮：跨页面图文信息行归一

- 反馈提示文字宽度由 424px 扩展为 448px，保持 `x=28` 与 48px 正式反馈图标不变；原有 20px 图文间距和 `28/40px` 左右边界统一为精确 8px 间距与左右各 28px。
- 词库状态文字由 `x=24,w=430` 调整为 `x=20,w=456`，保持 32px 金币图标不变；原有 25px 图文间距和 `22/31px` 左右边界统一为精确 8px 间距与左右各 22px。两行原文案、字号、垂直轴、动态状态和功能行为均不改变。
- 运行时测试必须在长屏和最短屏冻结两行各自的八像素图文边界及对称卡片边距；Creator/微信需确认图标与说明读作一个整体，真实长状态文案有足够空间且不贴右框。

### 17.117 第一百一十五轮：创建配置说明卡横向成组

- “已选模式”卡的标题和摘要列由 `x=42,w=360` 调整为 `x=50,w=388`，与保持不变的 92px 模式图标形成精确 8px 图文边界，并将卡片左右留白由 `30/52px` 统一为各 30px。
- “开始条件”文字由 `x=29,w=430` 调整为 `x=30,w=432`，原有 8px 图文边界保持不变，卡片左右留白由 `28/30px` 校正为各 28px。文案、字号、垂直轴、卡片尺寸、创建房间、更换词库和自动准备逻辑均不改变。
- 运行时测试必须在长屏和最短屏冻结模式标题、模式摘要及开始条件三行的图文边界和对称外边距；Creator/微信需确认配置说明与图标成组、两行模式信息右边界一致且长文不贴框。

### 17.118 第一百一十六轮：创建页当前词库三列等距

- “当前词库”名称列由 `x=-18,w=300` 调整为 `x=-17,w=304`，在保持 66px 正式词库图标和 118px“更换”按钮位置不变的前提下，把图标/名称与名称/按钮间距由 `9px/11px` 统一为 `8px/8px`。
- “更换”按钮内部原有 24px 图标与标题继续保持 8px 边界，并纳入精确回归。真实词库名继续使用 `SHRINK`；卡片、字号、按钮外观/热区、选择词库、返回创建页和自动准备持久化均不改变。
- 运行时测试必须在长屏和最短屏冻结完整的图标/名称/按钮三列链路及按钮内部图文边界；Creator/微信需使用较长教材单元名称确认名称不挤压图标或操作按钮。

### 17.119 第一百一十七轮：准备大厅状态点与文字成组

- “当前状态”文字由 `x=42,w=420` 调整为 `x=18,w=436`，与保持 `x=-222,w=28` 的已准备/需关注状态点形成精确 8px 图文边界，替代原有 40px 断裂感。
- 状态行在 520px 卡片内的左右留白由 `24/8px` 统一为各 24px；17px 字号、底边对齐、状态点颜色与显隐、后台安静刷新、准备选择态和开始按钮高亮均不改变。
- 运行时测试必须在长屏和最短屏冻结两种状态点的八像素图文边界与对称卡片边距，并继续覆盖等待、已准备、双方准备和错误状态；Creator/微信需确认状态点与实时文案读作同一信息组，长文不贴右框。

### 17.120 第一百一十八轮：准备大厅工具卡横向节奏

- 房间码文字由 `x=-88,w=216` 调整为 `x=-85,w=222`，112px“复制”按钮由 `x=84` 移至 `x=90`，112px“邀请”按钮保持 `x=210`；码值/复制、复制/邀请、邀请/卡片右框由 `8/14/8px` 统一为 `8/8/8px`。
- 复制和邀请按钮内部的 24px 图标/标题边界固定为 8px。“当前词库”正文由 `x=18,w=430` 调整为 `x=0,w=472`，左右留白由 `63/27px` 统一为各 24px。真实房间码、词库名、字号、热区、复制、邀请和房间状态均不改变。
- 运行时测试必须在长屏和最短屏冻结房间码完整三列链路、两个按钮内部图文边界及词库正文对称边距；Creator/微信需确认码值和两个操作等距、长词库名居中且不贴框。
