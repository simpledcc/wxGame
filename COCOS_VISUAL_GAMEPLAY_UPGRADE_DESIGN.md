# Cocos 视觉与游戏化升级目标设计说明书

## 1. 文档用途

本文档用于指导《词斗乐园单词比拼》Cocos 版本从“功能可运行”升级为“具有完整视觉风格、明确玩法氛围和持续游玩吸引力的单词学习游戏”。

后续 Codex 必须按本文档分阶段执行，每次只推进一个阶段。任何视觉升级都不能破坏当前稳定的词库、房间、计时、计分、历史记录、隐私、内容安全和云函数协议。

本文档与以下文件共同构成执行基线：

- `COCOS_MIGRATION_DESIGN.md`：业务、接口和迁移边界。
- `COCOS_REAL_PHONE_TARGET_TASK.md`：真机运行与发布验收。
- `CODEX_HANDOFF.md`：项目现状和历史问题。

## 2. 最终目标

最终版本应让玩家一进入游戏就能理解核心幻想：

> 玩家是一名“捕词员”，通过认识、选择和拼写单词，在词语生态园中发现、捕获和研究单词虫，并与伙伴完成竞赛和协作任务。

升级后的游戏必须同时满足：

1. 统一：主界面、背单词、房间、PK、合作、拼词、结算和历史记录属于同一个世界。
2. 清晰：学习信息和可点击目标始终比装饰更醒目。
3. 有反馈：正确、错误、连击、捕获、等待队友、胜利和失败都有明确反馈。
4. 有区分：不同模式有不同氛围，但不形成互不相关的多套产品。
5. 可扩展：后续可增加皮肤、角色、虫种、场景和音频，不重写业务层。
6. 流畅：中端手机目标 60 FPS，低端手机可稳定操作，不因特效阻塞输入或网络同步。
7. 可发布：主包、分包、隐私、内容安全和备案描述继续满足微信小游戏要求。

## 3. 当前基线与已知约束

### 3.1 当前工程事实

- Cocos Creator 版本为 3.8.8。
- 游戏采用 640 × 960 竖屏逻辑分辨率。
- `mode_pk` 和 `mode_spell` 已作为微信小游戏分包输出。
- `theme_default`、`theme_island` 已具备 Asset Bundle 和主题清单雏形。
- 房间、PK、默契捕词赛、同舟拼词记、历史记录和反馈已有业务实现与自动化测试。
- 当前主包约 3.93 MiB，距离 4 MiB 上限很近。
- 当前词库生成脚本体积较大，Cocos 构建会出现 Babel 超过 500 KB 的优化提示。

### 3.2 不允许破坏的能力

- 现有词库 ID、词库内容和解锁状态。
- 当前金币存储和旧版本地数据兼容。
- 房间创建、加入、邀请、准备、机器人规则和云函数接口。
- PK、合作捕词和合作拼词的计分、计时及结算规则。
- 同舟拼词记的双人提交、超时切词和每题历史详情。
- 每种模式最近 50 局和历史最佳成绩。
- 隐私授权、反馈内容安全检查和无公开 UGC 的合规边界。

### 3.3 本次升级非目标

- 不新增充值、付费、抽卡或广告系统。
- 不改变云数据库结构和云函数协议，除非单独立项并提供兼容迁移。
- 不新增用户自定义昵称、头像、聊天、签名或公开内容。
- 不在视觉阶段改变计分数值、题目规则和胜负判定。
- 不一次性替换所有页面；每个阶段必须形成可运行、可回退、可验收的小闭环。

## 4. 世界观与美术方向

### 4.1 核心主题：词语生态园

单词以“单词虫”的形式生活在不同生态区域。玩家通过学习含义、识别英文和完成拼写来发现和捕获它们。

视觉叙事只作为游戏表现层，不改变题目和得分规则：

- 学习一个单词：发现并记录一种单词虫。
- 选择正确英文：使用捕虫网完成捕获。
- 选择错误英文：捕虫网落空，虫子短暂警觉。
- 连续正确：形成捕获连击和能量增长。
- 双人 PK：两名捕词员在竞技场比拼速度和准确率。
- 默契捕词赛：两名玩家共同完成团队捕获进度。
- 同舟拼词记：两名玩家驾驶叶舟，用各自字母修复单词航标。

### 4.2 视觉关键词

- 卡通、明快、轻松、自然、益智。
- 轮廓清晰，避免过度细碎的装饰。
- 2D 分层插画与少量骨骼或序列帧动画。
- 圆润但不幼稚，适合学生和家庭用户。
- 使用深色文字保证学习内容可读，不把文字直接压在复杂背景上。

### 4.3 主角色与视觉 IP

首版只建立一名固定系统角色和一组虫子，不开放角色自定义：

- 主角色：中性化的年轻捕词员，绿色帽子、黄色背包、捕虫网，表情积极。
- 引导伙伴：一只发光的小萤火虫，用于加载、提示、等待队友和结果反馈。
- 单词虫：圆润甲虫、瓢虫、萤火虫、叶虫、蜂虫等轮廓家族。
- 单词文字应位于虫子携带的叶片牌、腹部光牌或独立标签中，不能被翅膀和特效遮挡。

角色和虫子必须支持颜色以外的形状区分，避免仅靠红绿判断状态。

### 4.4 基础配色

统一色板建议如下，实际实现应进入主题 Token，不在业务组件内散落十六进制颜色：

| Token | 建议颜色 | 用途 |
| --- | --- | --- |
| `ink` | `#18323A` | 主文字、轮廓 |
| `cloud` | `#FFFDF6` | 内容底、文字牌 |
| `leaf` | `#2F9E62` | 主操作、成长、正确 |
| `sun` | `#F7C948` | 奖励、金币、重点 |
| `coral` | `#F46F61` | PK、攻击、警示强调 |
| `sky` | `#37A7D8` | 信息、计时、自己 |
| `partner` | `#D95B9B` | 队友、合作分区 |
| `danger` | `#D94B4B` | 错误、扣分 |
| `fog` | `#6B7F85` | 次级文字、禁用 |

界面不能被单一绿色或蓝色支配。背景、主按钮、奖励和对抗反馈必须形成清晰层次。

### 4.5 字体与文字

- 中文正文优先使用系统字体，避免引入体积巨大的完整中文字库。
- 游戏 Logo 可使用图片字或小范围定制字形。
- 正文建议 18 至 22 逻辑像素，重要按钮不低于 22。
- 分数和倒计时可使用小型数字位图字体，但必须提供普通字体回退。
- 英文单词使用高可读的无衬线字体，不使用花体。
- 所有 Label 保持 `SHRINK` 或受控换行，长单词和长中文释义不得越界。

## 5. 统一界面语言

### 5.1 竖屏布局规则

- 逻辑分辨率保持 640 × 960。
- 左右安全边距至少 24。
- 顶部从系统安全区下方再留 16。
- 底部从安全区上方再留 16。
- 主要触控区域不小于 56 × 56。
- 页面按“顶部状态区、中部核心场景、底部操作区”组织。
- 重要学习信息不得与微信胶囊按钮、刘海和底部手势区重叠。

### 5.2 UI 组件规范

需要建立统一组件或 Prefab：

- `GameTopBar`：返回、标题、金币、设置。
- `PrimaryButton`、`SecondaryButton`、`DangerButton`、`IconButton`。
- `WordCard`：英文、中文、状态和标签。
- `ScoreBadge`、`TimerBadge`、`ComboBadge`。
- `PlayerBadge`：自己、对手、队友和准备状态。
- `ModeEntry`：模式图标、名称、简短状态和进入按钮。
- `ResultRibbon`：胜利、失败、团队完成。
- `Toast`、`LoadingOverlay`、`ConfirmDialog`。

按钮使用九宫格拉伸资源，不为每个宽度重复制作图片。图标使用统一线宽、轮廓和阴影方向。

### 5.3 交互反馈规范

| 事件 | 视觉反馈 | 建议时长 |
| --- | --- | --- |
| 按下按钮 | 缩放至 0.96、亮度降低 | 60–90 ms |
| 松开按钮 | 回弹至 1.00 | 100–140 ms |
| 正确 | 绿色光环、捕获轨迹、`+分数` 上浮 | 350–650 ms |
| 错误 | 目标轻摇、红色短闪、`-分数` 上浮 | 220–450 ms |
| 连击 | 连击徽章弹跳、能量条增长 | 200–350 ms |
| 等待云端 | 保留本地反馈，状态栏显示小型同步动画 | 不阻塞输入层 |
| 等待队友 | 队友徽章呼吸，不重复弹窗 | 循环但低频 |
| 胜利 | 角色庆祝、奖章、分数计数 | 1.2–2.0 s |
| 失败 | 角色短暂失落后显示重试入口 | 0.8–1.4 s |

动画不得改变业务状态。云端校正发生时，应平滑校正数值并显示“已同步”，不能重新播放完整捕获动画。

## 6. 各页面和玩法目标

### 6.1 Boot 与隐私页

主题表现：清晨生态园入口。

- 使用轻量静态背景、Logo 和萤火虫加载动画。
- 隐私正文始终位于高对比度面板中。
- “查看隐私保护指引”“同意并进入”“暂不进入”层级清楚。
- 用户同意前不加载大型主题和玩法分包。
- Boot 主包新增资源预算不超过 40 KB。

### 6.2 首页

主题表现：捕词营地和任务地图。

- 第一视觉信号是游戏 Logo、主角色和当前学习区域。
- 当前词库、时长、金币和历史最佳保持可见，但不堆叠成工具型表单。
- “开始背”作为主任务入口，尺寸和位置最突出。
- “双人 PK”“双人合作”作为两块有场景缩略图的模式入口。
- 换词库、历史、玩法、反馈放在清晰的次级导航中。
- 不增加无功能的设置、任务、商城和签到按钮。
- 开发日志、性能报告和主题调试入口仅在 `DEV` 下显示。

### 6.3 词库选择

主题表现：生态图鉴和探索地图。

- 年级、教材、单元形成清晰层级。
- 已解锁、当前选中和未解锁使用图标、文字和颜色共同表达。
- 金币解锁沿用现有业务规则，不新增购买文案。
- 页面应优先保证长词库名称完整显示。
- 确认后继续返回来源页面。

### 6.4 背单词模式

主题表现：晨光温室中的单词图鉴。

- 英文是页面最大信息，中文释义是第二层信息。
- 隐藏中文后，页面保持探索感，不显示空白的大面板。
- “查看本词释义”和“下一个”放在拇指容易触达的底部区域。
- 上一个、随机、加入错题库和换词库作为次级操作。
- 每切换一个单词，单词虫或图鉴卡有轻量入场动画。
- 显示当前释义时使用翻页或揭示动画，不改变全局隐藏设置。
- 首版成长反馈只表现为本地视觉进度，不新增服务端等级系统。

### 6.5 房间页

主题表现：任务集结营地。

- 房间码像任务旗帜一样突出，复制和邀请入口紧邻。
- 两个玩家使用明确的左右或上下席位，显示准备状态。
- 自己与队友/对手不仅靠颜色区分，还使用头像框形状和文字。
- 创建、加入、准备和开始按钮根据房间状态变化，不能同时争夺注意力。
- PK 允许现有机器人规则；两个合作模式继续要求两名真实玩家。
- 网络同步状态使用小图标或状态文字，不使用持续遮挡页面的加载层。

### 6.6 双人 PK

主题表现：暮光捕词竞技场。

页面结构：

1. 顶部：中文提示、双方分数、总倒计时。
2. 中部：三条捕虫跑道，每条显示两个单词虫。
3. 底部：连击、道具、状态提示和离开入口。

氛围要求：

- 自己使用 `sky`，对手使用 `coral`，中间倒计时使用 `sun`。
- 正确捕获时虫子被网兜吸入己方收集罐。
- 错误时捕虫网落空，虫子做短促躲闪，不播放过长惩罚动画。
- 连击达到阈值时只增强边缘光和徽章，不遮挡单词。
- 道具保留现有逻辑，只替换为统一图标和施放表现。

#### 6.6.1 PK 虫子重叠强制修复

当前问题原因：`PkWordTarget` 实例各自在相同的全宽 `leftBound/rightBound` 内独立移动。同一行的两个目标会进入相同横向区域，因此发生视觉重叠或碰撞。

目标方案：新增集中式 `TargetMotionController` 或等价的纯逻辑运动调度器。

- 保持三行、每行两个目标。
- 每行划分左、右两个运动区段，中间保留最小间隔。
- 左目标只能在左区段运动，右目标只能在右区段运动。
- 虫子可有不超过 4 像素的上下浮动，但基础 Y 由车道固定。
- 运动相位根据目标 ID 或槽位确定，不能每帧随机。
- 题目刷新时复用节点并重置位置、方向、速度和动画状态。
- 长单词目标可以缩小字体，但不能扩大碰撞盒侵入另一运动区段。
- 不引入刚体物理系统处理 UI 目标碰撞，避免额外性能和同步复杂度。

必须增加运动模拟测试：

- 6 个目标运行至少 10,000 帧。
- 任意帧目标 AABB 不重叠。
- 所有目标始终位于竞技场边界内。
- 固定种子和相同输入得到相同运动结果。
- 切换题目后不存在旧目标继续更新。

### 6.7 默契捕词赛

主题表现：萤火温室中的团队捕获任务。

- 中央显示团队共同目标和团队总分。
- 两名玩家分列两侧，捕获动作流向同一个团队收集罐。
- 自己贡献、队友贡献和团队连击都可见，但总分最突出。
- 队友操作到达时播放较轻的伙伴轨迹，避免和本地操作抢焦点。
- 队友离开、超时或网络恢复时有明确状态，不让页面停在假等待中。
- 不出现机器人按钮。

### 6.8 同舟拼词记

主题表现：叶舟溪谷和萤火航标。

- 保留“同舟”意象，船体改为叶舟或生态研究艇，与捕虫世界统一。
- 船和旗帜不能遮挡单词、空格、倒计时和键盘。
- 自己负责的两个空格更大、更亮，显示“请填”。
- 队友区域更小，显示队友填写中/已提交，不公开队友未提交答案。
- 26 键 QWERTY 键盘必须保持手机键盘习惯，删除键固定右下角。
- 按键最小触控高度 56，按下有缩放和高亮反馈。
- 自己提交后，按键区变为等待状态；进入新题后所有本地字母和提交状态清空。
- 单题 20 秒和总时长规则保持不变。
- 双方提交后，正确时点亮航标，错误时船轻晃并显示扣分。

### 6.9 结算与奖励

结算页不是普通数据表，应形成一局结束的情绪闭环：

- PK：胜利旗帜、双方分数、胜负角色动作。
- 默契捕词赛：团队收集罐、总分和双方贡献。
- 同舟拼词记：航行距离、团队总分和正确/错误题数。
- 分数从旧值平滑计数到最终值，但总时长不超过 1.2 秒。
- 奖励只展示当前真实存在的金币或成绩，不虚构未入账道具。
- “再来一局”“返回首页”“查看战绩”层级明确。
- 失败也给出已完成的学习量和继续入口，避免纯负反馈。

### 6.10 历史记录与详情

- 使用图鉴册或任务日志视觉，但保持列表扫描效率。
- 三种模式名称与首页、房间和备案一致。
- 历史最佳使用奖章表现。
- 同舟拼词详情继续显示每个单词、双方填写和错误来源。
- 详情页首先保证文字可读，不添加遮挡内容的复杂背景动画。

## 7. 动画与特效架构

### 7.1 分层原则

动画必须分为三层：

1. 业务状态层：分数、题目、房间、计时和结算，保持现有 Store/Service 控制。
2. 表现事件层：把业务变化转换为一次性 `VisualEvent`。
3. 播放层：对象池、Tween、序列帧、粒子和音频消费表现事件。

建议事件结构：

```ts
interface VisualEvent {
  id: string;
  type: "correct" | "wrong" | "capture" | "score" | "combo" | "submit" | "win" | "lose";
  targetId?: string;
  delta?: number;
  playerSide?: "local" | "opponent" | "partner" | "team";
  createdAt: number;
}
```

同一事件 ID 只能播放一次。页面销毁、切后台或切题时必须取消旧 Tween、回收节点并清空事件队列。

### 7.2 动画技术选择

- UI 位移、缩放、透明度：Cocos Tween。
- 虫子翅膀和角色表情：小型序列帧或骨骼动画，优先序列帧。
- 捕获轨迹、星光和得分粒子：对象池管理的 Sprite/ParticleSystem2D。
- 背景：分层静态图加少量低频循环，不做全屏高开销 Shader。
- 数值跳字：复用现有反馈池思想，扩展为统一 `FloatingTextPool`。
- 不在 `update()` 中创建节点、材质、Color、数组或闭包。

### 7.3 动效降级

需要提供标准和简化两档表现：

- 标准：完整 Tween、少量粒子、角色动作。
- 简化：关闭背景循环和多余粒子，保留输入反馈、分数和关键状态。

当平均帧耗时连续超标、应用切后台或设备性能等级较低时，自动使用简化档。降级不能影响题目和输入。

## 8. 音频目标

当前版本没有完整声音资源，音频放在独立阶段实现。

### 8.1 音频分类

- 首页环境音乐：轻松、自然、弱节奏。
- 背单词：安静、低干扰。
- PK：节奏更快，但不能遮盖提示音。
- 合作：温暖、共同推进感。
- 正确、错误、点击、捕获、连击、提交、倒计时、胜利、失败音效。

### 8.2 音频规则

- 音乐和音效分开控制，为未来背景音乐预留独立开关。
- 默认音量保守，音效不能突然高于背景音乐。
- 切后台暂停，回前台按状态恢复。
- 短时间重复点击同一音效需要限频或复用 AudioSource。
- 音频资源进入对应主题或玩法 Bundle，不挤占主包。
- 没有真实音频资源前，不显示无效声音开关。

## 9. 美术资源规范

### 9.1 目录建议

```text
cocos-client/assets/
  art-core/
    boot/
    logo/
    ui/
  bundles/
    theme_word_garden/
      backgrounds/
      characters/
      common-effects/
      ui-atlas/
    mode_study/
      atlas/
      effects/
    mode_pk/
      atlas/
      effects/
      audio/
    mode_spell/
      atlas/
      effects/
      audio/
```

目录可以按现有 Bundle 结构调整，但主包与玩法资源边界必须清楚。

### 9.2 文件命名

- 全部使用小写英文、数字和短横线。
- 背景：`bg-home-camp.jpg`。
- 角色：`character-catcher-idle-01.png`。
- 虫子：`bug-leaf-fly-01.png`。
- 图标：`icon-mode-pk.png`。
- 特效：`fx-capture-spark-01.png`。
- 音效：`sfx-answer-correct-01.mp3`。

### 9.3 尺寸与预算

| 资源 | 建议规格 | 单项预算 |
| --- | --- | --- |
| 竖屏背景 | 720 × 1280 JPG | ≤ 180 KB |
| 模式缩略图 | 320 × 180 JPG/PNG | ≤ 60 KB |
| UI 图集 | 1024 × 1024 PNG | ≤ 350 KB |
| 虫子序列帧图集 | 1024 × 1024 PNG | ≤ 400 KB |
| 主角色图集 | 1024 × 1024 PNG | ≤ 450 KB |
| 单个图标 | 96 × 96 或进入图集 | ≤ 20 KB |
| 单个短音效 | MP3/WAV | ≤ 80 KB |
| 单首循环音乐 | MP3 | ≤ 500 KB |

透明资源优先进入图集，大背景使用 JPG。平台纹理压缩只能在完成 Android 和 iOS 真机验证后启用。

### 9.4 AI 生成资源要求

若后续使用图像生成工具：

- 先固定角色比例、轮廓、光源方向和色板，再批量生成。
- 每个角色和虫子必须有透明背景、统一视角和足够留白。
- 图片中的英文单词、分数和按钮文字必须由 Cocos Label 绘制，不能生成在位图中。
- AI 资源必须经过人工检查，排除多余肢体、错误文字、破碎边缘和风格漂移。
- 首轮只生成少量代表资源，通过真机效果后再扩充。

## 10. 包体与加载策略

当前主包接近 4 MiB，视觉升级前必须先建立资源预算门禁。

### 10.1 主包规则

- 主包目标降至不高于 3.75 MiB，保留至少 256 KB 安全余量。
- 主包只保留 Boot、隐私、最小 Logo、运行时代码和加载占位。
- 新增到主包的图片总量不得超过 40 KB，除非同时移出更多旧资源。
- 大型词库数据需要单独评估拆分或压缩，不能继续无上限增加生成 TS。

### 10.2 Bundle 规则

- 首页首屏主题资源按需加载，失败时回退纯色和最小 UI。
- PK 动画、虫子、道具和音效进入 `mode_pk`。
- 拼词船、键盘皮肤和音效进入 `mode_spell`。
- 背单词专属角色和图鉴资源可进入 `mode_study` 或默认主题 Bundle。
- 公共资源只能放一份，禁止多个 Bundle 重复拷贝同一大图。
- 进入玩法前允许显示 0.3 至 1.5 秒的主题加载页；加载失败必须可重试或回首页。

### 10.3 构建门禁

每个阶段必须运行：

```powershell
npm run verify
npm run build:wechat
npm run inspect:wechat-build
```

构建报告必须记录主包、每个分包和总包大小。超过预算时当前阶段不能标记完成。

## 11. 性能预算

### 11.1 帧率目标

- 中端 Android 和常用 iPhone：玩法平均接近 60 FPS。
- P95 单帧耗时不高于 24 ms。
- 低端设备允许降低特效，但输入和倒计时不能卡住。
- 单次点击本地视觉反馈目标小于 50 ms。

### 11.2 渲染预算

| 指标 | 首页 | 玩法页 |
| --- | --- | --- |
| 活跃节点 | ≤ 250 | ≤ 350 |
| Draw Call | ≤ 40 | ≤ 60 |
| 同时活跃粒子 | ≤ 30 | ≤ 80 |
| 动态 Label | ≤ 12 | ≤ 20 |
| 持续 Tween | ≤ 10 | ≤ 24 |

这些是目标预算，不是用删除必要反馈来硬凑数字。超标时优先合图、对象池、减少透明叠层和停止屏外动画。

### 11.3 内存与生命周期

- 切换玩法后释放不再需要的 Bundle 资源或保留受控缓存。
- 页面退出后没有旧 Tween、定时器、轮询和事件订阅。
- 切后台暂停非必要动画和音频。
- 连续进入退出同一玩法 20 次，节点数和内存不持续增长。

## 12. 问题审计清单

每个改造阶段不仅做美化，还要检查与当前页面相关的问题：

| 类别 | 必查问题 | 处理要求 |
| --- | --- | --- |
| 布局 | 刘海、胶囊、底部手势区、长文字、窄屏 | 自动化边界检查加真机截图 |
| 输入 | 重复点击、按钮过小、触摸穿透 | 防重入且本地反馈立即发生 |
| 动画 | 旧动画跨题、跨房间、跨路由继续播放 | 退出和切题统一取消与回收 |
| PK 目标 | 虫子重叠、越界、速度突变、长词遮挡 | 车道调度器和运动模拟测试 |
| 网络 | 云端慢时 UI 冻结、旧请求覆盖新状态 | 保持现有请求 ID 和状态校正机制 |
| 拼词 | 新题携带旧字母、提交状态不清、时间不同步 | 题目 ID 驱动本地状态重置 |
| 合作 | 队友反馈重复、假等待、机器人入口 | 快照去重并遵守两名真人规则 |
| 资源 | Bundle 重复、加载失败黑屏、主包超限 | 构建门禁和回退资源 |
| 性能 | 每帧创建对象、粒子过多、Label 过多 | 对象池和性能报告 |
| 音频 | 重复播放、切后台不断、无效开关 | AudioService 生命周期统一管理 |
| 合规 | 自定义昵称、充值文案、反馈未安全检查 | 发布静态检查继续通过 |

发现问题时应在当前阶段修复与该页面直接相关的问题；跨模块问题记录到阶段进度，不进行无边界重构。

## 13. 自动化和真机验收

### 13.1 自动化测试

除现有测试外，升级期间逐步增加：

- 竖屏 UI 边界测试覆盖所有路由和弹窗。
- 主题 Token、资源路径和 Bundle 清单一致性测试。
- PK 车道 10,000 帧不重叠模拟测试。
- 表现事件 ID 去重测试。
- 对象池容量和复用测试。
- 页面销毁后 Tween、定时器和订阅清理测试。
- 音效限频与后台暂停测试。
- 构建包大小和分包位置测试。

### 13.2 截图矩阵

每个视觉阶段至少保留：

- 360 × 800 级别窄屏 Android。
- 393 × 852 级别常见 iPhone。
- 430 × 932 级别大屏手机。
- 有刘海/胶囊和底部手势区的真实微信环境。

核心截图包括首页、当前阶段页面、加载、空状态、错误状态和结算状态。

### 13.3 双人真机

涉及房间和多人玩法时，必须使用两台真实设备或两个真实微信账号：

- 创建、邀请、加入和准备。
- 一局正常完成。
- 一方切后台再返回。
- 一方弱网或超时。
- 双方结算和历史记录一致。

## 14. 分阶段实施计划

阶段编号使用 `V0` 至 `V9`，与原 Cocos 迁移阶段区分。

### V0：视觉基线与问题审计

目标：冻结当前可运行版本，建立后续可对比证据。不要修改业务代码。

交付物：

- 当前所有页面截图清单。
- 主要交互录屏清单。
- 节点数、帧耗时、Draw Call 和包体报告。
- 已知问题表，至少包含 PK 虫子重叠。
- 当前资源清单和 Bundle 归属表。
- 视觉验收检查表。

通过标准：可以明确回答每个页面“改前是什么样、有哪些问题、改后如何判断更好”。

### V1：视觉基础设施与美术样板

目标：建立主题 Token、通用 Prefab、资源命名和一套最小美术样板。

实现范围：

- ThemeManifest v2 或兼容扩展。
- 通用按钮、顶部栏、徽章、WordCard、Toast 和加载层。
- Logo、主角色静态形象、三种虫子、基础 UI 图集。
- 首页、PK、拼词各一张低保真场景样板。
- 资源和包体自动检查。

通过标准：样板在三种手机尺寸完整显示；切换主题不修改业务代码；主包仍低于目标预算。

### V2：首页、Boot 和导航升级

目标：完成玩家进入游戏后的统一第一印象。

实现范围：

- Boot、隐私、Logo、轻量加载动画。
- 捕词营地首页。
- 模式入口、当前词库、时长、金币和历史最佳。
- 次级导航图标。

通过标准：所有入口功能可用；没有占位按钮；隐私同意前不加载云能力和大型资源；竖屏无裁切。

### V3：词库与背单词升级

目标：建立“探索、发现、图鉴成长”的单人氛围。

实现范围：

- 词库生态图鉴表现。
- 背单词图鉴卡和单词虫入场。
- 隐藏中文、查看本词释义、下一个的拇指区布局。
- 切词、揭示、加入错题库反馈。

通过标准：现有学习逻辑全部通过；长单词和长释义完整；连续切换 100 个单词无状态残留和节点增长。

### V4：PK 竞技场、虫子运动和战斗反馈

目标：提升竞争感并彻底解决虫子重叠问题。

实现范围：

- 暮光竞技场场景和双方 HUD。
- `TargetMotionController` 或等价车道调度器。
- 单词虫 Prefab、捕获、错误、连击和道具表现。
- 本地即时反馈与云端校正的视觉衔接。
- 运动、快速点击、旧响应隔离和性能测试。

通过标准：10,000 帧无重叠越界；点击反馈小于 50 ms；连续 PK 3 分钟无明显掉帧、节点增长或分数重复。

### V5：默契捕词赛团队表现

目标：让合作捕词与 PK 明显不同，突出共同目标。

实现范围：

- 团队场景、双方席位、团队收集罐和共同进度。
- 本地与队友反馈层级。
- 等待、断线、恢复和团队结算表现。

通过标准：两台手机完成一局；团队总分一致；队友反馈不重复；不出现机器人入口。

### V6：同舟拼词记场景与键盘

目标：建立伙伴协作和共同航行感，同时保持输入效率。

实现范围：

- 叶舟场景、航标、自己与队友空格区。
- QWERTY 键盘、删除、清空、提交和跳过反馈。
- 提交等待、超时切词、正确和错误表现。
- 新题完整重置本地视觉状态。

通过标准：两台手机完成一局；旧字母不进入新题；20 秒超时正确切题；键盘在窄屏完整可点。

### V7：结算、奖励和历史记录

目标：形成完整局后情绪与成就闭环。

实现范围：

- 三种模式的差异化结算。
- 分数计数、奖章、角色状态和真实奖励展示。
- 历史列表和同舟拼词详情视觉升级。

通过标准：结果与服务端一致；历史最近 50 局和最佳成绩不变；拼词详情信息完整。

### V8：音频、整体动效和性能收敛

目标：补齐声音和最终手感，同时完成低端机降级。

实现范围：

- AudioService、音乐/音效独立设置和生命周期。
- 各模式最小音乐与音效集。
- 标准/简化动效档位。
- 图集、对象池、Bundle、内存和 Draw Call 优化。

通过标准：切后台正确暂停；快速点击不爆音；性能指标达到预算；关闭音效不影响未来背景音乐开关。

### V9：真机 QA、合规和发布候选

目标：形成可提交开发版和审核的发布候选包。

实现范围：

- 三种尺寸截图矩阵。
- 两台手机完成 PK、默契捕词和同舟拼词。
- 弱网、后台、重复进入、长时间运行和资源加载失败测试。
- 隐私、内容安全、备案截图和发布文案核对。
- 最终包体和性能报告。

通过标准：无阻断问题；主包和分包通过检查；所有主要功能真机可完成；提交材料与当前版本一致。

## 15. 阶段执行规则

每次执行一个阶段时，Codex 必须：

1. 阅读本文档、`COCOS_MIGRATION_DESIGN.md`、`COCOS_REAL_PHONE_TARGET_TASK.md` 和 `CODEX_HANDOFF.md`。
2. 检查当前 Git 状态，不覆盖用户已有修改。
3. 先运行与本阶段相关的现有测试并记录基线。
4. 只实现当前阶段内容，不提前扩张其他玩法。
5. 美术资源进入明确 Bundle，不把大资源直接放主包。
6. 新增动画必须有销毁、回收和后台暂停路径。
7. 修改后运行阶段测试、`npm run verify`、微信构建和包体检查。
8. 需要视觉验收时使用开发者工具或真实手机截图，不以代码存在代替画面验收。
9. 更新本文档进度表和交接记录。
10. 未达到通过标准时不能标记阶段完成。

## 16. 后续 Codex 目标模板

```text
请按照 COCOS_VISUAL_GAMEPLAY_UPGRADE_DESIGN.md 执行阶段 V<N>：<阶段名称>。

要求：
1. 先阅读设计书、COCOS_MIGRATION_DESIGN.md、COCOS_REAL_PHONE_TARGET_TASK.md 和 CODEX_HANDOFF.md。
2. 检查当前分支、工作区、现有资源和相关页面实现。
3. 先运行本阶段相关测试，记录改造前基线。
4. 只实现本阶段要求，不改变现有词库、计分、房间和云函数协议。
5. 对本阶段涉及页面同时检查布局、输入、动画生命周期、资源加载和性能问题。
6. 修改后运行相关测试、完整 npm run verify、npm run build:wechat 和 npm run inspect:wechat-build。
7. 使用开发者工具或真机截图进行视觉验收，并报告包体和性能变化。
8. 更新设计书进度和交接记录；未达到验收标准时不要标记完成。
```

## 17. 进度表

| 阶段 | 状态 | 结果证据 | 备注 |
| --- | --- | --- | --- |
| V0 视觉基线与问题审计 | 进行中 | `COCOS_VISUAL_BASELINE_V0.md`、`cocos-client/build/visual-baseline/` | 已完成代码、资源、包体、Web 实测截图、FPS/Draw Call、节点基线，以及本地审计模拟的 PK/默契捕词/拼词/拼词结算路径；发现捕词模式结算时 `GameplayFeedbackPool` 销毁异常。待微信真机双人对局、结算、性能 JSON、3 分钟轮询和该错误的微信复验。 |
| V1 视觉基础设施与美术样板 | 未开始 |  |  |
| V2 首页、Boot 和导航升级 | 未开始 |  |  |
| V3 词库与背单词升级 | 未开始 |  |  |
| V4 PK 竞技场与虫子运动 | 未开始 |  | 重点修复重叠 |
| V5 默契捕词赛团队表现 | 未开始 |  |  |
| V6 同舟拼词记场景与键盘 | 未开始 |  |  |
| V7 结算、奖励和历史记录 | 未开始 |  |  |
| V8 音频、动效和性能收敛 | 未开始 |  |  |
| V9 真机 QA 与发布候选 | 未开始 |  |  |

## 18. 整体完成定义

只有同时满足以下条件，视觉与游戏化升级才算完成：

- 玩家能从首页、角色、场景和反馈中明确感知“背单词捕虫”主题。
- 背单词、PK、默契捕词赛和同舟拼词记具有不同氛围且视觉统一。
- 正确、错误、捕获、连击、提交、等待、胜利和失败都有清晰反馈。
- PK 虫子不会重叠、越界或遮挡单词。
- 所有页面在竖屏常见尺寸和安全区域内完整显示。
- 动画、音频和资源加载不会阻塞输入和云端同步。
- 主包、分包、帧率、节点和 Draw Call 达到预算。
- 两台真实手机可以完成三种多人模式并正确结算。
- 隐私、内容安全、历史记录和发布材料保持合规且与版本一致。
